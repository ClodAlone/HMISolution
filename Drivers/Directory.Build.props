<Project>
  <Import Project="..\HMISolution\Version.props" />
  <PropertyGroup>
    <!-- Copy NuGet package DLLs to the build output so they can be deployed alongside drivers -->
    <CopyLocalLockFileAssemblies>true</CopyLocalLockFileAssemblies>
  </PropertyGroup>
</Project>
