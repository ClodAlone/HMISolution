<UserControl x:Class="WpfApp3.Plugins.HtmlEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{DynamicResource PanelBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <Button Content="Open" Click="Open_Click" Margin="0,0,5,0" Padding="5,2" Style="{StaticResource TitleBarButtonStyle}" Background="#3E3E42" BorderThickness="1"/>
            <Button Content="Save" Click="Save_Click" Margin="0,0,5,0" Padding="5,2" Style="{StaticResource TitleBarButtonStyle}" Background="#3E3E42" BorderThickness="1"/>
            <Button Content="Refresh Preview" Click="Refresh_Click" Padding="5,2" Style="{StaticResource TitleBarButtonStyle}" Background="#3E3E42" BorderThickness="1"/>
            <TextBlock x:Name="StatusText" Text="" Foreground="{DynamicResource TextForegroundBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="EditorBox" 
                     AcceptsReturn="True" 
                     AcceptsTab="True"
                     FontFamily="Consolas" 
                     FontSize="12"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     Background="{DynamicResource EditorBackgroundBrush}"
                     Foreground="{DynamicResource TextForegroundBrush}"
                     BorderThickness="0"
                     TextChanged="EditorBox_TextChanged"/>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}"/>

            <WebBrowser x:Name="PreviewBrowser" Grid.Column="2" Margin="5"/>
        </Grid>
    </Grid>
</UserControl>