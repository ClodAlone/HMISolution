<UserControl x:Class="WpfApp3.Plugins.GraphicEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar Grid.Row="0">
            <Button Content="Open" Click="Open_Click"/>
            <Button Content="Save" Click="Save_Click"/>
            <Separator />
            <Button Content="Import SVG" Click="ImportSVG_Click"/>
            <Button Content="Export SVG" Click="ExportSVG_Click"/>
        </ToolBar>
        <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="DesignCanvas" Width="2000" Height="2000" AllowDrop="True" Drop="DesignCanvas_Drop" DragOver="DesignCanvas_DragOver" PreviewMouseLeftButtonDown="DesignCanvas_PreviewMouseLeftButtonDown" MouseMove="DesignCanvas_MouseMove" MouseLeftButtonUp="DesignCanvas_MouseLeftButtonUp">
                <Canvas.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Group" Click="Group_Click" />
                        <MenuItem Header="Ungroup" Click="Ungroup_Click" />
                    </ContextMenu>
                </Canvas.ContextMenu>
                <Canvas.Background>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute" Opacity="0.1">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing>
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,20,20"/>
                                </GeometryDrawing.Geometry>
                                <GeometryDrawing.Pen>
                                    <Pen Brush="{DynamicResource TextForegroundBrush}" Thickness="1"/>
                                </GeometryDrawing.Pen>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Canvas.Background>
            </Canvas>
        </ScrollViewer>
    </Grid>
</UserControl>