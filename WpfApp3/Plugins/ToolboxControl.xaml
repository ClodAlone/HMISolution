<UserControl x:Class="WpfApp3.Plugins.ToolboxControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="200">
    <Grid Background="{DynamicResource PanelBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ToolsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Tag="{Binding Content}" Margin="5,2" Padding="5" Click="Tool_Click" Style="{StaticResource TitleBarButtonStyle}" Background="#3E3E42" BorderThickness="1">
                            <StackPanel>
                                <Border Width="50" Height="30" Margin="0,0,0,2" Background="Transparent" ClipToBounds="True">
                                    <Viewbox Stretch="Uniform">
                                        <ContentPresenter Content="{Binding Visual}" />
                                    </Viewbox>
                                </Border>
                                <TextBlock Text="{Binding Name}" HorizontalAlignment="Center" FontSize="10" TextWrapping="Wrap" MaxWidth="60"/>
                            </StackPanel>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>