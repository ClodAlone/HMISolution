# Exclude everything by default (repo root has many unrelated projects)
*

# Whitelist only what the Dockerfiles need
!SharedModels/
!Drivers/
!Server/
!ServerEditorWeb/

# Re-exclude build artifacts inside whitelisted folders
**/bin/
**/obj/
**/.vs/
**/*.user
