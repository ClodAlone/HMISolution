<Application x:Class="WpfApp2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WpfApp2"
             xmlns:ad="http://schemas.xceed.com/wpf/xaml/avalondock"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Dark theme colors (default) -->
        <SolidColorBrush x:Key="BackgroundColor" Color="#2D2D30" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="ForegroundColor" Color="#E6E6E6" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="AccentColor" Color="#007ACC" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="PanelBackgroundColor" Color="#252526" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="PanelBorderColor" Color="#333337" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="ControlBorderColor" Color="#1E1E1E" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>

        <!-- System Colors Override -->
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#252526" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="#E6E6E6" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#2D2D30" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="#E6E6E6" PresentationOptions:Freeze="False" xmlns:PresentationOptions="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"/>

        <!-- Window Styling -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderColor}"/>
        </Style>

        <!-- Menu Styling -->
        <Style TargetType="Menu">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PanelBorderColor}"/>
        </Style>

        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
        </Style>

        <!-- StackPanel Styling -->
        <Style TargetType="StackPanel">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
        </Style>

        <!-- TreeView Styling -->
        <Style TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>

        <!-- TextBlock Styling -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
        </Style>

        <!-- TextBox Styling -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundColor}"/>
        </Style>

        <!-- Grid Styling -->
        <Style TargetType="Grid">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
        </Style>

        <!-- Button Styling -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource AccentColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                               BorderBrush="{TemplateBinding BorderBrush}" 
                               BorderThickness="{TemplateBinding BorderThickness}"
                               CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center" 
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ProgressBar Styling -->
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="{DynamicResource PanelBorderColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- ListBox Styling -->
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ControlBorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- AvalonDock Styling -->
        <Style TargetType="ad:DockingManager">
            <Setter Property="Background" Value="{DynamicResource PanelBackgroundColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}"/>
        </Style>
    </Application.Resources>
</Application>
