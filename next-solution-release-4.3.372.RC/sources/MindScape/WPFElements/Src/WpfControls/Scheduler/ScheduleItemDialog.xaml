<UserControl x:Class="Mindscape.WpfElements.ScheduleItemDialog"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:ms="clr-namespace:Mindscape.WpfElements">
  <UserControl.Resources>
    <ResourceDictionary>
      <ms:BooleanToObjectConverter x:Key="RecurrenceButtonContentConverter" TrueObject="Edit Recurrence" FalseObject="Add Recurrence" />
    </ResourceDictionary>
  </UserControl.Resources>

  <Grid x:Name="LayoutRoot" Margin="10">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="70" />
      <ColumnDefinition Width="10" />
      <ColumnDefinition />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="10" />
      <RowDefinition Height="22" />
      <RowDefinition Height="10" />
      <RowDefinition Height="22" />
      <RowDefinition Height="10" />
      <RowDefinition Height="22" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <Button Command="{x:Static ms:SchedulerCommands.EditRecurrenceCommand}" Grid.Column="2" Width="100" HorizontalAlignment="Left"
            Content="{Binding HasRecurrence, Converter={StaticResource RecurrenceButtonContentConverter}}" />

    <TextBlock TextAlignment="Right" VerticalAlignment="Center" Grid.Row="2">Subject:</TextBlock>
    <TextBox Text="{Binding Subject, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="2" />

    <TextBlock TextAlignment="Right" VerticalAlignment="Center" Grid.Row="4">Start time:</TextBlock>
    <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="2">
      <ms:DropDownDatePicker Width="205" Margin="0,0,10,0" Value="{Binding StartDate, Mode=TwoWay}" Format="Custom" CustomFormat="dd/MM/yyyy" />
      <ms:TimePicker Width="80" SelectedTime="{Binding StartTime, Mode=TwoWay}" ItemsSource="{Binding StartTimeList}" />
    </StackPanel>

    <TextBlock TextAlignment="Right" VerticalAlignment="Center" Grid.Row="6">End time:</TextBlock>
    <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.Column="2">
      <ms:DropDownDatePicker Width="205" Margin="0,0,10,0" Value="{Binding EndDate, Mode=TwoWay}" Format="Custom" CustomFormat="dd/MM/yyyy" />
      <ms:TimePicker Width="80" SelectedTime="{Binding EndTime, Mode=TwoWay}" ItemsSource="{Binding EndTimeList}" />
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="7" Grid.Column="2">
      <Button Command="{x:Static ms:SchedulerCommands.OkCommand}" Content="Save &amp; Close" Width="95" Margin="0,18,0,0" />
      <Button Command="{x:Static ms:SchedulerCommands.CancelCommand}" Content="Cancel" Width="75" Margin="10,18,0,0" />
    </StackPanel>
  </Grid>
</UserControl>