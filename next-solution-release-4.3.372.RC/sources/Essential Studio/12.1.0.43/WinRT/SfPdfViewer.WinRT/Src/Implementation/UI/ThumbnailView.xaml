<UserControl
    x:Class="Syncfusion.Windows.PdfViewer.ThumbnailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Syncfusion.Windows.PdfViewer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <UserControl.Resources>
        <Style x:Key="GridViewItemStyle1" TargetType="GridViewItem">
            <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
            <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="TabNavigation" Value="Local"/>
            <Setter Property="IsHoldingEnabled" Value="True"/>
            <Setter Property="Margin" Value="60,2,22,2"/>
            <Setter Property="BorderThickness" Value="-22,-2,-22,-2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GridViewItem">
                        <GridViewItemPresenter 
                            PointerOverBackground="#4CFFFFFF" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="0.1"></RowDefinition>
            
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Background="Transparent" Grid.Row="0" Name="adjustmentTop"></Grid>
        <GridView Grid.Row="1" Grid.Column="0" x:Name="ItemGridView"    
                  ScrollViewer.IsHorizontalScrollChainingEnabled="False"
                  Background="Transparent"                
                  CanDragItems="False"
                  AllowDrop="False"
                  IsItemClickEnabled="True"                
                  SelectionMode="None"
                  VerticalAlignment="Top"
                  BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" Loaded="ItemGridView_Loaded"
                  ItemContainerStyle="{StaticResource GridViewItemStyle1}"/>
        <TextBox Name="InvisibleText" FontSize="0.001" Grid.Row="2" Grid.Column="0" Width="100" Height="100" Background="Transparent" Foreground="Transparent" BorderBrush="Transparent"></TextBox>
        
    </Grid>
</UserControl>
