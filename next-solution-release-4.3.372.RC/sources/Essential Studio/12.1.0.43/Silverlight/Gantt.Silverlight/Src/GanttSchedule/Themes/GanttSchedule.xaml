<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:schedule="clr-namespace:Syncfusion.Windows.Controls.Gantt.Schedule">

    <Style TargetType="schedule:GanttSchedule">
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="schedule:GanttSchedule">
                    <Border Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}" HorizontalAlignment="Stretch"
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ItemsPresenter  x:Name="PART_ItemsPresenter" HorizontalAlignment="Stretch" VerticalAlignment="Top" Height="Auto"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="schedule:GanttScheduleRow">
        <Setter Property="MinHeight" Value="20"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="schedule:GanttScheduleRow">
                    <schedule:GanttScheduleRowPanel x:Name="PART_RowItemsPresenter" MinHeight="{TemplateBinding MinHeight}" HorizontalAlignment="Left"
                                                    Width="Auto" Background="{TemplateBinding Background}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="schedule:GanttScheduleCell">
        <Setter Property="UseLayoutRounding" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="schedule:GanttScheduleCell">
                    <Border BorderThickness="0.75,0.75,0,0"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding CellBorderBrush}">
                        <ToolTipService.ToolTip>
                            <ToolTip Content="{TemplateBinding CellToolTip}"/>
                        </ToolTipService.ToolTip>
                        <ContentPresenter  Margin="1,0,1,0"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>