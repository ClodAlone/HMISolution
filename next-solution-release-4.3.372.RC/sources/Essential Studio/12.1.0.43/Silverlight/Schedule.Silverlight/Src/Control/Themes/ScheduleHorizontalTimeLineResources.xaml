<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Schedule">

    <!-- TimelineHour text block style -->
    <Style x:Key="HorizontalTimelineHourTextBlockStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Foreground" Value="#373737"/>
        <Setter Property="FontFamily" Value="Trebuchet MS"/>
        <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="FontStretch" Value="Condensed"/>
    </Style>

    <ItemsPanelTemplate x:Key="HorizontalTimeLinePanelTemplateKey">
        <local:UniformStackPanel Orientation="Horizontal" />
    </ItemsPanelTemplate>

    <!-- TimeLineItemsControl -->
    <Style TargetType="local:ScheduleHorizontalTimeLineItemsControl">
        <Setter Property="ItemsPanel" Value="{StaticResource HorizontalTimeLinePanelTemplateKey}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ScheduleHorizontalTimeLineItemsControl">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <Grid>
                            <ItemsPresenter/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--Horizontal TimeLineHourControl -->
    <Style TargetType="local:ScheduleHorizontalTimeLineHourControl">   
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ScheduleHorizontalTimeLineHourControl">
                    <Grid x:Name="TimeLinehourGrid" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Line x:Name="LineTop" Grid.Row="1" VerticalAlignment="Bottom" Stretch="Fill" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}" Grid.ColumnSpan="2" X2="1"/>
                        <Line x:Name="Line1" Stretch="Fill" Grid.RowSpan="2" Stroke="{TemplateBinding BorderBrush}" StrokeThickness="{TemplateBinding BorderThickness}" HorizontalAlignment="Left" Visibility="{TemplateBinding TimelineVisibility}"  Y2="1"/>
                        <TextBlock Margin="2,2,0,0" Grid.RowSpan="2" x:Name="HourValue" HorizontalAlignment="Left" Style="{StaticResource HorizontalTimelineHourTextBlockStyle}" VerticalAlignment="Top" Grid.Column="0" FontSize="16" FontWeight="Thin" FontFamily="Verdana"/>
                        <TextBlock Margin="0,2,0,0" Grid.RowSpan="2" x:Name="MinsValue" HorizontalAlignment="Left" Style="{StaticResource HorizontalTimelineHourTextBlockStyle}" VerticalAlignment="Top" Grid.Column="1" FontSize="9.333" FontFamily="Verdana"/>
                        <local:UniformStackPanel Grid.Row="1" x:Name="PART_HorizontalTimeIntervalsPanel" Grid.ColumnSpan="2" Orientation="Horizontal" Visibility="{TemplateBinding TimelineVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
