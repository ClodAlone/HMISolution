<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vsm="clr-namespace:System.Windows;assembly=System.Windows"
     xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Ribbon.Silverlight" >


    <SolidColorBrush Color="#7F000000" x:Key="TransparentThemeMenuPopupBorderBrush" />
    <SolidColorBrush Color="White" x:Key="TransparentThemeMenuPopupLeftBarBackground" Opacity="0.6"/>

    <SolidColorBrush Color="#E5FFFFFF"  x:Key="TransparentThemeMenuPopupBackgroundBrush"/>
    <SolidColorBrush Color="Black"  x:Key="TransparentForegroundBrush"/>
    <LinearGradientBrush x:Key="TransparentMenuGroupHeaderBackground" EndPoint="0.5,1" StartPoint="0.5,0" Opacity="0.6">
        <GradientStop Color="#FFFAFAFA" Offset="0"/>
        <GradientStop Color="#FFC8C8C8" Offset="0.491"/>
        <GradientStop Color="#FF999999" Offset="0.531"/>
        <GradientStop Color="#FFB6B6B6" Offset="0.789"/>
        <GradientStop Color="#FFD5D5D5" Offset="0.996"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="TransparentGroupHeaderBorderBrush" Color="#7F000000" />
    <Style TargetType="local:RibbonMenuGroup"  x:Key="TransparentRibbonMenuGroupStyle">
        <Setter Property="IconBarEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RibbonMenuGroup">
                    <Grid Name="gri">
                        <Grid.Resources>
                            <local:TextToVisibilityConverter x:Key="TextConverter"/>
                            <local:BooleanToVisibilityConverter x:Key="BoolConverter"/>
                            <local:VisibilityToThicknessConverter x:Key="ThicknessConverter"/>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Margin="0" Background="{StaticResource TransparentMenuGroupHeaderBackground}" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=Header, Converter={StaticResource TextConverter}}" Height="22" Grid.Row="0" BorderThickness="0,0,0,1" BorderBrush="{StaticResource TransparentGroupHeaderBorderBrush}">
                            <ContentControl Margin="5,0,0,0" Content="{TemplateBinding Header}" VerticalAlignment="Center" FontWeight="Bold" Foreground="{StaticResource TransparentForegroundBrush}" HorizontalContentAlignment="Left">
                                <ContentControl.Effect>
                                    <DropShadowEffect BlurRadius="0" ShadowDepth="1" Direction="270" Color="#03FFFFFF" Opacity="0.5"/>
                                </ContentControl.Effect>
                            </ContentControl>
                        </Border>
                        <Border Grid.Row="1">
                            <Grid Background="Transparent"  >
                                <Border x:Name="IconTray" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IconBarEnabled, Converter={StaticResource BoolConverter}}"  Background="{StaticResource TransparentThemeMenuPopupLeftBarBackground}"  Width="26" HorizontalAlignment="Left"/>
                                <Border>
                                    <ScrollViewer TabNavigation="Cycle" Padding="-1" VerticalScrollBarVisibility="{TemplateBinding ScrollBarVisibility}" BorderThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ScrollBarVisibility, Converter={StaticResource ThicknessConverter} }">
                                        <ItemsPresenter Margin="2,0,0,0"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Border>
                        <Border Grid.Row="2">
                            <Grid>
                                <Border BorderBrush="#7F000000" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsMoreItemsIconTrayEnabled, Converter={StaticResource BoolConverter} }" Background="#FFFFFFFF" BorderThickness="0,0,2,0" Width="22" HorizontalAlignment="Left"/>
                                <ItemsControl ItemsSource="{TemplateBinding MoreItems}"/>
                            </Grid>
                        </Border>
                        <Border Background="Transparent"  Grid.Row="3" Height="14" BorderThickness ="0,1,0,0" BorderBrush="#FFDDE7EE" Width="{Binding ElementName=PART_ResizeThumb, Path=ActualWidth}" HorizontalAlignment="Stretch" Visibility="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsResizable, Converter={StaticResource BoolConverter}}">

                            <Grid>
                                <Border HorizontalAlignment="right" Margin="0,0,2,0">
                                    <Grid >
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="4"/>
                                            <RowDefinition Height="4"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle  Fill="#7F000000"  Height="2" Width="2" Margin="1" Grid.Row="1" Grid.Column="1">
                                            <Rectangle.Effect>
                                                <DropShadowEffect Color="White" Opacity="0.7" Direction="319"  BlurRadius="0" ShadowDepth="2"></DropShadowEffect>
                                            </Rectangle.Effect>
                                        </Rectangle>

                                        <Rectangle  Fill="#7F000000" Height="2" Width="2"  Margin="1" Grid.Row="0" Grid.Column="1">
                                            <Rectangle.Effect>
                                                <DropShadowEffect Color="White"  Opacity="0.7"  Direction="319"  BlurRadius="0" ShadowDepth="2"></DropShadowEffect>
                                            </Rectangle.Effect>
                                        </Rectangle>
                                        <Rectangle  Fill="#7F000000" Height="2" Width="2" Margin="1" Grid.Row="1" Grid.Column="0">
                                            <Rectangle.Effect>
                                                <DropShadowEffect Color="White" Opacity="0.7"  Direction="319"  BlurRadius="0" ShadowDepth="2"></DropShadowEffect>
                                            </Rectangle.Effect>
                                        </Rectangle>

                                    </Grid>
                                </Border>
                                <Thumb x:Name="PART_ResizeThumb" Opacity="0" Cursor="SizeNS"/>
                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource TransparentRibbonMenuGroupStyle}" TargetType="local:RibbonMenuGroup"/>
	
</ResourceDictionary>
