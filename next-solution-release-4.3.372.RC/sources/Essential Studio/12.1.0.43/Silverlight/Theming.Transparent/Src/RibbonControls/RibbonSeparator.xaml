<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Ribbon.Silverlight">

    <SolidColorBrush x:Key="SepartorBrushKey" Color="Black"  Opacity="0.3"/>
    <Style TargetType="local:RibbonSeparator" x:Key="TransparentRibbonSeparatorStyle">
		<Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="4,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RibbonSeparator">
                    <Grid>
                        <Line X1="1" Stroke="{StaticResource SepartorBrushKey}" Name="PART_Horizontal" Stretch="Fill" />
                        <Grid Name="PART_Vertical" Visibility="Collapsed">
                            <Border Margin="{TemplateBinding Margin}" Width="1" Height="{TemplateBinding Height}" BorderBrush="{StaticResource SepartorBrushKey}" Background="{StaticResource SepartorBrushKey}"/>
                          
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:RibbonSeparator" BasedOn="{StaticResource TransparentRibbonSeparatorStyle}"/>

</ResourceDictionary>
