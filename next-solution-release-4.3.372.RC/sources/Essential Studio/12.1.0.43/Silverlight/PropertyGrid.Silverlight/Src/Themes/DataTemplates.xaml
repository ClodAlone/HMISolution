<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Syncfusion.Windows.PropertyGrid;assembly=Syncfusion.PropertyGrid.Silverlight">

    <local:TypeToNameConverter x:Key="NameConverter"/>
    <local:BooleanToTooltipConverter x:Key="TooltipConverter"/>
    <local:BooleanTobrushConverter x:Key="BrushConverter"/>

    <DropShadowEffect x:Key="ForegroundEffect" BlurRadius="0" ShadowDepth="1" Direction="270" Color="#03FFFFFF" Opacity="0.5"/>

    <LinearGradientBrush x:Key="Backgroundbrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#7FBEBEBE" Offset="1"/>
        <GradientStop Color="#99FFFFFF"/>
    </LinearGradientBrush>

    <DataTemplate x:Key="DefaultTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="{Binding IsSelected, Mode=TwoWay, Converter={StaticResource BrushConverter}}"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>
                  
   <DataTemplate x:Key="Windows7Template">
    <Border >
        <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            <ToolTipService.ToolTip>
                <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                    <Binding.ConverterParameter>
                        <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                        </TextBlock>
                    </Binding.ConverterParameter>
                </Binding>
            </ToolTipService.ToolTip>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="{Binding IsSelected, Mode=TwoWay, Converter={StaticResource BrushConverter}}"/>
            </Border>

            <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
            </Border>
        </Grid>
    </Border>
    </DataTemplate>

    <DataTemplate x:Key="BlendTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="White"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="#FF333333">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="Office2007BlueTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="Office2007BlackTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="Office2007SilverTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="Office2010BlueTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="Office2010BlackTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>
    
    <DataTemplate x:Key="Office2010SilverTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="Black"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="VS2010Template">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="{Binding IsSelected, Mode=TwoWay, Converter={StaticResource BrushConverter}}"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="LightGray" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>
    <DataTemplate x:Key="MetroTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,2,0">
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="#FFCCCCCC" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="{Binding IsSelected, Mode=TwoWay, Converter={StaticResource BrushConverter}}"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="#FFCCCCCC" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="White">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

    <DataTemplate x:Key="TransparentTemplate">
        <Border >
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="0,0,2,0">
                <ToolTipService.ToolTip>
                    <Binding Path="EnableToolTip" Converter="{StaticResource TooltipConverter}">
                        <Binding.ConverterParameter>
                            <TextBlock Margin="3" >
                                <Run Text="{Binding DisplayName}"/>
                                <Run Text=":"/>
                                <Run Text="{Binding Path=PropertyType, Converter={StaticResource NameConverter}}" FontWeight="Bold"/>
                            </TextBlock>
                        </Binding.ConverterParameter>
                    </Binding>
                </ToolTipService.ToolTip>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="5*"/>
                    <ColumnDefinition Width="5*"/>
                </Grid.ColumnDefinitions>

                <Border BorderThickness="0.3" BorderBrush="#7F000000" x:Name="PART_Name" >
                    <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" Margin="6,2,0,2" Foreground="{Binding IsSelected, Mode=TwoWay, Converter={StaticResource BrushConverter}}" Effect="{StaticResource ForegroundEffect}"/>
                </Border>

                <Border BorderThickness="0.3" BorderBrush="#7F000000" x:Name="PART_Editor" VerticalAlignment="Stretch"  Grid.Column="1" Background="{StaticResource Backgroundbrush}">
                    <ContentControl Content="{Binding PropertyEditor}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                    HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" IsTabStop="False" />
                </Border>
            </Grid>
        </Border>
    </DataTemplate>

</ResourceDictionary>
