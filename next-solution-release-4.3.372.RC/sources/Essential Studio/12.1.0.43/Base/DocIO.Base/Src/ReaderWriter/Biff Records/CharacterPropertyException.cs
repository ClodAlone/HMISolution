#region Copyright Syncfusion Inc. 2001 - 2014
//
//  Copyright Syncfusion Inc. 2001 - 2014. All rights reserved.
//
//  Use of this code is subject to the terms of our license.
//  A copy of the current license can be obtained at any time by e-mailing
//  licensing@syncfusion.com. Re-distribution in any form is strictly
//  prohibited. Any infringement will be prosecuted under applicable laws. 
//
#endregion

#region file using directives
using System;
using System.Diagnostics;
using System.IO;
using Syncfusion.DocIO.DLS;
#if !WINRT && !WP
using System.Drawing;
#endif
#endregion

namespace Syncfusion.DocIO.ReaderWriter.Biff_Records
{
    /// <summary>
    /// Summary description for CharPropertyException (CHPX).
    /// </summary>
    [CLSCompliant(false)]
    [Syncfusion.Documentation.DocumentationExclude()]
    internal class CharacterPropertyException : BaseWordRecord
    {
        #region Class members
        /// <summary>
        /// Count of bytes of following data in CHPX.
        /// </summary>
        private byte m_btLength;
        /// <summary>
        /// A list of the sprms (Single PRoperty modifier) that encode
        /// the differences between CHP (CHaracter property) for a run
        /// of text and the CHP generated by the paragraph and character
        /// styles that tag the run.
        /// </summary>
        //private ArrayList m_arrSprms = new ArrayList();
        //    protected SinglePropertyModifierArray m_arrSprms = new SinglePropertyModifierArray();
        protected SinglePropertyModifierArray m_arrSprms;
        /// <summary>
        /// Byte array of sprms.
        /// </summary>
        //    private byte[] m_sprms;
        #endregion

        #region Class Properties
        /// <summary>
        /// A list of the sprms (Single PRoperty modifier) that encode
        /// the differences between CHP (CHaracter property) for a run
        /// of text and the CHP generated by the paragraph and character
        /// styles that tag the run.
        /// </summary>
        internal SinglePropertyModifierArray PropertyModifiers
        {
            get
            {
                if (m_arrSprms == null)
                {
                    m_arrSprms = new SinglePropertyModifierArray();
                }
                return m_arrSprms;
                //        return GetPropertyModifiers();
            }
            set
            {
                m_arrSprms = value;
            }
        }
        /// <summary>
        /// Returns number of property modifiers. Read-only.
        /// </summary>
        internal int ModifiersCount
        {
            get
            {
                return PropertyModifiers.Count;
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal override int Length
        {
            get
            {
                return 1 + PropertyModifiers.Length;
            }
        }
        //    /// <summary>
        //    /// Gets/sets sprms byte array.
        //    /// </summary>
        //    internal byte[] SprmsByteArray
        //    {
        //      get
        //      {
        //        return m_sprms;
        //      }
        //      set
        //      {
        //        m_sprms = value;
        //      }
        //    }
        #endregion

        #region Class Modifier Special properties
        /// <summary>
        /// 
        /// </summary>
        internal bool Special
        {
            get
            {
                return false;
            }
            set
            {
                // TODO: ...
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal bool EmbededObj
        {
            get
            {
                return false;
            }
            set
            {
                // TODO: ...
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal bool IsData
        {
            get
            {
                return false;
            }
            set
            {
                // TODO: ...
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal ushort CharacterStyleId
        {
            get
            {
                return m_arrSprms.GetUShort(WordSprmOptions.sprmCIstd, 0);
            }
            set
            {
                if (value != 0)
                {
                    m_arrSprms.SetValue(WordSprmOptions.sprmCIstd, value);
                }
            }
        }
        #endregion

        #region Class Modifier properties
        /// <summary>
        /// 
        /// </summary>
        internal bool Bold
        {
            get
            {
                return PropertyModifiers.GetBoolean(WordSprmOptions.sprmCFBold, false);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCFBold, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal bool Italic
        {
            get
            {
                return PropertyModifiers.GetBoolean(WordSprmOptions.sprmCFItalic, false);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCFItalic, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal bool Strike
        {
            get
            {
                return PropertyModifiers.GetBoolean(WordSprmOptions.sprmCFStrike, false);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCFStrike, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal byte UnderlineCode
        {
            get
            {
                return PropertyModifiers.GetByte(WordSprmOptions.sprmCKul, 0);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCKul, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal ushort FontAscii
        {
            get
            {
                return PropertyModifiers.GetUShort(WordSprmOptions.sprmCRgFtc0, ushort.MaxValue);
            }
            set
            {
                // TODO: Checks if font is exists...
                PropertyModifiers.SetValue(WordSprmOptions.sprmCRgFtc0, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal ushort FontFarEast
        {
            get
            {
                return PropertyModifiers.GetUShort(WordSprmOptions.sprmCRgFtc1, 0);
            }
            set
            {
                // TODO: Checks if font is exists...
                PropertyModifiers.SetValue(WordSprmOptions.sprmCRgFtc1, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal ushort FontNonFarEast
        {
            get
            {
                return PropertyModifiers.GetUShort(WordSprmOptions.sprmCRgFtc2, 0);
            }
            set
            {
                // TODO: Checks if font is exists...
                PropertyModifiers.SetValue(WordSprmOptions.sprmCRgFtc2, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal ushort FontSizeHP
        {
            get
            {
                return PropertyModifiers.GetUShort(WordSprmOptions.sprmCHps, 1);
            }
            set
            {
                // TODO: Checks if font is exists...
                PropertyModifiers.SetValue(WordSprmOptions.sprmCHps, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal byte FontColor
        {
            get
            {
                return PropertyModifiers.GetByte(WordSprmOptions.sprmCIco, 0);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCIco, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal Color DBG_FontColor
        {
            get
            {
                byte color = FontColor;

                switch (color)
                {
                    case 0: return Color.Black;
                    case 255: return Color.White;
                    default: return Color.Red;
                }
            }
            set
            {
                byte color = 0;

                if (value == Color.Black)
                    color = 0;
                else if (value == Color.White)
                    color = 255;
                else
                    color = 1;

                FontColor = color;
            }
        }

        /// <summary>
        /// 
        /// </summary>
        internal bool Outline
        {
            get
            {
                return PropertyModifiers.GetBoolean(WordSprmOptions.sprmCFOutline, false);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCFOutline, value);
            }
        }
        /// <summary>
        /// 
        /// </summary>
        internal bool Shadow
        {
            get
            {
                return PropertyModifiers.GetBoolean(WordSprmOptions.sprmCFShadow, false);
            }
            set
            {
                PropertyModifiers.SetValue(WordSprmOptions.sprmCFShadow, value);
            }
        }
        #endregion

        #region Class Initialize/Finalize methods
        /// <summary>
        /// Default constructor.
        /// </summary>
        internal CharacterPropertyException()
        { }
        /// <summary>
        /// Default constructor.
        /// </summary>
        internal CharacterPropertyException(byte[] arrData, int iOffset)
        {
            Parse(arrData, iOffset, arrData.Length);
        }
        /// <summary>
        /// 
        /// </summary>
        /// <param name="property"></param>
        /// <param name="converter"></param>
        internal CharacterPropertyException(UniversalPropertyException property)
        {
            PropertyModifiers.Parse(property.Data, 0, property.Data.Length);
            //      InitSprmByteArr( property.Data, 0, property.Data.Length );
        }
        /// <summary>
        /// 
        /// </summary>
        /// <param name="arrData"></param>
        /// <param name="iOffset"></param>
        internal CharacterPropertyException(byte[] arrData)
        {
            PropertyModifiers.Parse(arrData, 0, arrData.Length);
            //      InitSprmByteArr( arrData, 0, arrData.Length );
        }
        #endregion

        #region Class methods
        /// <summary>
        /// Parses the specified data.
        /// </summary>
        /// <param name="arrData">The data.</param>
        /// <param name="iOffset">The offset.</param>
        /// <param name="iCount">The count.</param>
        internal override void Parse(byte[] arrData, int iOffset, int iCount)
        {
            m_btLength = arrData[iOffset];
            iOffset++;
            //      m_arrSprms.Clear();
            PropertyModifiers.Parse(arrData, iOffset, m_btLength);
            //      InitSprmByteArr( arrData, iOffset, m_btLength );
            //      while( iOffset < m_btLength )
            //      {
            //        SinglePropertyModifierRecord sprm = new SinglePropertyModifierRecord();
            //        iOffset = sprm.Parse( arrData, iOffset );
            //        m_arrSprms.Add( sprm );
            //      }
        }
        /// <summary>
        /// Saves record into array of bytes.
        /// </summary>
        /// <param name="arrData">Array of bytes to save record into.</param>
        /// <param name="iOffset">Offset in the array.</param>
        /// <returns>Number of bytes in the written data.</returns>
        internal override int Save(byte[] arrData, int iOffset)
        {
            if (arrData == null)
                throw new ArgumentNullException("arrData");

            byte iLength = (byte)m_arrSprms.Length;

            if (iOffset < 0 || iOffset + iLength + 1 > arrData.Length)
                throw new ArgumentOutOfRangeException("iOffset");

            arrData[iOffset++] = iLength;

            if (m_arrSprms != null)
            {
                m_arrSprms.Save(arrData, iOffset);
            }

            return iLength + 1;
        }
        /// <summary>
        /// 
        /// </summary>
        /// <param name="writer"></param>
        /// <param name="stream"></param>
        /// <param name="length"></param>
        /// <returns></returns>
        internal int Save(BinaryWriter writer, Stream stream, int length)
        {
            if (writer == null)
                throw new ArgumentNullException("stream");

            //      byte iLength = ( byte )m_arrSprms.Length;
            byte iLength = (byte)(length - 1);

            //      if( iOffset < 0 || iOffset + iLength + 1 > arrData.Length )
            //        throw new ArgumentOutOfRangeException( "iOffset" );

            writer.Write(iLength);
            //      arrData[ iOffset++ ] = iLength;

            //Debug variant
            //      if( m_sprms != null )
            //      {
            //        writer.Write( m_sprms );
            //      }
            if (m_arrSprms != null)
            {
                m_arrSprms.Save(writer, stream, iLength);
            }

            return iLength + 1;
        }
        /// <summary>
        /// Determines whether this instance has SPRMS.
        /// </summary>
        /// <returns>
        /// 	<c>true</c> if this instance has SPRMS; otherwise, <c>false</c>.
        /// </returns>
        internal bool HasSprms()
        {
            return (m_arrSprms != null && m_arrSprms.Count > 0) ? true : false;
        }
        #endregion

        #region Class helper methods
        //    /// <summary>
        //    /// Inits the SPRM byte array.
        //    /// </summary>
        //    /// <param name="data">The data.</param>
        //    /// <param name="startOffset">The start offset.</param>
        //    /// <param name="length">The length.</param>
        //    private void InitSprmByteArr( byte[] data, int startOffset, int length )
        //    {
        //      m_sprms = new byte[ length ];
        //      Array.Copy( data, startOffset, m_sprms, 0, length );
        //    }   
        //    /// <summary>
        //    /// Gets the property modifiers.
        //    /// </summary>
        //    /// <returns></returns>
        //    private SinglePropertyModifierArray GetPropertyModifiers()
        //    {
        //      if( m_arrSprms == null )
        //      {
        //        m_arrSprms = new SinglePropertyModifierArray();
        //        if( m_sprms != null )
        //        {
        //          m_arrSprms.Parse( m_sprms, 0, m_sprms.Length, StreamsManager.Converter );
        //        }        
        //      }
        //
        //      return m_arrSprms;
        //    }
        /// <summary>
        /// Returns a value indicating whether this instance is equal to the character property exception passed as argument.
        /// </summary>
        /// <param name="chpx">chpx</param>
        /// <returns></returns>
        public bool Equals(CharacterPropertyException chpx)
        {
            bool isEqual = false;
            foreach (SinglePropertyModifierRecord sprm in this.PropertyModifiers)
            {
                #region switch case
                switch (sprm.Options)
                {
                    case WordSprmOptions.sprmCBrc:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCBrc))
                            {
                                //4 bytes
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCCharScale:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCCharScale))
                            {
                                //2-byte unsigned integer
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UIntValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCChs:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCChs))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCCpg:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCCpg))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCDefault:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCDefault))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCDispFldRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCDispFldRMark))
                            {
                                //A DispFldRmOperand value that indicates a revision within the result of a display field. This sprm MUST be applied only to a LISTNUM display field.
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCDttmRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCDttmRMark))
                            {
                                //A DTTM that specifies the date and time at which the text was inserted. This is recorded only if revision marking is on at the time of the insertion. By default, all fields of this DTTM are zero.
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCDttmRMarkDel:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCDttmRMarkDel))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCDxaSpace:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCDxaSpace))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFBiDi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFBiDi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFBold:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFBold))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFBoldBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFBoldBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFCaps:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFCaps))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFComplexScripts:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFComplexScripts))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFData:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFData))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFDiacColor:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFDiacColor))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFDStrike:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFComplexScripts))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFEmboss:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFEmboss))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFFldVanish:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFFldVanish))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFFtcAsciSymb:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFFtcAsciSymb))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFImprint:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFImprint))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFItalic:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFItalic))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFItalicBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFItalicBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFNoProof:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFNoProof))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFObj:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFObj))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFOle2:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFOle2))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFOutline:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFOutline))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFRMark))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFRMarkDel:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFRMarkDel))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFShadow:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFShadow))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFSmallCaps:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFSmallCaps))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFSpec:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFSpec))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFStrike:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFStrike))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }

                    case WordSprmOptions.sprmCFtcBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFtcBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFtcDefault:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFtcDefault))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFUsePgsuSettings:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFUsePgsuSettings))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCFVanish:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCFVanish))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHighlight:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHighlight))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHps:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHps))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsInc:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsInc))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsInc1:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsInc1))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsKern:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsKern))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsMul:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsMul))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsNew50:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsNew50))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsPos:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsPos))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCHpsPosAdj:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCHpsPosAdj))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIbstRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIbstRMark))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIbstRMarkDel:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIbstRMarkDel))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIco:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIco))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIcoBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIcoBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIcoe:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIcoe))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UIntValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIdCharType:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIdCharType))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIdctHint:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIdctHint))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIdslRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIdslRMark))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIdslRMarkDel:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIdslRMarkDel))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIss:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIss))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIstd:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIstd))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCIstdPermute:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCIstdPermute))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCKcd:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCKcd))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCKul:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCKul))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCLid:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCLid))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCLidBi:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCLidBi))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCMajority:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCMajority))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCMajority50:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCMajority50))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCObjLocation:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCObjLocation))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPbiHasImage:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPbiHasImage))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.Boolean);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPbiImageIndex:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPbiImageIndex))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.IntValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPicLocation:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPicLocation))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.IntValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPlain:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPlain))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPropRMark:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPropRMark))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCPropRMark1:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCPropRMark1))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgFtc0:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgFtc0))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgFtc1:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgFtc1))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgFtc2:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgFtc2))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.UShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgLid0:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgLid0))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgLid1:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgLid1))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgLid3:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgLid3))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCRgLid3_2:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCRgLid3_2))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCSfxText:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCSfxText))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCShd:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCShd))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ShortValue);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCShdNew:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCShdNew))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCSizePos:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCSizePos))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCSymbol:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCSymbol))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUnderlineColor:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUnderlineColor))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented1:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented1))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented2:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented2))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented2879:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented2879))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented2A86:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented2A86))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented3:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented3))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented4:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented4))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented5:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented5))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented6:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented6))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented6815:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented6815))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented6816:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented6816))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented6817:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented6817))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented7:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented7))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented8:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented8))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumented811:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumented811))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumentedRevisionProblem:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumentedRevisionProblem))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCUndocumentedSpacing:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCUndocumentedSpacing))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCWall:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCWall))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmCYsri:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmCYsri))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    case WordSprmOptions.sprmNone:
                        {
                            if (chpx.PropertyModifiers.Contain(WordSprmOptions.sprmNone))
                            {
                                SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                                isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                            }
                            else
                                isEqual = false;
                            break;
                        }
                    default:
                        if (chpx.PropertyModifiers.Contain(sprm.Options))
                        {
                            SinglePropertyModifierRecord prevSprm = chpx.PropertyModifiers[sprm.Options];
                            isEqual = IsSprmEqual(sprm, prevSprm, SprmCompareType.ByteArray);
                        }
                        else
                            isEqual = false;
                        break;

                }
                #endregion
                if (isEqual == false)
                    break;
            }
            if (isEqual)
            {
                return IsChpxEqual(chpx);
            }
            return isEqual;
        }
        /// <summary>
        /// Check whether the properties of Current Chpx object is equal to the argument passed.
        /// </summary>
        /// <param name="chpx">chpx</param>
        /// <returns></returns>
        private bool IsChpxEqual(CharacterPropertyException chpx)
        {
            //Checks 18 properties of Chpx
            if (this.Bold == chpx.Bold &&
                this.CharacterStyleId == chpx.CharacterStyleId &&
                this.DBG_FontColor == chpx.DBG_FontColor &&
                this.EmbededObj == chpx.EmbededObj &&
                this.FontAscii == chpx.FontAscii &&
                this.FontColor == chpx.FontColor &&
                this.FontFarEast == chpx.FontFarEast &&
                this.FontNonFarEast == chpx.FontNonFarEast &&
                this.FontSizeHP == chpx.FontSizeHP &&
                this.IsData == chpx.IsData && 
                this.Italic == chpx.Italic &&
                this.Length == chpx.Length && 
                this.ModifiersCount == chpx.ModifiersCount && 
                this.Outline == chpx.Outline && 
                this.Shadow == chpx.Shadow && 
                this.Special == chpx.Special && 
                this.Strike == chpx.Strike && 
                this.UnderlineCode == chpx.UnderlineCode)
                return true;

            return false; 
        }
        /// <summary>
        /// Check whether the sprm passed in the arguments are equal.
        /// </summary>
        /// <param name="sprm">sprm</param>
        /// <param name="prevSprm">prevSprm</param>
        /// <param name="sprmCompareType">sprm Compare type</param>
        /// <returns></returns>
        private bool IsSprmEqual(SinglePropertyModifierRecord sprm, SinglePropertyModifierRecord prevSprm, SprmCompareType sprmCompareType)
        {
            switch (sprmCompareType)
            {
                case SprmCompareType.Boolean:
                    {
                        if (sprm.BoolValue == prevSprm.BoolValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.ByteValue:
                    {
                        if (sprm.ByteValue == prevSprm.ByteValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.IntValue:
                    {
                        if (sprm.IntValue == prevSprm.IntValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.ShortValue:
                    {
                        if (sprm.ShortValue == prevSprm.ShortValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.UIntValue:
                    {
                        if (sprm.UIntValue == prevSprm.UIntValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.UShortValue:
                    {
                        if (sprm.UshortValue == prevSprm.UshortValue)
                            return true;
                        else
                            return false;
                    }
                    break;
                case SprmCompareType.ByteArray:
                    {
                        for (int j = 0; j < sprm.ByteArray.Length; j++)
                            if (sprm.ByteArray[j] != prevSprm.ByteArray[j])
                            {
                                return false;
                            }
                        return true;
                    }
                    break;
            }
            return false;
        }
        #endregion
    }
}