<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Schedule">

    <!-- TimelineHour text block style -->
    <Style x:Key="TimelineHourTextBlockStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Foreground" Value="#373737"/>
        <Setter Property="FontFamily" Value="Trebuchet MS"/>
        <Setter Property="FontWeight" Value="Light"/>
        <Setter Property="FontStretch" Value="Condensed"/>
    </Style>

    <ItemsPanelTemplate x:Key="TimeLinePanelTemplateKey">
        <local:UniformStackPanel Orientation="Vertical" />
    </ItemsPanelTemplate>

    <!-- TimeLineItemsControl -->
    <Style TargetType="local:ScheduleTimeLineItemsControl">
        <Setter Property="LinesStroke" Value="#FFA5BFE1" />
        <Setter Property="ItemsPanel" Value="{StaticResource TimeLinePanelTemplateKey}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ScheduleTimeLineItemsControl">
                    <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                        <Grid>
                            <ItemsPresenter/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TimeLineHourControl -->
    <Style TargetType="local:ScheduleTimeLineHourControl">
        <Setter Property="LinesStroke" Value="#FFA5BFE1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ScheduleTimeLineHourControl">
                    <Grid x:Name="TimeLinehourGrid" Margin="{TemplateBinding Padding}" HorizontalAlignment="Left">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Line x:Name="Line1" Stretch="Fill" Stroke="#4F93E3" StrokeThickness="1" VerticalAlignment="Top" Visibility="{TemplateBinding TimelineVisibility}" Grid.ColumnSpan="2" X2="1"/>
                        <TextBlock Margin="0,2,0,0" x:Name="HourValue" HorizontalAlignment="Right" Style="{StaticResource TimelineHourTextBlockStyle}" VerticalAlignment="Top" Grid.Column="0" FontSize="16" FontWeight="Thin" FontFamily="Verdana"/>
                        <TextBlock Margin="4,2,0,0" x:Name="MinsValue" HorizontalAlignment="Left" Style="{StaticResource TimelineHourTextBlockStyle}" VerticalAlignment="Top" Grid.Column="1" FontSize="9.333" FontFamily="Verdana"/>
                        <local:UniformStackPanel x:Name="PART_TimeIntervalsPanel" Grid.Column="1" Orientation="Vertical" Visibility="{TemplateBinding TimelineVisibility}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>