<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Shared"
                    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
>
    <!--<SolidColorBrush x:Key="MetroBrush" Color="#FF119EDA" />-->

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Shared.WPF;component/SkinManager/MetroThemeBrushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <local:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <ControlTemplate x:Key="RepeatButtonTemplate" TargetType="{x:Type RepeatButton}">
        <Border Name="Chrome"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                             BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="0"
                                        SnapsToDevicePixels="true">

            <Border BorderThickness="0" Background="Transparent" Margin="1">
                <ContentPresenter Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Border>
        </Border>
        <ControlTemplate.Triggers>

            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Repeat Button Style -->
    <Style x:Key="RepeatButtonStyle"
           TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background"
                Value="{DynamicResource   MetroBackgroundBrush}" />
        <Setter Property="BorderBrush"
                Value="{DynamicResource MetroBorderBrush}" />
        <Setter Property="Foreground"
                Value="{DynamicResource MetroForegroundBrush}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="HorizontalContentAlignment"
                Value="Center" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="Padding"
                Value="1" />
        <Setter Property="Template" Value="{StaticResource RepeatButtonTemplate}" />
    </Style>

    <Style x:Key="MetroTimeSpanEditStyle" TargetType="{x:Type  local:TimeSpanEdit}">
        <Setter Property="Foreground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="Background" Value="{DynamicResource  MetroPanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource  MetroBorderBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource MetroFontFamily}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="AllowDrop" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimeSpanEdit}">

                    <Border  x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"  SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <ScrollViewer HorizontalAlignment="Stretch"  VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <RepeatButton  Margin="0,-1,-1,0" x:Name="upbutton" IsTabStop="False" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=UpCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0"   Style="{StaticResource RepeatButtonStyle}" Width="17">
                                    <Path Stretch="Uniform" x:Name="upbuttonpath" Width="7" Height="4" Fill="{DynamicResource  MetroForegroundBrush}" Data="F1 M 541.537,173.589L 531.107,173.589L 536.322,167.49L 541.537,173.589 Z " HorizontalAlignment="Stretch"  VerticalAlignment="Stretch">
                                    </Path>
                                </RepeatButton>
                                <RepeatButton x:Name="downbutton" Margin="0,-1,-1,0" IsTabStop="False" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=DownCommand, RelativeSource={RelativeSource TemplatedParent}}"  Grid.Row="1" Style="{StaticResource RepeatButtonStyle}" Width="17">
                                    <Path Stretch="Uniform" x:Name="downbuttonpath" Fill="{DynamicResource  MetroForegroundBrush}" Height="4" Width="7" Data="F1 M 531.107,321.943L 541.537,321.943L 536.322,328.042L 531.107,321.943 Z " HorizontalAlignment="Stretch"  VerticalAlignment="Stretch">
                                    </Path>
                                </RepeatButton>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" TargetName="Bd" Value="0.5" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="local:SkinStorage.EnableTouch" Value="False" />
                                <Condition Property="IsMouseOver" SourceName="upbutton" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="upbutton" Property="Background" Value="{DynamicResource MetroHoverBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsPressed" SourceName="upbutton" Value="True">
                            <Setter TargetName="upbutton" Property="Background" Value="{DynamicResource  MetroBrush}" />
                            <Setter TargetName="upbuttonpath" Property="Fill" Value="{DynamicResource MetroHighlightedForegroundBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="local:SkinStorage.EnableTouch" Value="False" />
                                <Condition Property="IsMouseOver" SourceName="downbutton" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="downbutton" Property="Background" Value="{DynamicResource MetroHoverBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsPressed" SourceName="downbutton" Value="True">
                            <Setter TargetName="downbutton" Property="Background" Value="{DynamicResource  MetroBrush}" />
                            <Setter TargetName="downbuttonpath" Property="Fill" Value="{DynamicResource MetroHighlightedForegroundBrush}" />
                        </Trigger>
                        <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="1,8" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Width" TargetName="upbutton" Value="40" />
                            <Setter Property="Width" TargetName="downbutton" Value="40" />
                            <Setter Property="Height" TargetName="upbutton" Value="25" />
                            <Setter Property="Height" TargetName="downbutton" Value="25" />
                            <Setter Property="Width" TargetName="upbuttonpath" Value="12" />
                            <Setter Property="Width" TargetName="downbuttonpath" Value="12" />
                            <Setter Property="Height" TargetName="downbuttonpath" Value="8" />
                            <Setter Property="Height" TargetName="upbuttonpath" Value="8" />
                            <Setter Property="EnableTouch" Value="True" />
                        </Trigger>
                        <!--<Trigger Property="IsMouseOver" Value="true">
                        </Trigger>-->
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource MetroTimeSpanEditStyle}" TargetType="{x:Type local:TimeSpanEdit}" />
</ResourceDictionary>