<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Shared"
					xmlns:system="clr-namespace:System;assembly=mscorlib"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

			xmlns:converters="clr-namespace:Syncfusion.Windows.Tools">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Shared.WPF;component/SkinManager/MetroThemeBrushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters:CultureToCalendarConverter x:Key="CultureToCalendarConverter" />
    <local:BooleanToVisibilityConverterEx x:Key="BooleanToVisibilityConverter" />

    <system:String x:Uid="system:String_4" x:Key="AccessTodayText">Today :</system:String>

    <SolidColorBrush x:Key="DefaultHeaderBackground" Color="Transparent" />

    <SolidColorBrush x:Key="MetroThemelistboxInaActiveBrush" Color="#FFEBEBEB" />
    <SolidColorBrush x:Key="MetroNavigationButtonFill" Color="White" />

    <Style x:Key="MetroListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontFamily" Value="{DynamicResource MetroFontFamily}" />
        <Setter Property="Padding" Value="3,2,2,1" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                        <ContentPresenter Margin="{TemplateBinding Padding}" TextBlock.Foreground="{TemplateBinding Foreground}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="true">
                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource MetroBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource MetroHighlightedForegroundBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="False" />
                                <Condition Property="IsMouseOver" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource MetroBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource MetroHighlightedForegroundBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="true" />
                                <Condition Property="Selector.IsSelectionActive" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" TargetName="Bd" Value="{StaticResource  MetroThemelistboxInaActiveBrush}" />
                            <Setter Property="Foreground" Value="Black" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource MetroListBoxItemStyle}" TargetType="{x:Type ListBoxItem}" />
    <!--ControlTemplates-->

    <!--NavigateButtonBaseDefault-->
    <ControlTemplate x:Key="NavigateButtonBaseDefault" TargetType="{x:Type local:NavigateButtonBase}">
        <Border x:Name="Border"
		    Background="{TemplateBinding Background}"
			BorderBrush="{TemplateBinding Border.BorderBrush}"
			CornerRadius="{TemplateBinding Border.CornerRadius}"
		    BorderThickness="{TemplateBinding Border.BorderThickness}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" />
        </Border>
    </ControlTemplate>

    <ControlTemplate x:Key="OfficeButtonNextDefault" TargetType="{x:Type local:NavigateButton}">
        <local:NavigateButtonBase x:Name="Button" Command="local:CalendarEdit.NextCommand"
								  Background="Transparent" IsTabStop="False"
								  BorderThickness="0.5"
								  BorderBrush="Transparent"
								  CornerRadius="3">
            <Grid>

                <!--<Grid.ColumnDefinitions>
                    <ColumnDefinition Width="19" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>-->
                <Ellipse x:Name="circle"
                                     Width="19"
                                     Height="19"
                                     Stroke="#FFC1C1C1"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Fill="{StaticResource MetroNavigationButtonFill}" />
                <Path x:Name="path1" Data="M 1,1.5 L 4.5,5 L 8,1.5" Stroke="#FF3B3B3B" HorizontalAlignment="Center" Stretch="fill"  Height="6" StrokeThickness="2" VerticalAlignment="Center" Width="9" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="-90" />
                    </Path.RenderTransform>
                </Path>

            </Grid>
        </local:NavigateButtonBase>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Stroke" TargetName="circle" Value="#FF838383" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" TargetName="path1" />
            </Trigger>
            <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                <Setter Property="Width" TargetName="path1" Value="15" />
                <Setter Property="Height" TargetName="path1" Value="12" />
                <Setter Property="Height" TargetName="circle" Value="40" />
                <Setter Property="Width" TargetName="circle" Value="40" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--ButtonPrevDefault-->
    <ControlTemplate x:Key="OfficeButtonPrevDefault" TargetType="{x:Type local:NavigateButton}">
        <local:NavigateButtonBase x:Name="Button" Command="local:CalendarEdit.PrevCommand"
								  Background="Transparent" IsTabStop="False"
								  BorderThickness="0.5" Padding="2,1,2,1"
								  BorderBrush="Transparent"
							      CornerRadius="3">
            <Grid Cursor="Hand">

                <!--<Grid.ColumnDefinitions>
                    <ColumnDefinition Width="19" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>-->
                <Ellipse x:Name="circle"
                                     Width="19"
                                     Height="19"
                                     Stroke="#FFC1C1C1"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Fill="{StaticResource MetroNavigationButtonFill}" />
                <Path x:Name="path" Data="M 1,1.5 L 4.5,5 L 8,1.5" Stroke="#FF3B3B3B" HorizontalAlignment="Center" Stretch="fill"  Height="6" StrokeThickness="2" VerticalAlignment="Center" Width="9" RenderTransformOrigin="0.5,0.5">
                    <Path.RenderTransform>
                        <RotateTransform Angle="90" />
                    </Path.RenderTransform>
                </Path>

            </Grid>
        </local:NavigateButtonBase>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Stroke" TargetName="circle" Value="#FF838383" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" TargetName="path" />
            </Trigger>
            <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                <Setter Property="Width" TargetName="path" Value="15" />
                <Setter Property="Height" TargetName="path" Value="12" />
                <Setter Property="Height" TargetName="circle" Value="40" />
                <Setter Property="Width" TargetName="circle" Value="40" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!--NavigateButtonBase-->
    <Style  TargetType="{x:Type local:NavigateButtonBase}">
        <Setter Property="Control.HorizontalContentAlignment" Value="{x:Static HorizontalAlignment.Center}" />
        <Setter Property="Control.VerticalContentAlignment" Value="{x:Static VerticalAlignment.Center}" />
        <Setter Property="Template" Value ="{StaticResource NavigateButtonBaseDefault}" />
    </Style>

    <!--NavigateButton-->
    <Style  TargetType="{x:Type local:NavigateButton}">
        <Setter Property="Control.HorizontalContentAlignment" Value="{x:Static HorizontalAlignment.Center}" />
        <Setter Property="Control.VerticalContentAlignment" Value="{x:Static VerticalAlignment.Center}" />
    </Style>

    <ControlTemplate x:Key="OfficeMonthButtonDefault" TargetType="{x:Type local:MonthButton}">
        <Border x:Name="Border"
		    Background="{TemplateBinding Background}"
			BorderBrush="{TemplateBinding Border.BorderBrush}"
			CornerRadius="{TemplateBinding Border.CornerRadius}"
		    BorderThickness="{TemplateBinding Border.BorderThickness}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" Content="{TemplateBinding ContentControl.Content}" />
        </Border>
    </ControlTemplate>

    <Style x:Key="OfficeMonthButtonStyle" TargetType="{x:Type local:MonthButton}">
        <Setter Property="HorizontalContentAlignment" Value="{x:Static HorizontalAlignment.Center}" />
        <Setter Property="VerticalContentAlignment" Value="{x:Static VerticalAlignment.Center}" />
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="TextBlock.FontFamily" Value="{Binding Path=FontFamily,RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:CalendarEdit}}}" />
        <Setter Property="TextBlock.FontSize" Value="{Binding Path=FontSize,RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:CalendarEdit}}}" />
        <Setter Property="TextBlock.FontStyle" Value="{Binding Path=FontStyle,RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:CalendarEdit}}}" />
        <Setter Property="TextBlock.FontWeight" Value="ExtraBold" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template" Value ="{StaticResource OfficeMonthButtonDefault}" />
    </Style>

    <Style x:Key="OfficeTodayButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{DynamicResource MetroForegroundBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                        <ContentPresenter Content="{TemplateBinding Content}" Margin="1,0,1,0" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="MetroCalendarEditStyle" TargetType="{x:Type local:CalendarEdit}">
        <Setter Property="Control.MinWidth" Value="150" />
        <Setter Property="Control.MinHeight" Value="150" />
        <Setter Property="Width" Value="180" />
        <Setter Property="Height" Value="164" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Control.MaxWidth" Value="450" />
        <Setter Property="Control.MaxHeight" Value="450" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="WeekNumberBorderThickness" Value="1" />
        <Setter Property="WeekNumberCornerRadius" Value="2" />
        <Setter Property="WeekNumberSelectionBorderThickness" Value="1" />
        <Setter Property="WeekNumberSelectionBorderCornerRadius" Value="2" />
        <Setter Property="Calendar" Value="{Binding	Path=Culture, RelativeSource={RelativeSource Self}, Converter={StaticResource CultureToCalendarConverter}}" />
        <Setter Property="Background" Value="{DynamicResource  MetroPanelBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}" />
        <Setter Property="TodayCellForeground"  Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource  MetroBorderBrush}" />
        <Setter Property="HeaderBackground" Value="{DynamicResource  DefaultHeaderBackground}" />
        <Setter Property="HeaderForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="SelectionForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="SelectionBorderBrush" Value="{DynamicResource  MetroBrush}" />
        <Setter Property="WeekNumberSelectionBorderBrush" Value="Transparent" />
        <Setter Property="WeekNumberHoverBorderBrush" Value="Transparent" />
        <Setter Property="WeekNumberHoverBackground" Value="Transparent" />
        <Setter Property="WeekNumberHoverForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="WeekNumberSelectionBackground" Value="Transparent" />
        <Setter Property="WeekNumberSelectionBorderBrush" Value="Transparent" />
        <Setter Property="WeekNumberSelectionForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="WeekNumberBackground" Value="Transparent" />
        <Setter Property="WeekNumberForeground" Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="MouseOverBackground" Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="MouseOverBorderBrush" Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="MouseOverForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="SelectedDayCellBorderBrush" Value="{DynamicResource  MetroBrush}" />
        <Setter Property="SelectedDayCellBackground" Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="SelectedDayCellForeground" Value="{DynamicResource  MetroForegroundBrush}" />
        <Setter Property="TodayCellBorderBrush" Value="{DynamicResource  MetroBrush}" />
        <Setter Property="TodayCellSelectedBorderBrush"  Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="SelectedDayCellHoverBackground"  Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="TodayCellBackground" Value="Transparent" />
        <Setter Property="TodayCellSelectedBackground" Value="{DynamicResource  MetroHoverBrush}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="NotCurrentMonthForeground" Value="#FFA3ABB5" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate  TargetType="{x:Type local:CalendarEdit}">
                    <Border Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                Background="{TemplateBinding Background}">
                        <!--<Border BorderBrush="{StaticResource Default.CalendarInnerBorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">-->
                        <Grid x:Name="MainGrid" FlowDirection="{TemplateBinding FlowDirection}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="10" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="4*" />
                                <ColumnDefinition />
                                <ColumnDefinition Width="10" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="1*" />
                                <RowDefinition Height="1.5*" />
                                <RowDefinition Height="5.5*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="7" />
                            </Grid.RowDefinitions>

                            <Grid Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="6">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="4*" />
                                    <ColumnDefinition />
                                </Grid.ColumnDefinitions>

                                <StackPanel Name="PART_NextMonthButtonPanel" VerticalAlignment="Center" KeyboardNavigation.IsTabStop="False" Grid.Row="0" Grid.Column="2" Background="{Binding Path=HeaderBackground, RelativeSource={RelativeSource TemplatedParent}}">
                                    <local:NavigateButton x:Name="PART_NextMonthButton" IsTabStop="False"
				            Foreground="{Binding Path=HeaderForeground, RelativeSource={RelativeSource TemplatedParent}}"
				            MinWidth="5" MinHeight="5" Margin="0,0,8,0"
				            HorizontalAlignment="Right" VerticalAlignment="Center"
				            Template="{StaticResource OfficeButtonNextDefault}" />
                                </StackPanel>
                                <StackPanel VerticalAlignment="Center"  Name="PART_PrevMonthButtonPanel" KeyboardNavigation.IsTabStop="False"  Grid.Row="0"  Grid.Column="0" Background="{Binding Path=HeaderBackground, RelativeSource={RelativeSource TemplatedParent}}">
                                    <local:NavigateButton x:Name ="PART_PrevMonthButton" IsTabStop="False"
				                Foreground="{Binding Path=HeaderForeground, RelativeSource={RelativeSource TemplatedParent}}"
				                MinWidth="5" MinHeight="5" Margin="8,0,0,0"
				                HorizontalAlignment="Left" VerticalAlignment="Center"
				                Template="{StaticResource OfficeButtonPrevDefault}" />
                                </StackPanel>

                                <local:MonthButton VerticalAlignment="Center"  Name="PART_Month1" Grid.Row="0" Grid.Column="1"
                                            Style="{StaticResource OfficeMonthButtonStyle}"
							     Background="{Binding Path=HeaderBackground, RelativeSource={RelativeSource TemplatedParent}}"
							     Foreground="{Binding Path=HeaderForeground, RelativeSource={RelativeSource TemplatedParent}}" />
                                <local:MonthButton VerticalAlignment="Center"  Name="PART_Month2" Grid.Row="0" Grid.Column="1"
                                            Style="{StaticResource OfficeMonthButtonStyle}"
							     Background="{Binding Path=HeaderBackground, RelativeSource={RelativeSource TemplatedParent}}"
							     Foreground="{Binding Path=HeaderForeground, RelativeSource={RelativeSource TemplatedParent}}" />

                                <StackPanel Name="PART_YearUpDownPanel"
                              Grid.Row="0" Grid.Column="1"
                              Visibility="Collapsed"
                              HorizontalAlignment="Center"
                              Orientation="Horizontal">
                                    <TextBlock Name="PART_EditMonthName"
                                   Padding="1"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right" />
                                    <local:UpDown Name="PART_YearUpDown"
                                        MinValue="{Binding Path=MinDate.Year, RelativeSource={RelativeSource AncestorType={x:Type local:CalendarEdit}}}"
                                        MaxValue="{Binding Path=MaxDate.Year, RelativeSource={RelativeSource AncestorType={x:Type local:CalendarEdit}}}"
                                        Width="45" NumberDecimalDigits="0"
                                        Height="18"
                                        HorizontalAlignment="Center"
                                        BorderThickness="0" />
                                </StackPanel>
                            </Grid>

                            <Popup Name="PART_MonthPopup" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="6" Placement="Center">
                                <ListBox Width="{TemplateBinding Width}" HorizontalContentAlignment ="Center" />
                            </Popup>

                            <ContentPresenter Name="PART_WeekNumbers" Visibility="Collapsed" Grid.Column="1" Grid.Row="2"
                    ClipToBounds="True"
                    Content="{Binding Path=WeekNumbersGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter VerticalAlignment="Center"  Grid.Column="2" Grid.Row="1" Grid.ColumnSpan="3"  Content="{Binding Path=DayNamesGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=CurrentDayGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Name="WeekNumbersForYearCurrent" Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=CurrentWeekNumbersGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Name="WeekNumbersForYearFollow" Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=FollowingWeekNumbersGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=FollowingDayGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="1" Grid.RowSpan="2"  Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=CurrentMonthGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=CurrentYearGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=CurrentYearRangeGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Margin="0,4,0,0"  Grid.Row="1" Grid.RowSpan="2"  Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=FollowingMonthGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=FollowingYearGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <ContentPresenter Grid.Column="2" Grid.Row="1" Grid.RowSpan="2" Grid.ColumnSpan="3" ClipToBounds="True" Content="{Binding Path=FollowingYearRangeGrid, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Grid  Name="PART_TodayGrid" Grid.Row="3" Grid.ColumnSpan="3"  Grid.Column="1"
						Background="Transparent"
						TextBlock.Foreground="{DynamicResource  MetroForegroundBrush}"
						Visibility="{Binding Path=TodayRowIsVisible, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Button Grid.Column="0"  x:Uid="AccessTodayTextID"
							Content="{StaticResource AccessTodayText}"
							Style="{StaticResource OfficeTodayButtonStyle}"
							Width="Auto" Name="PART_TodayButton" />

                                <Border Grid.Column="1"
							HorizontalAlignment="Center"
							VerticalAlignment="Center">
                                    <TextBlock HorizontalAlignment="Center"
								Text="{Binding Path=TodayDate, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Border>
                            </Grid>
                        </Grid>
                        <!--</Border>-->
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" TargetName="Border" />
                        </Trigger>

                        <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="MinHeight" TargetName="PART_NextMonthButton" Value="40" />
                            <Setter Property="MinWidth" TargetName="PART_NextMonthButton" Value="40" />
                            <Setter Property="MinHeight" TargetName="PART_PrevMonthButton" Value="40" />
                            <Setter Property="MinWidth" TargetName="PART_PrevMonthButton" Value="40" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                <Setter Property="Width" Value="420" />
                <Setter Property="Height" Value="380" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontStyle" Value="Normal" />
                <Setter Property="FontFamily" Value="Segoe UI" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <Style BasedOn="{StaticResource MetroCalendarEditStyle}" TargetType="{x:Type local:CalendarEdit}" />
</ResourceDictionary>