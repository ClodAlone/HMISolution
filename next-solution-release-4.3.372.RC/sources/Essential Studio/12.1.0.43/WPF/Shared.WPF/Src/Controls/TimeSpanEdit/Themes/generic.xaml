<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Shared"
                    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
>

    <SolidColorBrush x:Key="GlyphBrushKey1" Color="#FF5F6EA5" />

    <local:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <LinearGradientBrush x:Key="ButtonPressedBrush.Blue" EndPoint="0,1" StartPoint="0,0">
        <GradientStop Color="#FFF3B277" Offset="0" />
        <GradientStop Color="#FFF4B378" Offset="0.4" />
        <GradientStop Color="#FFF4A770" Offset="0.4" />
        <GradientStop Color="#FFF37E24" Offset="1" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="ForegroundBrush.Blue" Color="Black" />

    <LinearGradientBrush x:Key="TextboxMouseOverBorderBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FF5794BF" Offset="0" />
        <GradientStop Color="#FFC7E2F1" Offset="1" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="UpdownBackground" Color="White" />
    <LinearGradientBrush x:Key="RepeatButtonBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFF2F2F2" Offset="0.102" />
        <GradientStop Color="#FFE8E8E8" Offset="0.932" />
        <GradientStop Color="#FFD1D1D1" Offset="0.966" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonBorderBrush" Color="#FFABADB3" />
    <LinearGradientBrush x:Key="RepeatButtonHoverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFE9F6FD" Offset="0.134" />
        <GradientStop Color="#FFD2EDFC" Offset="0.966" />
        <GradientStop Color="#FFE9F5FC" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonHoverBorderBrush" Color="#FF3C7FB1" />
    <LinearGradientBrush x:Key="RepeatButtonPressedBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFA6C7D9" Offset="0.113" />
        <GradientStop Color="#FFC2E4F6" Offset="0.135" />
        <GradientStop Color="#FFC2E4F6" Offset="0.842" />
        <GradientStop Color="#FF91CCEB" Offset="0.865" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonPressedBorderBrush" Color="#FF3C7FB1" />

    <ControlTemplate x:Key="RepeatButtonTemplate" TargetType="{x:Type RepeatButton}">
        <Border Name="Chrome"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                                        CornerRadius="0"
                                        SnapsToDevicePixels="true">
            <Border Name="innerBorder" BorderBrush="{TemplateBinding Background}" BorderThickness="1" CornerRadius="0" SnapsToDevicePixels="True">
                <ContentPresenter Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="local:SkinStorage.EnableTouch" Value="False" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource RepeatButtonHoverBorderBrush}" />
                <Setter TargetName="innerBorder" Property="BorderBrush" Value="#FFFBFBFB" />
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource RepeatButtonHoverBackground}" />
            </MultiTrigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource RepeatButtonPressedBorderBrush}" />
                <Setter TargetName="innerBorder" Property="BorderBrush" Value="Transparent" />
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource RepeatButtonPressedBackground}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="RepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background"
                Value="{StaticResource RepeatButtonBackground}" />
        <Setter Property="BorderBrush"
                Value="{StaticResource RepeatButtonBorderBrush}" />
        <Setter Property="Foreground"
                Value="{StaticResource ForegroundBrush.Blue}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="HorizontalContentAlignment"
                Value="Center" />
        <Setter Property="VerticalContentAlignment"
                Value="Center" />
        <Setter Property="Padding"
                Value="0" />
        <Setter Property="Template" Value="{StaticResource RepeatButtonTemplate}" />
    </Style>
    <Style TargetType="{x:Type  local:TimeSpanEdit}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="Background" Value="{StaticResource UpdownBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource RepeatButtonBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="AllowDrop" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimeSpanEdit}">

                    <Microsoft_Windows_Themes:ListBoxChrome x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" RenderMouseOver="{TemplateBinding IsMouseOver}" RenderFocused="{TemplateBinding IsKeyboardFocusWithin}" SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <ScrollViewer HorizontalAlignment="Stretch"  VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <RepeatButton x:Name="upbutton" IsTabStop="False" Margin="0,-1,-1,0" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=UpCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0" Width="17"  Style="{StaticResource RepeatButtonStyle}">
                                    <Path Stretch="Uniform" x:Name="upbuttonpath" Width="8" Height="4" Fill="{StaticResource GlyphBrushKey1}" Data="F1 M 541.537,173.589L 531.107,173.589L 536.322,167.49L 541.537,173.589 Z " HorizontalAlignment="Center"  VerticalAlignment="Center">
                                    </Path>
                                </RepeatButton>
                                <RepeatButton x:Name="downbutton" IsTabStop="False" Margin="0,0,-1,-1" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=DownCommand, RelativeSource={RelativeSource TemplatedParent}}" Width="17" Grid.Row="1" Style="{StaticResource RepeatButtonStyle}">
                                    <Path Stretch="Uniform" x:Name="downbuttonpath" Fill="{StaticResource GlyphBrushKey1}" Height="4" Width="8" Data="F1 M 531.107,321.943L 541.537,321.943L 536.322,328.042L 531.107,321.943 Z " HorizontalAlignment="Center"  VerticalAlignment="Center">
                                    </Path>
                                </RepeatButton>
                            </Grid>
                        </Grid>
                    </Microsoft_Windows_Themes:ListBoxChrome>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource TextboxMouseOverBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                        <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="1,8" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Width" TargetName="upbutton" Value="40" />
                            <Setter Property="Width" TargetName="downbutton" Value="40" />
                            <Setter Property="Height" TargetName="upbutton" Value="40" />
                            <Setter Property="Height" TargetName="downbutton" Value="40" />
                            <Setter Property="Width" TargetName="upbuttonpath" Value="12" />
                            <Setter Property="Width" TargetName="downbuttonpath" Value="12" />
                            <Setter Property="Height" TargetName="downbuttonpath" Value="8" />
                            <Setter Property="Height" TargetName="upbuttonpath" Value="8" />
                            <Setter Property="EnableTouch" Value="True" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>