<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Shared"
                    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
>

    <local:BoolToVisibilityConverter x:Key="BooleanToVisibilityConverter" />

    <SolidColorBrush x:Key="BlendTimeSpanEditRpeatButtonBorderBrush" Color="#FF333333" />

    <SolidColorBrush x:Key="BlendTimeSpanEditRepeatButtonMouseOverBackgroundBrush" Color="#FFAAAAAA" />
    <SolidColorBrush x:Key="BlendTimeSpanEditButtonPressedBackgroundBrush" Color="#FF898989" />
    <SolidColorBrush x:Key="BlendTimeSpanEditRepeatButtonBackground" Color="#FF808080" />

    <ControlTemplate x:Key="RepeatButtonTemplate" TargetType="{x:Type RepeatButton}">

        <Border Name="Chrome" CornerRadius="2" BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        SnapsToDevicePixels="true">
            <Border Background="Transparent" BorderThickness="0" Margin="1">
                <ContentPresenter Margin="{TemplateBinding Padding}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
            </Border>
        </Border>
        <ControlTemplate.Triggers>

            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="local:SkinStorage.EnableTouch" Value="False" />
                    <Condition Property="IsMouseOver" Value="True" />
                </MultiTrigger.Conditions>
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource BlendTimeSpanEditRepeatButtonMouseOverBackgroundBrush}" />
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource BlendTimeSpanEditRpeatButtonBorderBrush}" />
                <Setter Property="Foreground" Value="Black" />
            </MultiTrigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource BlendTimeSpanEditButtonPressedBackgroundBrush}" />
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource BlendTimeSpanEditRpeatButtonBorderBrush}" />
                <Setter Property="Foreground" Value="White" />
            </Trigger>
            <Trigger Property="Button.IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <!-- Repeat Button Style -->
    <Style x:Key="RepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="Background" Value="{StaticResource BlendTimeSpanEditRepeatButtonBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BlendTimeSpanEditRpeatButtonBorderBrush}" />

        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template" Value="{StaticResource RepeatButtonTemplate}" />
    </Style>

    <SolidColorBrush Color="#FF333333" x:Key="BlendTimeSpanEditBackground" />
    <SolidColorBrush Color="#FF333333" x:Key="BlendTimeSpanEditBorderBrush" />
    <SolidColorBrush Color="WhiteSmoke" x:Key="BlendTimeSpanEditForeground" />
    <SolidColorBrush Color="#FF707070" x:Key="BlendTimeSpanEditMouseOverBorderBrush" />
    <Style x:Key="BlendTimeSpanEditStyle" TargetType="{x:Type  local:TimeSpanEdit}">
        <Setter Property="Foreground" Value="{StaticResource  BlendTimeSpanEditForeground}" />
        <Setter Property="Background" Value="{StaticResource  BlendTimeSpanEditBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource BlendTimeSpanEditMouseOverBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="AllowDrop" Value="False" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="Stylus.IsFlicksEnabled" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:TimeSpanEdit}">

                    <Border x:Name="Bd" CornerRadius="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}"  SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="Auto"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Center" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" x:Name="PART_ContentHost" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />

                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition />
                                </Grid.RowDefinitions>
                                <RepeatButton x:Name="upbutton" IsTabStop="False" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=UpCommand, RelativeSource={RelativeSource TemplatedParent}}" Grid.Row="0"   Style="{StaticResource RepeatButtonStyle}" Width="15">
                                    <Path Stretch="Uniform" x:Name="upbuttonpath" Height="5" Width="8" Fill="Transparent" Data="M 1,5 L 4.5,1 L 8,5" HorizontalAlignment="Center" VerticalAlignment="Center" Stroke="White" StrokeThickness="1.5" Margin="1" />
                                </RepeatButton>
                                <RepeatButton x:Name="downbutton" IsTabStop="False" Visibility="{Binding ShowArrowButtons, Converter={StaticResource BooleanToVisibilityConverter}, RelativeSource={RelativeSource TemplatedParent}}" Command="{Binding Path=DownCommand, RelativeSource={RelativeSource TemplatedParent}}"  Grid.Row="1" Style="{StaticResource RepeatButtonStyle}" Width="15">
                                    <Path Stretch="Uniform" x:Name="downbuttonpath" Height="5" Width="8" Fill="Transparent" Data="M 1,0 L 4.5,5 L 8,0" HorizontalAlignment="Center" VerticalAlignment="Center" Stroke="White" StrokeThickness="1.5" Margin="1"></Path>
                                </RepeatButton>
                            </Grid>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity"  TargetName="Bd" Value="0.5" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="true">
                            <!--<Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource  BlendTimeSpanEditMouseOverBorderBrush}" />-->
                        </Trigger>
                        <Trigger Property="local:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="1,8" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Width" TargetName="upbutton" Value="40" />
                            <Setter Property="Width" TargetName="downbutton" Value="40" />
                            <Setter Property="Height" TargetName="upbutton" Value="25" />
                            <Setter Property="Height" TargetName="downbutton" Value="25" />
                            <Setter Property="Width" TargetName="upbuttonpath" Value="12" />
                            <Setter Property="Width" TargetName="downbuttonpath" Value="12" />
                            <Setter Property="Height" TargetName="downbuttonpath" Value="8" />
                            <Setter Property="Height" TargetName="upbuttonpath" Value="8" />
                            <Setter Property="EnableTouch" Value="True" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource BlendTimeSpanEditStyle}" TargetType="{x:Type local:TimeSpanEdit}" />
</ResourceDictionary>