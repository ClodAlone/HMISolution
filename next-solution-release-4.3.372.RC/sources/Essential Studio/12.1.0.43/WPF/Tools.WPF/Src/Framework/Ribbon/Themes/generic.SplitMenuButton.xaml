<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
					xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
					xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"					
          xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"    
					xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna">

    <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Blue.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Silver.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Black.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Default.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Office2003.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.ShinyBlue.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Blend.xaml"/>
            <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.ShinyRed.xaml"/>
       
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/generic.CustomScrollViewer.xaml"/>
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Converters.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--SplitMenuButton template-->
    <ControlTemplate x:Key="Office2007BlueSplitMenuButtonTemplate" TargetType="{x:Type local:SplitMenuButton}">
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid HorizontalAlignment="Stretch">
                <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                    <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                        <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                            <Path Fill="{StaticResource Blue.Foreground}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                    </Border>
                    <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                        <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                            <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16" 
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource Blue.Foreground}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                            </WrapPanel>
                        </Border>
                    </Border>
                </DockPanel>
                <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                        <Border Background="{StaticResource Default.SimpleMenuPopupBrush}" BorderThickness="1" CornerRadius="{StaticResource Default.QuickDropDownCornerRadius}" BorderBrush="{StaticResource Default.QuickDropDownBorderBrush}">
                            <ScrollViewer x:Name="PART_ScrollViwer" Margin="0,0,0,-5" Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsPresenter
								  Margin="0,0,0,5"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </Microsoft_Windows_Themes:SystemDropShadowChrome>
                </local:CustomPopup>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blue.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMenuOpen" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blue.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blue.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Setter Property="CanCommandExecute" Value="True"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="Office2007BlackSplitMenuButtonTemplate" TargetType="{x:Type local:SplitMenuButton}">
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid HorizontalAlignment="Stretch">
                <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                    <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                        <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                            <Path Fill="{StaticResource Black.Foreground}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                    </Border>
                    <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                        <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                            <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16" 
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource Black.Foreground}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                            </WrapPanel>
                        </Border>
                    </Border>
                </DockPanel>
                <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                        <Border Background="{StaticResource Default.SimpleMenuPopupBrush}" BorderThickness="1" CornerRadius="{StaticResource Default.QuickDropDownCornerRadius}" BorderBrush="{StaticResource Default.QuickDropDownBorderBrush}">
                            <ScrollViewer x:Name="PART_ScrollViwer" Margin="0,0,0,-5" Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsPresenter
								  Margin="0,0,0,5"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </Microsoft_Windows_Themes:SystemDropShadowChrome>
                </local:CustomPopup>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Black.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMenuOpen" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Black.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Black.Foreground}"/>
            </Trigger>

            <Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Setter Property="CanCommandExecute" Value="True"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Black.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="Office2007SilverSplitMenuButtonTemplate" TargetType="{x:Type local:SplitMenuButton}">
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid HorizontalAlignment="Stretch">
                <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                    <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                        <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                            <Path Fill="{StaticResource Silver.Foreground}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                    </Border>
                    <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                        <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                            <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16" 
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource Silver.Foreground}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                            </WrapPanel>
                        </Border>
                    </Border>
                </DockPanel>
                <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                        <Border Background="{StaticResource Default.SimpleMenuPopupBrush}" BorderThickness="1" CornerRadius="{StaticResource Default.QuickDropDownCornerRadius}" BorderBrush="{StaticResource Default.QuickDropDownBorderBrush}">
                            <ScrollViewer x:Name="PART_ScrollViwer" Margin="0,0,0,-5" Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsPresenter
								  Margin="0,0,0,5"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </Microsoft_Windows_Themes:SystemDropShadowChrome>
                </local:CustomPopup>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMenuOpen" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
            </Trigger>

            <Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Setter Property="CanCommandExecute" Value="True"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="Background" Value="{StaticResource MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="Office2003SplitMenuButtonTemplate" TargetType="{x:Type local:SplitMenuButton}">
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid HorizontalAlignment="Stretch">
                <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                    <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                        <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                            <Path Fill="{StaticResource Blue.Foreground}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                    </Border>
                    <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                        <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                            <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16" 
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource Blue.Foreground}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                            </WrapPanel>
                        </Border>
                    </Border>
                </DockPanel>
                <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                        <Border Background="{StaticResource Default.SimpleMenuPopupBrush}" BorderThickness="1" CornerRadius="{StaticResource Office2003.QuickDropDownCornerRadius}" BorderBrush="{StaticResource Office2003.QuickDropDownBorderBrush}">
                            <ScrollViewer x:Name="PART_ScrollViwer" Margin="0,0,0,-5" Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsPresenter
								  Margin="0,0,0,5"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </Microsoft_Windows_Themes:SystemDropShadowChrome>
                </local:CustomPopup>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Office2003.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Office2003.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMenuOpen" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Office2003.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Office2003.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Office2003.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Office2003.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>

            <Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Setter Property="CanCommandExecute" Value="True"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Office2003.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="Background" Value="{StaticResource Office2003.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="BlendSplitMenuButtonTemplate" TargetType="{x:Type local:SplitMenuButton}">
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid HorizontalAlignment="Stretch">
                <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                    <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                        <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                            <Path Fill="{StaticResource Blend.Foreground2}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                <Path.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Path.RenderTransform>
                            </Path>
                        </Border>
                    </Border>
                    <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                        <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                            <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16" 
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource Blend.Foreground2}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                            </WrapPanel>
                        </Border>
                    </Border>
                </DockPanel>
                <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                    <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                        <Border Background="{StaticResource Blend.SimpleMenuPopupBrush}" BorderThickness="1" CornerRadius="{StaticResource Blend.QuickDropDownCornerRadius}" BorderBrush="{StaticResource Blend.QuickDropDownBorderBrush}">
                            <ScrollViewer x:Name="PART_ScrollViwer" Margin="0,0,0,-5" Style="{StaticResource CustomScrollViewerStyle}">
                                <ItemsPresenter
								  Margin="0,0,0,5"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                            </ScrollViewer>
                        </Border>
                    </Microsoft_Windows_Themes:SystemDropShadowChrome>
                </local:CustomPopup>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Blend.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blend.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
            </Trigger>

            <Trigger Property="IsMenuOpen" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Blend.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blend.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
            </Trigger>

            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Blend.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="Background" Value="{StaticResource Blend.IsDropDownOpenBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
            </Trigger>

            <Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Setter Property="CanCommandExecute" Value="True"/>
                <Setter TargetName="PART_ToggleButton"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_ToggleButton"  Property="Background" Value="{StaticResource Blend.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_OutterBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.OutterBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="PART_InnerBorder"  Property="Background" Value="{StaticResource Blend.MenuButton.MouseOverBrush}"/>
                <Setter TargetName="PART_TogleButtonInnerBorder"  Property="BorderBrush" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                <Setter TargetName="label" Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitMenuButtonTemplate.ShinyRed" TargetType="{x:Type local:SplitMenuButton}">
        <ControlTemplate.Resources>
            <Storyboard x:Key="HighlightedOn">
                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.Opacity)">
                    <SplineDoubleKeyFrame KeyTime="00:00:00.1000000" Value="1"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
            <Storyboard x:Key="HighlightedOff">
                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.Opacity)">
                    <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
        </ControlTemplate.Resources>
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid>
                <Border Background="{StaticResource NormalBackgroundBrushKey.ShinyRed}"  
		    Opacity="0" x:Name="border"/>
                <Grid HorizontalAlignment="Stretch">

                    <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                        <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                            <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                                <Path Fill="{StaticResource TextBrushKey.ShinyRed}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="-90"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Border>
                        </Border>
                        <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                            <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                                <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                    <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16"
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                    <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource TextBrushKey.ShinyRed}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                                </WrapPanel>
                            </Border>
                        </Border>
                    </DockPanel>
                    <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                        <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                            <Border Background="{StaticResource MenuBackgroundBrushKey.ShinyRed}" BorderThickness="1" CornerRadius="{StaticResource Default.QuickDropDownCornerRadius}" BorderBrush="{StaticResource NormalBorderBrushKey.ShinyRed}">
                                <ScrollViewer x:Name="PART_ScrollViwer"  Style="{StaticResource CustomScrollViewerStyle}">
                                    <ItemsPresenter
								  Margin="{TemplateBinding Control.Padding}"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Border>
                        </Microsoft_Windows_Themes:SystemDropShadowChrome>
                    </local:CustomPopup>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="true">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsMenuOpen" Value="true">
                <!--<Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard"/>
                </Trigger.ExitActions>-->
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard1"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>

            <!--<Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard2"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SplitMenuButtonTemplate.ShinyBlue" TargetType="{x:Type local:SplitMenuButton}">
        <ControlTemplate.Resources>
            <Storyboard x:Key="HighlightedOn">
                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.Opacity)">
                    <SplineDoubleKeyFrame KeyTime="00:00:00.1000000" Value="1"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
            <Storyboard x:Key="HighlightedOff">
                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="border" Storyboard.TargetProperty="(UIElement.Opacity)">
                    <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0"/>
                </DoubleAnimationUsingKeyFrames>
            </Storyboard>
        </ControlTemplate.Resources>
        <Border	BorderThickness="{TemplateBinding BorderThickness}"
				BorderBrush="{TemplateBinding BorderBrush}"
				Background="{TemplateBinding Background}"
				HorizontalAlignment="Stretch"			
				Opacity="{TemplateBinding Opacity}"
				IsEnabled="{TemplateBinding IsEnabled}">
            <Grid>
                <Border Background="{StaticResource NormalBackgroundBrushKey.ShinyBlue}"  
		    Opacity="0" x:Name="border"/>
                <Grid HorizontalAlignment="Stretch">

                    <DockPanel HorizontalAlignment="Stretch" LastChildFill="True" >
                        <Border Name="PART_ToggleButton"
							Width="24"							
							DockPanel.Dock="Right"
							Background="Transparent"
							BorderThickness="0,1,1,1"
							BorderBrush="{TemplateBinding BorderBrush}">
                            <Border x:Name="PART_TogleButtonInnerBorder" BorderThickness="1">
                                <Path Fill="{StaticResource TextBrushKey.ShinyBlue}" Margin="5,16,0,0"
									  HorizontalAlignment="Center"
									  VerticalAlignment="Center"
									  Data="M 0 0 L 4 4 L 8 0 Z">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="-90"/>
                                    </Path.RenderTransform>
                                </Path>
                            </Border>
                        </Border>
                        <Border x:Name="PART_OutterBorder" BorderThickness="1" HorizontalAlignment="Stretch" DockPanel.Dock="Left">
                            <Border x:Name="PART_InnerBorder" BorderThickness="1" Background="Transparent">
                                <WrapPanel
								  Orientation="Horizontal"
								  VerticalAlignment="Center"
								  HorizontalAlignment="Left"
								  Margin="1">
                                    <Image Margin="5,3,5,3" Grid.Row="0"
                                        Width="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=width, RelativeSource={RelativeSource TemplatedParent}}"
                                        Height="{Binding Path=IconSize, Converter={StaticResource SizeToDoubleConverter}, ConverterParameter=height, RelativeSource={RelativeSource TemplatedParent}}"
							           MinHeight="16" MinWidth="16"
                                       Source="{Binding Path=Icon, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}"
								       />

                                    <TextBlock Margin="0,0,7,0" Name="label"
										   TextWrapping="Wrap"
										   TextAlignment="Center"
										   Foreground="{StaticResource TextBrushKey.ShinyBlue}"
										   VerticalAlignment="Center"
										   HorizontalAlignment="Center"
										   Text="{TemplateBinding Label}"/>
                                </WrapPanel>
                            </Border>
                        </Border>
                    </DockPanel>
                    <local:CustomPopup Name="PART_Popup"
					  Placement="Custom"
					  PlacementTarget ="{Binding Path=PlacementBorder, Mode=OneWay, RelativeSource={RelativeSource AncestorType={x:Type local:ApplicationMenu}}}"
					  Focusable="False"
					  PopupAnimation="Fade"
					  AllowsTransparency="True"
					  StaysOpen="True"
					  IsOpen="{Binding Path=IsMenuOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                        <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" SnapsToDevicePixels="true">
                            <Border Background="{StaticResource MenuBackgroundBrushKey.ShinyBlue}" BorderThickness="1" CornerRadius="{StaticResource Default.QuickDropDownCornerRadius}" BorderBrush="{StaticResource NormalBorderBrushKey.ShinyBlue}">
                                <ScrollViewer x:Name="PART_ScrollViwer"  Style="{StaticResource CustomScrollViewerStyle}">
                                    <ItemsPresenter
								  Margin="{TemplateBinding Control.Padding}"
								  HorizontalAlignment="Stretch"
								  VerticalAlignment="Stretch"
								  SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"/>
                                </ScrollViewer>
                            </Border>
                        </Microsoft_Windows_Themes:SystemDropShadowChrome>
                    </local:CustomPopup>
                </Grid>
            </Grid>
        </Border>

        <ControlTemplate.Triggers>
            <Trigger Property="HasDropShadow" Value="True" SourceName="PART_Popup">
                <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,4,4"/>
            </Trigger>

            <Trigger Property="IsFocused" Value="true">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsMenuOpen" Value="true">
                <!--<Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard"/>
                </Trigger.ExitActions>-->
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="true">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard1"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>

            <!--<Trigger SourceName="PART_OutterBorder"  Property="IsMouseOver" Value="True">
                <Trigger.ExitActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOff}" x:Name="HighlightedOff_BeginStoryboard2"/>
                </Trigger.ExitActions>
                <Trigger.EnterActions>
                    <BeginStoryboard Storyboard="{StaticResource HighlightedOn}"/>
                </Trigger.EnterActions>
                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
            </Trigger>-->
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <Style TargetType="{x:Type local:SplitMenuButton}">        
        <Setter Property="Padding" Value="1"/>       
        <Setter Property="Template" Value="{StaticResource Office2007BlueSplitMenuButtonTemplate}"/>
        <Style.Triggers>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2007Black">
                <Setter Property="Template" Value="{StaticResource Office2007BlackSplitMenuButtonTemplate}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver">
                <Setter Property="Template" Value="{StaticResource Office2007SilverSplitMenuButtonTemplate}"/>                
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2003">
                <Setter Property="Template" Value="{StaticResource Office2003SplitMenuButtonTemplate}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Blend">
                <Setter Property="Template" Value="{StaticResource BlendSplitMenuButtonTemplate}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="ShinyRed">
                <Setter Property="Template" Value="{StaticResource SplitMenuButtonTemplate.ShinyRed}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="ShinyBlue">
                <Setter Property="Template" Value="{StaticResource SplitMenuButtonTemplate.ShinyBlue}"/>
            </Trigger>            
        </Style.Triggers>
    </Style>

</ResourceDictionary>
