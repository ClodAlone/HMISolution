<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
	xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF">

    <ResourceDictionary.MergedDictionaries>
       
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Blue.xaml"/>

        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Silver.xaml"/>
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Black.xaml"/>

        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Default.xaml"/>
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Office2003.xaml"/>

        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.ShinyBlue.xaml"/>
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.Blend.xaml"/>

        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Framework/Ribbon/Themes/Generic.Brushes.ShinyRed.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="UpButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border BorderBrush="#FFC5D1DE" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{DynamicResource RepeatBorderBackgroundDisabled}">
                        <Border BorderBrush="#FFE8EAED" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorderInner">
                            <Path Data="M 0 4 L 7 4 L 3.5 0 Z" Fill="#FFB7B7B7" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="UIElement.IsEnabled" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blue.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Silver.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blue.Path.Fill}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>                                
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Black.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Black.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Black.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Black.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Silver.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Silver.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Silver.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Silver.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blue.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blend.PathFill}" />
                        </MultiTrigger>
                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Black.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </MultiTrigger>
                       
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.CheckBoxBackground}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.CheckBoxBackground}" />
                        </MultiTrigger>
                        <Trigger Property="ButtonBase.IsPressed" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>                              
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>                                
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                      
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UpButtonStyle.ShinyRed" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">          
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="HoverOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="HoverOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="0.6"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>                       
                    </ControlTemplate.Resources>
                    <Border BorderBrush="{StaticResource NormalBorderBrushKey.ShinyRed}" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{StaticResource NormalBackgroundBrushKey.ShinyRed}">
                        <Grid>
                            <Border x:Name="Hover" BorderThickness="{TemplateBinding BorderThickness}" Background="{StaticResource HighlightBrushKey.ShinyRed}"  CornerRadius="0" Opacity="0"/>

                            <Path Data="M 0 4 L 7 4 L 3.5 0 Z" Fill="#FFFFFFFF" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>                       
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOff}"/>
                            </Trigger.ExitActions>                           
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DownButtonStyle.ShinyRed" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="HoverOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="HoverOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="0.6"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                      
                    </ControlTemplate.Resources>
                    <Border BorderBrush="{StaticResource NormalBorderBrushKey.ShinyRed}" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{StaticResource NormalBackgroundBrushKey.ShinyBlue}">
                        <Grid>
                            <Border x:Name="Hover" BorderThickness="{TemplateBinding BorderThickness}" Background="{StaticResource HighlightBrushKey.ShinyRed}"  CornerRadius="0" Opacity="0"/>
                            <Path Data="M 0 0 L 3.5 4 L 7 0 Z" Fill="#FFFFFFFF" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="UpButtonStyle.ShinyBlue" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="HoverOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="HoverOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="0.6"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                    </ControlTemplate.Resources>
                    <Border BorderBrush="{StaticResource NormalBorderBrushKey.ShinyBlue}" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{StaticResource NormalBackgroundBrushKey.ShinyRed}">
                        <Grid>
                            <Border x:Name="Hover" BorderThickness="{TemplateBinding BorderThickness}" Background="{StaticResource HighlightBrushKey.ShinyBlue}"  CornerRadius="0" Opacity="0"/>

                            <Path Data="M 0 4 L 7 4 L 3.5 0 Z" Fill="#FFFFFFFF" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DownButtonStyle.ShinyBlue" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <ControlTemplate.Resources>
                        <Storyboard x:Key="HoverOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="HoverOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="Hover" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0" />
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOn">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.2000000" Value="0.6"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>
                        <Storyboard x:Key="PressedOff">
                            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="RepeatBorder" Storyboard.TargetProperty="(UIElement.Opacity)">
                                <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="1"/>
                            </DoubleAnimationUsingKeyFrames>
                        </Storyboard>

                    </ControlTemplate.Resources>
                    <Border BorderBrush="{StaticResource NormalBorderBrushKey.ShinyBlue}" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{StaticResource NormalBackgroundBrushKey.ShinyBlue}">
                        <Grid>
                            <Border x:Name="Hover" BorderThickness="{TemplateBinding BorderThickness}" Background="{StaticResource HighlightBrushKey.ShinyBlue}"  CornerRadius="0" Opacity="0"/>
                            <Path Data="M 0 0 L 3.5 4 L 7 0 Z" Fill="#FFFFFFFF" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource PressedOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOn}"/>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard Storyboard="{StaticResource HoverOff}"/>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DownButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True"/>
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Border BorderBrush="#FFC5D1DE" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorder" Background="{DynamicResource RepeatBorderBackgroundDisabled}">
                        <Border BorderBrush="#FFE8EAED" CornerRadius="0,0,0,0" BorderThickness="1,1,1,1" Name="RepeatBorderInner">
                            <Path Data="M 0 0 L 3.5 4 L 7 0 Z" Fill="#FFB7B7B7" Name="RepeatSign" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="UIElement.IsEnabled" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blue.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Silver.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blue.Path.Fill}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Black.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Black.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Black.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Black.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Silver.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Silver.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Silver.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Silver.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blue.Path.Fill}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.RepeatBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.RepeatBorderBackgroundEanbled}" />
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.RepeatInnerBorderBrush}" />
                            <Setter Property="Shape.Fill" TargetName="RepeatSign" Value="{StaticResource Blend.PathFill}" />
                        </MultiTrigger>
                        <Trigger Property="UIElement.IsMouseOver" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.Path.Fill}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blue.CheckBoxBackground}"/>
                            <Setter Property="Foreground" Value="{StaticResource Black.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blue.CheckBoxBackground}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.CheckBoxBackground}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsMouseOverBorderBrush}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsMouseOverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource Blend.Foreground3}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.CheckBoxBackground}" />
                        </MultiTrigger>
                        

                        <Trigger Property="ButtonBase.IsPressed" Value="True">
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Black"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blue.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Office2003"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Office2003.ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Office2003.MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="shared:SkinStorage.VisualStyle" Value="Blend"/>
                                <Condition Property="UIElement.IsEnabled" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsCheckedBorder}" />
                            <Setter Property="Panel.Background" TargetName="RepeatBorder" Value="{StaticResource Blend.ToggleButton.IsCheckedBrush}"/>
                            <Setter Property="Border.BorderBrush" TargetName="RepeatBorderInner" Value="{StaticResource Blend.MenuButton.InnerBorderBrush}"/>
                        </MultiTrigger>
                       
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="RibbonListBoxTemplate.Office2007Blue" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    
    <ControlTemplate x:Key="RibbonListBoxTemplate.Office2007Black" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="RibbonListBoxTemplate.Office2007Silver" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="RibbonListBoxTemplate.Office2003" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource Office2003.TextBoxBackgroundHover}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="RibbonListBoxTemplate.Blend" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource Blend.TextBoxBackgroundHover}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="RibbonListBoxTemplate.ShinyRed" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource TextBoxBackgroundBrushKey.ShinyRed}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle.ShinyRed}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle.ShinyRed}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
    <ControlTemplate x:Key="RibbonListBoxTemplate.ShinyBlue" TargetType="{x:Type ListBox}">
        <Border SnapsToDevicePixels="true" x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="1">
            <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                <ScrollViewer.Style>
                    <Style TargetType="{x:Type ScrollViewer}">
                        <Setter Property="OverridesDefaultStyle" Value="True" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Rectangle Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" x:Name="Corner" Grid.Column="1" Grid.Row="1"/>
                                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar">
                                            <ScrollBar.Style>
                                                <Style TargetType="{x:Type ScrollBar}">
                                                    <Setter Property="Stylus.IsFlicksEnabled" Value="false" />
                                                    <Setter Property="Background" Value="{StaticResource TextBoxBackgroundBrushKey.ShinyBlue}"/>
                                                    <Setter Property="Width" Value="16" />
                                                    <Setter Property="MinWidth" Value="16" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="{x:Type ScrollBar}">
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="*" />
                                                                        <RowDefinition Height="*" />
                                                                    </Grid.RowDefinitions>
                                                                    <RepeatButton Grid.Row="0" Style="{StaticResource UpButtonStyle.ShinyBlue}" Command="ScrollBar.LineUpCommand"/>


                                                                    <RepeatButton Grid.Row="2" Style="{StaticResource DownButtonStyle.ShinyBlue}" Command="ScrollBar.LineDownCommand"/>


                                                                </Grid>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ScrollBar.Style>
                                        </ScrollBar>
                                    </Grid>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ScrollViewer.Style>
                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" KeyboardNavigation.DirectionalNavigation="Contained"/>
            </ScrollViewer>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="false">
                <Setter Property="Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
            </Trigger>
            <Trigger Property="IsGrouping" Value="true">
                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style TargetType="{x:Type local:RibbonListBox}" x:Key="{x:Type local:RibbonListBox}">
        <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
		<Setter Property="BorderBrush" Value="{StaticResource ListBorder}"/>
		<Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Margin" Value="3,7,3,0"/>
		<Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
		<Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
		<Setter Property="VerticalContentAlignment" Value="Center"/>
		<Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.Office2007Blue}"/>
        <Style.Triggers>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2007Black">
                <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ListBorder}"/>
                <Setter Property="Foreground" Value="{StaticResource Black.Foreground}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.Office2007Black}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2007Silver">
                <Setter Property="Background" Value="{StaticResource Blue.TextBoxBackgroundHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ListBorder}"/>
                <Setter Property="Foreground" Value="{StaticResource Silver.Foreground}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.Office2007Silver}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Office2003">
                <Setter Property="Background" Value="{StaticResource Office2003.TextBoxBackgroundHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Office2003.ListBorder}"/>
                <Setter Property="Foreground" Value="{StaticResource Blue.Foreground}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.Office2003}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="Blend">
                <Setter Property="Background" Value="{StaticResource Blend.TextBoxBackgroundHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Blend.ListBorder}"/>
                <Setter Property="Foreground" Value="{StaticResource Blend.Foreground2}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.Blend}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="ShinyRed">
                <Setter Property="Background" Value="{StaticResource TextBoxBackgroundBrushKey.ShinyRed}"/>
                <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrushKey.ShinyRed}"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrushKey.ShinyRed}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.ShinyRed}"/>
            </Trigger>
            <Trigger Property="shared:SkinStorage.VisualStyle" Value="ShinyBlue">
                <Setter Property="Background" Value="{StaticResource TextBoxBackgroundBrushKey.ShinyBlue}"/>
                <Setter Property="BorderBrush" Value="{StaticResource NormalBorderBrushKey.ShinyBlue}"/>
                <Setter Property="Foreground" Value="{StaticResource TextBrushKey.ShinyBlue}"/>
                <Setter Property="Template" Value="{StaticResource RibbonListBoxTemplate.ShinyBlue}"/>
            </Trigger>
          
        </Style.Triggers>
		
	</Style>
</ResourceDictionary>