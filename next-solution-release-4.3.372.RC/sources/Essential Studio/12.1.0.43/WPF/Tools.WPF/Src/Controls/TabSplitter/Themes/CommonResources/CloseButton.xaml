<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls">

    <ResourceDictionary.MergedDictionaries>
        
        <ResourceDictionary Source="/Syncfusion.Tools.WPF;component/Controls/TabSplitter/Themes/Brushes.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <ControlTemplate x:Key="CloseButtonTemplate" TargetType="{x:Type ButtonBase}">
        <shared:Border3D x:Name="brdBack" Margin="1" BorderThickness="1"
                BorderBrush="{StaticResource Aero.ButtonInactiveBorderBrush}">
            <Path Name="pathButton" SnapsToDevicePixels="False" Stretch="Fill" StrokeThickness="1.5"
				Stroke="{StaticResource Aero.ButtonInactivePathBrush}"
                Data="M109,51 L216,142 M215,52 L109,142"
				HorizontalAlignment="Center" VerticalAlignment="Center" Width="9" Height="8"/>
        </shared:Border3D>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="brdBack" Property="IsInverted" Value="True"/>
                <Setter Property="Content" Value="1,1,-1,-1"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="brdBack" Property="Background" Value="{StaticResource Aero.ButtonBackgroundBrush}"/>
                <Setter TargetName="brdBack" Property="BorderBrush" Value="{StaticResource Aero.SplitterButtonActiveBorderBrush}"/>
                <Setter TargetName="pathButton" Property="Stroke" Value="{StaticResource Aero.ButtonActivePathBrush}" />
                <Setter TargetName="pathButton" Property="Fill" Value="{StaticResource Aero.ButtonActivePathBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="MenuButtonTemplate" TargetType="{x:Type ButtonBase}">
        <shared:Border3D x:Name="brdBack" Margin="1" BorderThickness="1"
                BorderBrush="{StaticResource Aero.ButtonInactiveBorderBrush}">
            <Path Name="pathButton" SnapsToDevicePixels="False" Stretch="Fill" StrokeThickness="1.5"
                Stroke="{StaticResource Aero.ButtonInactivePathBrush}" Fill="{StaticResource Aero.ButtonInactivePathBrush}"
				HorizontalAlignment="Center" VerticalAlignment="Center" Width="9" Height="8">
                <Path.Data>
                    <GeometryGroup>
                        <LineGeometry StartPoint="4,7" EndPoint="9,7"/>
                        <PathGeometry>
                            <PathFigure StartPoint="5,10" IsFilled="True" IsClosed="True" >
                                <LineSegment Point="8,10"/>
                                <LineSegment Point="6.5,12.5"/>
                            </PathFigure>
                        </PathGeometry>
                    </GeometryGroup>
                </Path.Data>
            </Path>
        </shared:Border3D>
        <ControlTemplate.Triggers>
            <DataTrigger Binding="{Binding Path=IsAllItemsVisible,RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                <Setter TargetName="pathButton" Property="Data" Value="M1,4L4.5,7 8,4Z"/>
                <Setter TargetName="pathButton" Property="Stretch" Value="None"/>
            </DataTrigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="brdBack" Property="IsInverted" Value="True"/>
                <Setter Property="Content" Value="1,1,-1,-1"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="brdBack" Property="Background" Value="{StaticResource Aero.ButtonBackgroundBrush}"/>
                <Setter TargetName="brdBack" Property="BorderBrush" Value="{StaticResource Aero.SplitterButtonActiveBorderBrush}"/>
                <Setter TargetName="pathButton" Property="Stroke" Value="{StaticResource Aero.ButtonActivePathBrush}" />
                <Setter TargetName="pathButton" Property="Fill" Value="{StaticResource Aero.ButtonActivePathBrush}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>
</ResourceDictionary>