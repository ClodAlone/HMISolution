<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF">

    <!--Brushes-->
    <SolidColorBrush x:Key="CheckListBoxBackgroundBrushKey" Color="#80FFFFFF" />
    <SolidColorBrush x:Key="CheckListBoxBorderBrush" Color="#7F000000" />
    <SolidColorBrush x:Key="CheckListBoxItemForeground" Color="Black" />
    <LinearGradientBrush x:Key="CheckListBoxItemMouseOverBrushKey" Opacity="0.4" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFAFAFA" Offset="0" />
        <GradientStop Color="White" Offset="0.491" />
        <GradientStop Color="#FFC8C8C8" Offset="0.531" />
        <GradientStop Color="#FFB6B6B6" Offset="0.789" />
        <GradientStop Color="#FFD5D5D5" Offset="0.996" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key ="CheckListBoxItemMouseOverBorderBrushKey" Opacity="0.2" Color="Black" />
    <LinearGradientBrush x:Key="CheckListBoxItemPressedBrushKey" Opacity="0.6" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFAFAFA" Offset="0" />
        <GradientStop Color="White" Offset="0.491" />
        <GradientStop Color="#FFC8C8C8" Offset="0.531" />
        <GradientStop Color="#FFB6B6B6" Offset="0.789" />
        <GradientStop Color="#FFD5D5D5" Offset="0.996" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key ="CheckListBoxItemPressedBorderBrushKey" Opacity="0.6" Color="Black" />
    <SolidColorBrush x:Key="CheckBoxBorderBrushKey" Color="#FF333333" Opacity=".5" />
    <SolidColorBrush x:Key="CheckBoxIconBrush" Color="#FF333333" />
    <SolidColorBrush x:Key="ContextMenuSeparatorBrush" Color="#FF434343" Opacity="0.495" />
    <SolidColorBrush x:Key="CheckListBoxItemBackground" Color="Transparent" />
    <SolidColorBrush x:Key="CheckListBoxItemBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="SelectedForeground.Blue" Color="Black" />
    <SolidColorBrush x:Key="CheckListBoxItemHoverForeground.Blue" Color="Black" />
    <LinearGradientBrush x:Key="CheckListBoxItemMouseOverBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFFFBEC" Offset="0.049" />
        <GradientStop Color="#FFFFF4D3" Offset="0.463" />
        <GradientStop Color="#FFFFECB5" Offset="0.544" />
    </LinearGradientBrush>
    <SolidColorBrush Color="#CCFFFFFF" x:Key="TransparentchecklistboxitemhoverInnerBorderBrush" />
    <SolidColorBrush x:Key="InnerBorderBrush" Color="#CCFFFFFF" />

    <!--CheckListBoxStyle-->
    <Style x:Key="TransparentCheckListBoxStyle" TargetType="{x:Type local:CheckListBox}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter  Property="BorderBrush" Value="{StaticResource CheckListBoxBorderBrush}" />
        <Setter Property="Background" Value="{StaticResource CheckListBoxBackgroundBrushKey}" />
        <Setter Property="Foreground" Value="{StaticResource CheckListBoxItemForeground}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Once" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBox}">
                    <Border Background="{TemplateBinding Background}"  SnapsToDevicePixels="True"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Border x:Name="InnerBorder" BorderBrush="{StaticResource InnerBorderBrush}" BorderThickness="1">
                            <ScrollViewer  Padding="{TemplateBinding Padding}"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="0,0,0,-1" />
                            </ScrollViewer>
                        </Border>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource TransparentCheckListBoxStyle}" TargetType="{x:Type local:CheckListBox}" />

    <!--CheckListBoxItemStyle-->
    <Style x:Key="TranaparentCheckListBoxItemStyle" TargetType="{x:Type local:CheckListBoxItem}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource CheckListBoxItemBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource CheckListBoxItemBorderBrush}" />
        <Setter  Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="local:CheckListBox.CheckBoxAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBoxItem}">
                    <Grid>
                        <Border Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="true">
                            <Border BorderThickness="1" x:Name="innerborder"  Padding="{TemplateBinding Padding}">
                                <Grid KeyboardNavigation.DirectionalNavigation="None">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Name="firstColumn" Width="Auto" />
                                        <ColumnDefinition Name="secondColumn" Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Focusable="False" Name="PART_CheckBox" IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource TemplatedParent}}"  VerticalAlignment="Center"  Margin="4" />
                                    <Border Name="contentBd" Grid.Column="1">
                                        <ContentPresenter Margin="3" ContentSource="Content">
                                            <ContentPresenter.Effect>
                                                <DropShadowEffect BlurRadius="0" ShadowDepth="1" Direction="270" Color="#03FFFFFF" Opacity="0.5" />
                                            </ContentPresenter.Effect>
                                        </ContentPresenter>
                                    </Border>
                                </Grid>
                            </Border>
                        </Border>
                        <Rectangle x:Name="BottomBorder" Fill="#7F000000" Height="1" Visibility="Collapsed" VerticalAlignment="Bottom" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger
                    Binding="{Binding Path=(local:CheckListBox.CheckBoxAlignment), RelativeSource={RelativeSource AncestorType={x:Type local:CheckListBox}}}" Value="Right">
                            <Setter TargetName="PART_CheckBox"
                                    Property="Grid.Column"
                                    Value="1" />
                            <Setter TargetName="contentBd" Property="Grid.Column"
                                    Value="0" />
                            <Setter TargetName="firstColumn" Property="Width"
                                    Value="*" />
                            <Setter TargetName="secondColumn" Property="Width"
                                    Value="Auto" />
                            <Setter TargetName="PART_CheckBox" Property="Margin"
                                    Value="5,1,1,1" />
                        </DataTrigger>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Padding" Value="12,6,12,6" />
                            <Setter Property="Margin" TargetName="contentBd" Value="5,0,0,0" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BottomBorder" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource CheckListBoxItemPressedBrushKey}" />
                            <Setter Property="BorderBrush"
                                    Value="{StaticResource CheckListBoxItemPressedBorderBrushKey}" />
                            <Setter Property="BorderBrush" TargetName="innerborder"
                                    Value="{StaticResource TransparentchecklistboxitemhoverInnerBorderBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="true" />
                                <Condition Property="shared:SkinStorage.EnableTouch" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource CheckListBoxItemBackground}" />
                            <Setter Property="BorderBrush" Value="{StaticResource CheckListBoxItemBorderBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true" />
                                <Condition Property="shared:SkinStorage.EnableTouch" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource CheckListBoxItemMouseOverBrushKey}" />
                            <Setter Property="BorderBrush"
                                    Value="{StaticResource CheckListBoxItemMouseOverBorderBrushKey}" />
                            <Setter Property="BorderBrush" TargetName="innerborder"
                                    Value="{StaticResource TransparentchecklistboxitemhoverInnerBorderBrush}" />
                        </MultiTrigger>

                        <Trigger Property="IsEnabled"
                                 Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource TranaparentCheckListBoxItemStyle}" TargetType="{x:Type local:CheckListBoxItem}" />
</ResourceDictionary>