<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:conv="clr-namespace:Syncfusion.Windows.Tools"
	xmlns:utilsOuter="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/syncfusion.Tools.WPF;component/Controls/FontComboBox/Themes/Brushes.xaml"/>
        <ResourceDictionary Source="/syncfusion.Shared.WPF;component/SkinManager/SkinManager.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <conv:PopupHeightToThumbOffsetConverter x:Key="PopupHeightToThumbOffsetConverter"/>


	
	<conv:FontToStringConverter x:Key="FontToStringConverter"/>
    
	

	<Style x:Key="ContentPresenterStyle" TargetType="{x:Type ContentPresenter}">
		<Setter Property="HorizontalAlignment" Value="Center" />
		<Setter Property="VerticalAlignment" Value="Center" />
	</Style>

    <Style x:Key="ToggleDropDownButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FrameworkElement.MinWidth" Value="0"/>
        <Setter Property="FrameworkElement.MinHeight" Value="0"/>
        <Setter Property="FrameworkElement.Width" Value="15"/>
        <Setter Property="FrameworkElement.Height" Value="Auto"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="{StaticResource Aero.Normalcolor.Background}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <utilsOuter:TrippleBorder Name="PART_Border" 
                                Background="{TemplateBinding Panel.Background}"
                                InsideBorderThickness="1"
                                BorderThickness="1"
                                OutsideBorderThickness="1"
                                InsideBorderBrush="{StaticResource Aero.Normalcolor.Background}"
                                BorderBrush="{StaticResource Aero.Normalcolor.Background}"
                                OutsideBorderBrush="{StaticResource Aero.Normalcolor.Background}">
                        <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              Margin="{TemplateBinding Control.Padding}"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                              ClipToBounds="True"/>
                    </utilsOuter:TrippleBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Blend">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="3" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="InsideBorderBrush" Value="{StaticResource Aero.Normalcolor.Highlight.DropDownButton}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource Aero.Normalcolor.Highlight.DropDownButton}" />
                            <Setter TargetName="PART_Border" Property="OutsideBorderBrush" Value="{StaticResource Aero.Normalcolor.Highlight.BorderOutside}" />
                        </Trigger>
                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Aero.Normalcolor.Highlight.DropDownButton}" />
                <Setter Property="BorderBrush" Value="{StaticResource Aero.Normalcolor.Highlight.DropDownButton}" />
            </Trigger>
         
        </Style.Triggers>
    </Style>

    <Style x:Key="Office2007ToggleDropDownButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FrameworkElement.MinWidth" Value="0"/>
        <Setter Property="FrameworkElement.MinHeight" Value="0"/>
        <Setter Property="FrameworkElement.Width" Value="15"/>
        <Setter Property="FrameworkElement.Height" Value="Auto"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="{StaticResource Office2007Blue.BackgroundBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <utilsOuter:TrippleBorder Name="PART_Border" 
                                Background="{TemplateBinding Panel.Background}"
                                InsideBorderThickness="1"
                                BorderThickness="1"
                                OutsideBorderThickness="1"
                                InsideBorderBrush="{StaticResource Office2007Blue.BackgroundBrush}"
                                BorderBrush="{StaticResource Office2007Blue.BackgroundBrush}"
                                OutsideBorderBrush="{StaticResource Office2007Blue.BackgroundBrush}">
                        <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              Margin="{TemplateBinding Control.Padding}"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                              ClipToBounds="True"/>
                    </utilsOuter:TrippleBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Black">
                            <Setter Property="InsideBorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Black.BackgroundBrush}"/>
                            <Setter Property="OutsideBorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Black.BackgroundBrush}"/>
                            <Setter Property="BorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Black.BackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Silver">
                            <Setter Property="InsideBorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Silver.BackgroundBrush}"/>
                            <Setter Property="OutsideBorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Silver.BackgroundBrush}"/>
                            <Setter Property="BorderBrush" TargetName="PART_Border"  Value="{StaticResource Office2007Silver.BackgroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="InsideBorderBrush" Value="{StaticResource Office2007.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource Office2007.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="OutsideBorderBrush" Value="{StaticResource Office2007.HighlightBorderBrush}" />
                        </Trigger>
                       
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Black">
                <Setter Property="Background"  Value="{StaticResource Office2007Black.BackgroundBrush}"/>
            </Trigger>
            <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Silver">
                <Setter Property="Background" Value="{StaticResource Office2007Silver.BackgroundBrush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Office2007.HighlightBackground}" />
                <Setter Property="BorderBrush" Value="{StaticResource Office2007.HighlightBorderBrush}" />
            </Trigger>
            
        </Style.Triggers>
    </Style>

    <Style x:Key="Office2003ToggleDropDownButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FrameworkElement.MinWidth" Value="0"/>
        <Setter Property="FrameworkElement.MinHeight" Value="0"/>
        <Setter Property="FrameworkElement.Width" Value="15"/>
        <Setter Property="FrameworkElement.Height" Value="Auto"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="{StaticResource Office2003.DropDownButton.Background}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <utilsOuter:TrippleBorder Name="PART_Border"
                                Background="{TemplateBinding Panel.Background}"
                                InsideBorderThickness="1"
                                BorderThickness="1"
                                OutsideBorderThickness="1"
                                InsideBorderBrush="{StaticResource Office2003.DropDownButton.Background}"
                                BorderBrush="{StaticResource Office2003.DropDownButton.BorderBrush}"
                                OutsideBorderBrush="{StaticResource Office2003.DropDownButton.BorderOutside}">
                        <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              Margin="{TemplateBinding Control.Padding}"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                              ClipToBounds="True"/>
                    </utilsOuter:TrippleBorder>
                    <ControlTemplate.Triggers>                       
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="InsideBorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBorder}" />
                            <Setter TargetName="PART_Border" Property="OutsideBorderBrush" Value="{StaticResource Office2003.DropDownButton.BorderOutside}" />
                        </Trigger>
                       
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Office2003.DropDownButton.HighlightBackground}" />
                <Setter Property="BorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBorder}" />
            </Trigger>
            
        </Style.Triggers>
    </Style>

    <Style x:Key="BlendToggleDropDownButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="1"/>
        <Setter Property="FrameworkElement.MinWidth" Value="0"/>
        <Setter Property="FrameworkElement.MinHeight" Value="0"/>
        <Setter Property="FrameworkElement.Width" Value="15"/>
        <Setter Property="FrameworkElement.Height" Value="Auto"/>
        <Setter Property="ClickMode" Value="Press"/>
        <Setter Property="Background" Value="{StaticResource Blend.DropDownButton.Background}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <utilsOuter:TrippleBorder Name="PART_Border" CornerRadius="3"
                                Background="{TemplateBinding Panel.Background}"
                                InsideBorderThickness="1"
                                BorderThickness="1"
                                OutsideBorderThickness="1"
                                InsideBorderBrush="{StaticResource Blend.DropDownButton.Background}"
                                BorderBrush="{StaticResource Blend.DropDownButton.Background}"
                                OutsideBorderBrush="{StaticResource Blend.DropDownButton.Background}">
                        <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              Margin="{TemplateBinding Control.Padding}"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                              ClipToBounds="True"/>
                    </utilsOuter:TrippleBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Blend">
                            <Setter TargetName="PART_Border" Property="CornerRadius" Value="3" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="InsideBorderBrush" Value="{StaticResource Blend.DropDownButton.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource Blend.DropDownButton.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="OutsideBorderBrush" Value="{StaticResource Blend.DropDownButton.BorderOutside}" />
                        </Trigger>
                       
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Blend.DropDownButton.HighlightBackground}" />
                <Setter Property="BorderBrush" Value="{StaticResource Blend.DropDownButton.HighlightBackground}" />
            </Trigger>
           
        </Style.Triggers>
    </Style>
    <Style x:Key="OrangeToggleDropDownButtonStyle"
           TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle"
                Value="true"/>
        <Setter Property="IsTabStop"
                Value="false"/>
        <Setter Property="Focusable"
                Value="false"/>
        <Setter Property="ClickMode"
                Value="Press"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">
                    <Border Name="Chrome" 
							SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto" SharedSizeGroup="ComboBoxButton"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Border Name="Chrome1" Grid.Column="0" Background="{StaticResource ToggleButtonChromeBrush1.Orange}">                                
                            </Border>
                            <Border Name="Chrome2" Margin="-1,0,0,0" 
                                    Grid.Column="1" Width="40"  Grid.Row="0" Background="{StaticResource ToggleButtonChromeBrush2.Orange}">                                
                            </Border>
                         
                        </Grid>
                    </Border>                   
                    <ControlTemplate.Triggers>                        
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome2" Property="Background" Value="{StaticResource DropDownButton.Highlight.Orange}"/>
                            <Setter TargetName="Chrome1" Property="Background" Value="{StaticResource TextBoxBackgroundHighlight.Orange}"/>
                        </Trigger>                        
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>        
    </Style>
    
  

    <!--FontListComboBox template-->
	<ControlTemplate x:Key="FontListComboBoxTemplate" TargetType="{x:Type local:FontListComboBox}" >
		<Border SnapsToDevicePixels="True" 
				BorderThickness="1" Name="Bd1"
				BorderBrush="{StaticResource Aero.FontListComboBoxBorderBrush}">
			<Grid SnapsToDevicePixels="True">
				<Border Name="Bd" 
                        BorderThickness="{TemplateBinding Border.BorderThickness}"
						BorderBrush="{Binding Path=BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                        Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
						TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}">
                    <Grid Grid.IsSharedSizeScope="True">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="1" />
							<ColumnDefinition Width="8*" />
							<ColumnDefinition SharedSizeGroup="ComboBoxButton" Width="*" />
						</Grid.ColumnDefinitions>
						<Border Margin="{TemplateBinding Control.Padding}"
                                Name="SelectedItemBorder"
                                Grid.ColumnSpan="2" />
						<ContentPresenter Name="textBox"
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter}, 
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          Grid.Column="1" />
                        <ToggleButton Name="toggleButton"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.Column="3"
                                      Style="{StaticResource ToggleDropDownButtonStyle}">
                            <Path Name="PART_Arrow"
                                Data="M0,0L3.5,3 7,0" 
                                Stroke="{StaticResource Black}" 
                                Fill="{StaticResource Black}"  
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Margin="0,0,0,0"/>
                        </ToggleButton>
					</Grid>
				</Border>
				<Popup Placement="Bottom"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
					<Border BorderThickness="1.5" Name="listBorder" BorderBrush="{StaticResource Aero.ListBoxBorderBrush}" CornerRadius="2" Background="{StaticResource Aero.Normalcolor.Background}" ClipToBounds="True">
						<DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White" 
                                   Height="{Binding ElementName=bt, Path=Offset}">
						<local:BottomThumb  Name="bt"  Height="10" DockPanel.Dock="Bottom" 
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter}, 
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"/>
              <!--Style="{utilsOuter:SkinObject ScrollViewerStyle}"-->
              <ScrollViewer >
						        <local:FontListBox x:Name="FontListBoxElement" DockPanel.Dock="Top"  
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                />
							</ScrollViewer>
						</DockPanel>
					</Border>
				</Popup>
			</Grid>
		</Border>
		<ControlTemplate.Triggers>	
           
			<Trigger Property="UIElement.IsEnabled" Value="False">
				<Setter Property="TextElement.Foreground">
					<Setter.Value>
						<DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
					</Setter.Value>
				</Setter>
				<Setter Property="Panel.Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
			</Trigger>
			<Trigger Property="IsDropDownOpen" Value="True">
				<Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
			</Trigger>
		</ControlTemplate.Triggers>
	</ControlTemplate>

    <ControlTemplate x:Key="Office2007FontListComboBoxTemplate" TargetType="{x:Type local:FontListComboBox}" >
        <Border SnapsToDevicePixels="True" 
				BorderThickness="1" Name="Bd1"
				BorderBrush="{StaticResource Office2007Blue.FontListComboBoxBorderBrush}">
            <Grid SnapsToDevicePixels="True">
                <Border Name="Bd" 
                        BorderThickness="{TemplateBinding Border.BorderThickness}"
						BorderBrush="{Binding Path=BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                        Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
						TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}">
                    <Grid Grid.IsSharedSizeScope="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1" />
                            <ColumnDefinition Width="8*" />
                            <ColumnDefinition SharedSizeGroup="ComboBoxButton" Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Margin="{TemplateBinding Control.Padding}"
                                Name="SelectedItemBorder"
                                Grid.ColumnSpan="2" />
                        <ContentPresenter Name="textBox"
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter}, 
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          Grid.Column="1" />
                        <ToggleButton Name="toggleButton"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.Column="3"
                                      Style="{StaticResource Office2007ToggleDropDownButtonStyle}">
                            <Path Name="PART_Arrow"
                                Data="M0,0L3.5,3 7,0" 
                                Stroke="{StaticResource Black}" 
                                Fill="{StaticResource Black}"  
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Margin="0,0,0,0"/>
                        </ToggleButton>
                    </Grid>
                </Border>
                <Popup Placement="Bottom"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
                    <Border BorderThickness="1.5" Name="listBorder" BorderBrush="{StaticResource Office2007Blue.ListBoxBorderBrush}" CornerRadius="2" Background="{StaticResource Office2007Blue.BackgroundBrush}" ClipToBounds="True">
                        <DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White" 
                                   Height="{Binding ElementName=bt, Path=Offset}">
                            <local:BottomThumb  Name="bt"  Height="10" DockPanel.Dock="Bottom" 
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter}, 
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"/>
                            <!--Style="{utilsOuter:SkinObject ScrollViewerStyle}"-->
                            <ScrollViewer >
                                <local:FontListBox x:Name="FontListBoxElement" DockPanel.Dock="Top"  
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                />
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Black">
                <Setter Property="BorderBrush" TargetName="Bd1" Value="{StaticResource Office2007Black.FontListComboBoxBorderBrush}"/>
                <Setter Property="BorderBrush" TargetName="listBorder" Value="{StaticResource Office2007Black.ListBoxBorderBrush}"/>
                <Setter Property="Background" TargetName="listBorder" Value="{StaticResource Office2007Black.BackgroundBrush}"/> 
                <Setter Property="Stroke" TargetName="PART_Arrow" Value="{StaticResource White}"/>
                <Setter Property="Fill" TargetName="PART_Arrow" Value="{StaticResource Black}"/>
            </Trigger>
            <Trigger Property="utilsOuter:SkinStorage.VisualStyle" Value="Office2007Silver">
                <Setter Property="BorderBrush" TargetName="Bd1"  Value="{StaticResource Office2007Silver.FontListComboBoxBorderBrush}"/>
                <Setter Property="BorderBrush" TargetName="listBorder" Value="{StaticResource Office2007Silver.ListBoxBorderBrush}"/>
                <Setter Property="Background" TargetName="listBorder" Value="{StaticResource Office2007Silver.BackgroundBrush}"/>
                <Setter Property="Stroke" TargetName="PART_Arrow" Value="{StaticResource Black}"/>
                <Setter Property="Fill" TargetName="PART_Arrow" Value="{StaticResource Black}"/>
            </Trigger>
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="TextElement.Foreground">
                    <Setter.Value>
                        <DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Panel.Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="Office2003FontListComboBoxTemplate" TargetType="{x:Type local:FontListComboBox}" >
        <Border SnapsToDevicePixels="True" 
				BorderThickness="1" Name="Bd1"
				BorderBrush="{StaticResource FontListComboBoxBorderBrush}">
            <Grid SnapsToDevicePixels="True">
                <Border Name="Bd" 
                        BorderThickness="{TemplateBinding Border.BorderThickness}"
						BorderBrush="{Binding Path=BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                        Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
						TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}">
                    <Grid Grid.IsSharedSizeScope="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1" />
                            <ColumnDefinition Width="8*" />
                            <ColumnDefinition SharedSizeGroup="ComboBoxButton" Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Margin="{TemplateBinding Control.Padding}"
                                Name="SelectedItemBorder"
                                Grid.ColumnSpan="2" />
                        <ContentPresenter Name="textBox"
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter}, 
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          Grid.Column="1" />
                        <ToggleButton Name="toggleButton"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.Column="3"
                                      Style="{StaticResource Office2003ToggleDropDownButtonStyle}">
                            <Path Name="PART_Arrow"
                                Data="M0,0L3.5,3 7,0" 
                                Stroke="{StaticResource Black}" 
                                Fill="Transparent"  
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Margin="0,0,0,0"/>
                        </ToggleButton>
                    </Grid>
                </Border>
                <Popup Placement="Bottom"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
                    <Border BorderThickness="1.5" Name="listBorder" BorderBrush="{StaticResource Office2003.ListBoxBorderBrush}" CornerRadius="2" Background="{StaticResource Office2003.BackgroundBrush}" ClipToBounds="True">
                        <DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White" 
                                   Height="{Binding ElementName=bt, Path=Offset}">
                            <local:BottomThumb  Name="bt"  Height="10" DockPanel.Dock="Bottom" 
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter}, 
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"/>
                            <!--Style="{utilsOuter:SkinObject ScrollViewerStyle}"-->
                            <ScrollViewer >
                                <local:FontListBox x:Name="FontListBoxElement" DockPanel.Dock="Top"  
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                />
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
           
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="TextElement.Foreground">
                    <Setter.Value>
                        <DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Panel.Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="BlendFontListComboBoxTemplate" TargetType="{x:Type local:FontListComboBox}" >
        <Border SnapsToDevicePixels="True" 
				BorderThickness="1" Name="Bd1"
				BorderBrush="{StaticResource Blend.FontListComboBoxBorderBrush}">
            <Grid SnapsToDevicePixels="True">
                <Border Name="Bd" 
                        BorderThickness="{TemplateBinding Border.BorderThickness}"
						BorderBrush="{Binding Path=BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                        Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
						TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}">
                    <Grid Grid.IsSharedSizeScope="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1" />
                            <ColumnDefinition Width="8*" />
                            <ColumnDefinition SharedSizeGroup="ComboBoxButton" Width="*" />
                        </Grid.ColumnDefinitions>
                        <Border Margin="{TemplateBinding Control.Padding}"
                                Name="SelectedItemBorder"
                                Grid.ColumnSpan="2" />
                        <ContentPresenter Name="textBox"
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter}, 
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          Grid.Column="1" />
                        <ToggleButton Name="toggleButton"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.Column="3"
                                      Style="{StaticResource BlendToggleDropDownButtonStyle}">
                            <Path Name="PART_Arrow"
                                Data="M0,0L3.5,3 7,0" 
                                Stroke="{StaticResource White}" 
                                Fill="Transparent"  
                                VerticalAlignment="Center" 
                                HorizontalAlignment="Center"
                                Margin="0,0,0,0"/>
                        </ToggleButton>
                    </Grid>
                </Border>
                <Popup Placement="Bottom"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
                    <Border BorderThickness="1.5" Name="listBorder" BorderBrush="{StaticResource Blend.ListBoxBorderBrush}" CornerRadius="2" Background="{StaticResource Blend.BackgroundBrush}" ClipToBounds="True">
                        <DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White" 
                                   Height="{Binding ElementName=bt, Path=Offset}">
                            <local:BottomThumb  Name="bt"  Height="10" DockPanel.Dock="Bottom" 
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter}, 
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"/>
                            <!--Style="{utilsOuter:SkinObject ScrollViewerStyle}"-->
                            <ScrollViewer >
                                <local:FontListBox x:Name="FontListBoxElement" DockPanel.Dock="Top"  
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                />
                            </ScrollViewer>
                        </DockPanel>
                    </Border>
                </Popup>
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
           
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="TextElement.Foreground">
                    <Setter.Value>
                        <DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Panel.Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <ControlTemplate x:Key="OrangeFontListComboBoxTemplate" TargetType="{x:Type local:FontListComboBox}" >        
        <Grid Name="MainGrid" 
                          SnapsToDevicePixels="true">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" 
                                              Width="0"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Popup Placement="Bottom" Grid.ColumnSpan="2"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay, 
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
                <Border BorderThickness="1.5" BorderBrush="{StaticResource ListBoxBorderBrush.Orange}" CornerRadius="2" Background="{StaticResource BackgroundBrush1.Orange}" ClipToBounds="True">
                    <DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White" 
                                   Height="{Binding ElementName=bt, Path=Offset}">
                        <local:BottomThumb  Name="bt"  Height="10" DockPanel.Dock="Bottom" 
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter}, 
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"/>
                       
                        <ScrollViewer >
                            <local:FontListBox x:Name="FontListBoxElement" BorderThickness="0,0,1,1" DockPanel.Dock="Top"  
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                />
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Popup>
            <ToggleButton Name="toggleButton" TextBlock.Foreground="{TemplateBinding Foreground}" 
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, 
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.ColumnSpan="2" 
                          Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Style="{StaticResource OrangeToggleDropDownButtonStyle}"/>
            
            

            <ContentPresenter Name="textBox" 
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter}, 
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          />
           
        </Grid>

        <ControlTemplate.Triggers>
           
            <Trigger Property="UIElement.IsEnabled" Value="False">
                <Setter Property="TextElement.Foreground">
                    <Setter.Value>
                        <DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
                    </Setter.Value>
                </Setter>
                <Setter Property="Panel.Background" TargetName="MainGrid" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
            </Trigger>
            <Trigger Property="IsDropDownOpen" Value="True">
                <Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


</ResourceDictionary>
