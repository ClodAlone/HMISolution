<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF">

    <!--Brushes-->
    <SolidColorBrush x:Key="CheckListBoxBorderBrush.Black" Color="#FF898989" />

    <SolidColorBrush x:Key="CheckListBoxBackground.Black" Color="#FFCED3DA" />

    <SolidColorBrush x:Key="CheckListBoxItemForeground.Black" Color="Black" />

    <SolidColorBrush x:Key="CheckListBoxItemBackground.Black" Color="Transparent" />

    <SolidColorBrush x:Key="CheckListBoxItemBorderBrush.Black" Color="Transparent" />

    <LinearGradientBrush x:Key="SelectedBrush.Black" StartPoint="0.5,1" EndPoint="0.5,0">
        <GradientStop Color="#FFDEB58C" Offset="0.005" />
        <GradientStop Color="#FFF6C99C" Offset="0.011" />
        <GradientStop Color="#FFFDD4AB" Offset="0.027" />
        <GradientStop Color="#FFFEB062" Offset="0.379" />
        <GradientStop Color="#FFFC9936" Offset="0.384" />
        <GradientStop Color="#FFF9E49F" Offset="0.886" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="SelectedBorderBrush.Black" Color="#FFDDCC9B" />

    <SolidColorBrush x:Key="SelectedForeground.Black" Color="Black" />

    <LinearGradientBrush x:Key="Office2007BlackCheckListBoxItemHoverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFCF8E4" Offset="0.131" />
        <GradientStop Color="#FFFFE9B0" Offset="0.468" />
        <GradientStop Color="#FFFED56A" Offset="0.487" />
        <GradientStop Color="#FFFEE49B" Offset="0.981" />
        <GradientStop Color="#FFFFF5B8" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="Office2007BlackCheckListBoxItemHoverBorderBrush" Color="#FFDDCC9B" />

    <SolidColorBrush x:Key="CheckListBoxItemHoverForeground.Blue" Color="Black" />

    <!--CheckListBoxStyle-->
    <Style x:Key="Office2007BlackCheckListBoxStyle" TargetType="{x:Type local:CheckListBox}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter  Property="BorderBrush" Value="{StaticResource CheckListBoxBorderBrush.Black}" />
        <Setter Property="Background" Value="{StaticResource CheckListBoxBackground.Black}" />
        <Setter Property="Foreground" Value="{StaticResource CheckListBoxItemForeground.Black}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Once" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBox}">
                    <Border Background="{TemplateBinding Background}"  SnapsToDevicePixels="True"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer  Padding="{TemplateBinding Padding}"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="0,0,0,-1" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007BlackCheckListBoxStyle}" TargetType="{x:Type local:CheckListBox}" />

    <!--CheckListBoxItemStyle-->
    <Style x:Key="Office2007BlackCheckListBoxItemStyle" TargetType="{x:Type local:CheckListBoxItem}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource CheckListBoxItemBackground.Black}" />
        <Setter Property="BorderBrush" Value="{StaticResource CheckListBoxItemBorderBrush.Black}" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="local:CheckListBox.CheckBoxAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBoxItem}">
                    <Grid>
                        <Border Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="true" CornerRadius="2">
                            <Grid KeyboardNavigation.DirectionalNavigation="None">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Name="firstColumn" Width="Auto" />
                                    <ColumnDefinition Name="secondColumn" Width="*" />
                                </Grid.ColumnDefinitions>
                                <CheckBox Focusable="False" Name="PART_CheckBox" IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource TemplatedParent}}"  VerticalAlignment="Center"  Margin="3,1,5,1" />
                                <Border Name="contentBd" Grid.Column="1">
                                    <ContentPresenter Margin="3" ContentSource="Content" />
                                </Border>
                            </Grid>
                        </Border>
                        <Rectangle x:Name="BottomBorder" Fill="#FF898989" Height="1" Visibility="Collapsed" VerticalAlignment="Bottom" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger
                    Binding="{Binding Path=(local:CheckListBox.CheckBoxAlignment), RelativeSource={RelativeSource AncestorType={x:Type local:CheckListBox}}}" Value="Right">
                            <Setter TargetName="PART_CheckBox"
                                    Property="Grid.Column"
                                    Value="1" />
                            <Setter TargetName="contentBd" Property="Grid.Column"
                                    Value="0" />
                            <Setter TargetName="firstColumn" Property="Width"
                                    Value="*" />
                            <Setter TargetName="secondColumn" Property="Width"
                                    Value="Auto" />
                            <Setter TargetName="PART_CheckBox" Property="Margin"
                                    Value="5,1,1,1" />
                        </DataTrigger>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Padding" Value="12,6,12,6" />
                            <Setter Property="Margin" TargetName="contentBd" Value="5,0,0,0" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BottomBorder" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource SelectedBrush.Black}" />
                            <Setter TargetName="Bd"
                                    Property="BorderBrush"
                                    Value="{StaticResource SelectedBorderBrush.Black}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource SelectedForeground.Black}" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="True" />
                                <Condition Property="IsSelected" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource CheckListBoxItemBackground.Black}" />
                            <Setter Property="BorderBrush" Value="{StaticResource CheckListBoxItemBorderBrush.Black}" />
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true" />
                                <Condition Property="shared:SkinStorage.EnableTouch" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource Office2007BlackCheckListBoxItemHoverBackground}" />
                            <Setter TargetName="Bd"
                                    Property="BorderBrush"
                                    Value="{StaticResource Office2007BlackCheckListBoxItemHoverBorderBrush}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource CheckListBoxItemHoverForeground.Blue}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled"
                                 Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007BlackCheckListBoxItemStyle}" TargetType="{x:Type local:CheckListBoxItem}" />
</ResourceDictionary>