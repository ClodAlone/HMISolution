<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF">

    <!--Brushes-->
    <SolidColorBrush x:Key="Office2010BlackCheckListBoxBorderBrush" Color="#FF6A6A6A" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxBackground" Color="#FFB3B3B3" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxItemForeground" Color="Black" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxForeground" Color="Black" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxItemBackground" Color="Transparent" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxItemBorderBrush" Color="Transparent" />

    <LinearGradientBrush x:Key="Office2010BlackSelectedBorderBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFC28A30" Offset="0" />
        <GradientStop Color="#FFC2A44D" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Office2010BlackSelectedBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFFE072" Offset="0.101" />
        <GradientStop Color="#FFFFD86C" Offset="0.449" />
        <GradientStop Color="#FFFFF480" Offset="0.96" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Office2010BlackSelectedForeground" Color="Black" />

    <LinearGradientBrush x:Key="Office2010BlackCheckListBoxItemMouseOverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFF8E9A8" Offset="0.044" />
        <GradientStop Color="#FFF8E188" Offset="0.504" />
        <GradientStop Color="#FFFBF9E0" Offset="0.978" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxItemMouseOverBorderBrush" Color="#FFECC757" />

    <SolidColorBrush x:Key="Office2010BlackCheckListBoxItemMouseOverForeground" Color="Black" />

    <!--CheckListBoxStyle-->
    <Style x:Key="Office2010BlackCheckListBoxStyle" TargetType="{x:Type local:CheckListBox}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter  Property="BorderBrush" Value="{StaticResource Office2010BlackCheckListBoxBorderBrush}" />
        <Setter Property="Background" Value="{StaticResource Office2010BlackCheckListBoxBackground}" />
        <Setter Property="Foreground" Value="{StaticResource Office2010BlackCheckListBoxForeground}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="KeyboardNavigation.TabNavigation" Value="Once" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBox}">
                    <Border Background="{TemplateBinding Background}"  SnapsToDevicePixels="True"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ScrollViewer  Padding="{TemplateBinding Padding}"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="0,0,0,-1" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2010BlackCheckListBoxStyle}" TargetType="{x:Type local:CheckListBox}" />

    <!--CheckListBoxItemStyle-->
    <Style x:Key="Office2010BlackCheckListBoxItemStyle" TargetType="{x:Type local:CheckListBoxItem}">
        <Setter  Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource Office2010BlackCheckListBoxItemBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Office2010BlackCheckListBoxItemBorderBrush}" />
        <Setter  Property="BorderThickness" Value="1" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="local:CheckListBox.CheckBoxAlignment" Value="Left" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CheckListBoxItem}">
                    <Grid>
                        <Border Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="true" CornerRadius="2">
                            <Border x:Name="innerBorder" Background="Transparent" BorderBrush="Transparent" BorderThickness="1" CornerRadius="2">
                                <Grid KeyboardNavigation.DirectionalNavigation="None">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Name="firstColumn" Width="Auto" />
                                        <ColumnDefinition Name="secondColumn" Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Focusable="False" Name="PART_CheckBox" IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource TemplatedParent}}"  VerticalAlignment="Center"  Margin="3,1,5,1" />
                                    <Border Name="contentBd" Grid.Column="1">
                                        <ContentPresenter Margin="3" ContentSource="Content" />
                                    </Border>
                                </Grid>
                            </Border>
                        </Border>
                        <Rectangle x:Name="BottomBorder" Fill="#FF6A6A6A" Height="1" Visibility="Collapsed" VerticalAlignment="Bottom" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger
                    Binding="{Binding Path=(local:CheckListBox.CheckBoxAlignment), RelativeSource={RelativeSource AncestorType={x:Type local:CheckListBox}}}" Value="Right">
                            <Setter TargetName="PART_CheckBox"
                                    Property="Grid.Column"
                                    Value="1" />
                            <Setter TargetName="contentBd" Property="Grid.Column"
                                    Value="0" />
                            <Setter TargetName="firstColumn" Property="Width"
                                    Value="*" />
                            <Setter TargetName="secondColumn" Property="Width"
                                    Value="Auto" />
                            <Setter TargetName="PART_CheckBox" Property="Margin"
                                    Value="5,1,1,1" />
                        </DataTrigger>
                        <Trigger Property="shared:SkinStorage.EnableTouch" Value="True">
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Padding" Value="12,6,12,6" />
                            <Setter Property="Margin" TargetName="contentBd" Value="5,0,0,0" />
                            <Setter Property="Visibility" Value="Visible" TargetName="BottomBorder" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bd"
                                    Property="BorderBrush"
                                    Value="{StaticResource Office2010BlackSelectedBorderBrush}" />
                            <Setter TargetName="innerBorder" Property="BorderBrush" Value="{StaticResource Office2010BlackSelectedBackground}" />
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource Office2010BlackSelectedBackground}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource Office2010BlackCheckListBoxItemMouseOverForeground}" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="true" />
                                <Condition Property="shared:SkinStorage.EnableTouch" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="{StaticResource Office2010BlackCheckListBoxItemBackground}" />
                            <Setter Property="BorderBrush" Value="{StaticResource Office2010BlackCheckListBoxItemBorderBrush}" />
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true" />
                                <Condition Property="shared:SkinStorage.EnableTouch" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Bd"
                                    Property="BorderBrush"
                                    Value="{StaticResource Office2010BlackCheckListBoxItemMouseOverBorderBrush}" />
                            <Setter TargetName="innerBorder" Property="BorderBrush" Value="White" />
                            <Setter TargetName="Bd"
                                    Property="Background"
                                    Value="{StaticResource Office2010BlackCheckListBoxItemMouseOverBackground}" />
                            <Setter Property="Foreground"
                                    Value="{StaticResource Office2010BlackCheckListBoxItemMouseOverForeground}" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled"
                                 Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2010BlackCheckListBoxItemStyle}" TargetType="{x:Type local:CheckListBoxItem}" />
</ResourceDictionary>