<UserControl x:Class="Syncfusion.Windows.Controls.Grid.SkinChooser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Grid"
    >
    <UserControl.Resources>
        <local:GridDataObjectToStringConverter x:Key="skinConv"/>

        <Style x:Key="ListBoxItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            <Setter Property="Padding" Value="2,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <ControlTemplate.Resources>
                            <Storyboard x:Key="Storyboard1">
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.2500000" Value="1.2"/>
                                </DoubleAnimationUsingKeyFrames>
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.2500000" Value="1.1"/>
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                            <Storyboard x:Key="Storyboard_Copy1">
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="1.1"/>
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.2500000" Value="0.9"/>
                                </DoubleAnimationUsingKeyFrames>
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleY)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="1.1"/>
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.2500000" Value="0.9"/>
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                            <Storyboard x:Key="OnLoaded1">
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.Opacity)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0"/>
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.9000000" Value="1"/>
                                </DoubleAnimationUsingKeyFrames>
                                <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="band" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[3].(TranslateTransform.Y)">
                                    <SplineDoubleKeyFrame KeyTime="00:00:00" Value="200"/>
                                    <SplineDoubleKeyFrame KeyTime="00:00:00.9000000" Value="0"/>
                                </DoubleAnimationUsingKeyFrames>
                            </Storyboard>
                        </ControlTemplate.Resources>
                        <Border x:Name="band" SnapsToDevicePixels="true" Height="30" Width="Auto" Background="Transparent">
                            <Border.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </Border.RenderTransform>
                            <Rectangle x:Name="rect1" Fill="{Binding Path=Background}" Stroke="Gray" HorizontalAlignment="Left" Margin="10,0,4,0" Width="80" Height="Auto"/>
                            <Border.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded" SourceName="band">
                                    <BeginStoryboard Storyboard="{StaticResource OnLoaded1}"/>
                                </EventTrigger>
                            </Border.Triggers>
                            <Border.ToolTip>
                                <Border CornerRadius="5" x:Name="borderPreviewSkin" BorderBrush="Transparent" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock HorizontalAlignment="Center" Text="{Binding Path=Name}" FontSize="16" FontWeight="Bold" Foreground="Black"/>
                                    </StackPanel>
                                </Border>
                            </Border.ToolTip>
                        </Border>                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Trigger.ExitActions>
                                    <BeginStoryboard Storyboard="{StaticResource Storyboard_Copy1}" x:Name="Storyboard_Copy1_BeginStoryboard"/>
                                </Trigger.ExitActions>
                                <Trigger.EnterActions>
                                    <BeginStoryboard Storyboard="{StaticResource Storyboard1}"/>
                                </Trigger.EnterActions>
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" TargetName="band" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>
                                <Trigger.ExitActions>
                                    <BeginStoryboard Storyboard="{StaticResource Storyboard_Copy1}"/>
                                </Trigger.ExitActions>
                                <Trigger.EnterActions>
                                    <BeginStoryboard Storyboard="{StaticResource Storyboard1}"/>
                                </Trigger.EnterActions>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="band" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"/>
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                            </MultiTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <local:SkinSelector x:Name="lstSkins" Background="Transparent" BorderBrush="Transparent"  ItemsSource="{Binding SkinCollection}" DisplayMemberPath="Name" ItemContainerStyle="{StaticResource ListBoxItemStyle}" SelectedItem="{Binding RelativeSource={RelativeSource
FindAncestor, AncestorType={x:Type local:SkinChooser}}, Path=Skin, Converter={StaticResource skinConv}}"  >
            <local:SkinSelector.ItemsPanel>
                <ItemsPanelTemplate>
                    <local:SkinSelectorPanel />
                </ItemsPanelTemplate>
            </local:SkinSelector.ItemsPanel>
        </local:SkinSelector>
        <Border Grid.Row="1" CornerRadius="10" BorderThickness="1" BorderBrush="Gray">
            <ComboBox Margin="8" SelectedIndex="1" SelectedItem="{Binding RelativeSource={RelativeSource
FindAncestor, AncestorType={x:Type local:SkinChooser}}, Path=SkinBand, Converter={StaticResource skinConv}}">
                <ComboBoxItem>Office14-2003</ComboBoxItem>
                <ComboBoxItem>Office2007</ComboBoxItem>
                <ComboBoxItem>ShineyBureau</ComboBoxItem>
                <ComboBoxItem>GlassyDefault</ComboBoxItem>
            </ComboBox>
        </Border>
    </Grid>
</UserControl>
