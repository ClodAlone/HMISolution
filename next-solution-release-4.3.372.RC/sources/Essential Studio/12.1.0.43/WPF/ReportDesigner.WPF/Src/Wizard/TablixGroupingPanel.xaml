<UserControl x:Class="Syncfusion.Windows.Reports.Designer.Dialogs.TablixGroupingPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:prop="clr-namespace:Syncfusion.Windows.Reports.Designer.Properties"
             MinWidth="250">
    <UserControl.Resources>
        <ContextMenu x:Key="GroupContextMenu">
            <MenuItem Header="Add Group">
                <MenuItem Header="{x:Static prop:Resources.headerParentGroup}" Click="AddParentGroup_Click"/>
                <MenuItem Header="{x:Static prop:Resources.headerChildGroup}" Click="AddChildGroup_Click"/>
                <MenuItem Header="Adjacent Before" Visibility="Collapsed"/>
                <MenuItem Header="Adjacent After" Visibility="Collapsed"/>
            </MenuItem>
            <MenuItem Header="Add Total" IsEnabled="False">
                <MenuItem Header="Before" Click="AddBeforeTotalMember_Click"/>
                <MenuItem Header="After" Click="AddAfterTotalMember_Click"/>
            </MenuItem>
            <MenuItem Header="Delete Group" Click="DeleteGroup_Click" Visibility="Collapsed"/>
            <MenuItem Header="Group Properties.." Click="GroupProperties_Click" Visibility="Collapsed"/>
        </ContextMenu>
        <Style x:Key="TreeViewItemFocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
            <Setter Property="Visibility" Value="Collapsed"/>
        </Style>
        <Style TargetType="{x:Type TreeViewItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TreeViewItem}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="1"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <ToggleButton x:Name="Expander" Style="{StaticResource ExpandCollapseToggleStyle}" />
                            <StackPanel x:Name="BgColor" Background="{TemplateBinding Background}"  Grid.Column="1" Grid.ColumnSpan="2" >
                                <Border x:Name="BgBorder" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="2,2,2,2">
                                    <Border x:Name="Bd" SnapsToDevicePixels="true" >
                                        <Grid>
                                            <StackPanel x:Name="MenuButton" HorizontalAlignment="Right" IsEnabled="False" VerticalAlignment="Center">
                                                <ToggleButton Width="15" Height="15" BorderThickness="0" Background="Transparent">
                                                    <Path Data="M 0 0 L 3.5 4 L 7 0 Z" Fill="Black"></Path>
                                                </ToggleButton>
                                            </StackPanel>
                                            <ContentPresenter x:Name="PART_Header" ContentSource="Header" Margin="10,0,0,0"/>
                                        </Grid>
                                    </Border>
                                </Border>
                            </StackPanel>
                            <ItemsPresenter x:Name="ItemsHost" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="true">
                                <Setter Property="Visibility" TargetName="ItemsHost" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="HasItems" Value="false">
                                <Setter Property="Visibility" TargetName="Expander" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Header" Value="static">
                                <Setter Property="Background" TargetName="BgColor" Value="LightGray"/>
                                <Setter Property="BorderBrush" TargetName="BgBorder" Value="DimGray"/>
                                <Setter Property="Visibility" TargetName="MenuButton" Value="Collapsed"/>
                            </Trigger>
                            <Trigger Property="Header" Value="(static)">
                                <Setter Property="Background" TargetName="BgColor" Value="LightGray"/>
                                <Setter Property="BorderBrush" TargetName="BgBorder" Value="DimGray"/>
                                <Setter Property="Visibility" TargetName="MenuButton" Value="Collapsed"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsSelectionActive" Value="true"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" TargetName="BgColor" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="grd_Panel" >
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Height="20" Width="auto">
            <Label Name="lbl_rowgroup" Content="{x:Static prop:Resources.labelRowGroup}" />
        </StackPanel>
        <StackPanel Grid.Column="2" Height="25" Orientation="Horizontal">
            <Label Name="lbl_colgroup" Content="{x:Static prop:Resources.labelColumnGroup}" />
        </StackPanel>
        <StackPanel Name="stpnl_AdvanceMode" Grid.Column="2" Background="Transparent" Orientation="Horizontal" Margin="0,0,5,0" HorizontalAlignment="Right" IsEnabled="False">
            <ToggleButton Name="popupButton" Height="15" Width="15" BorderBrush="Transparent" Click="popupButton_Click" >
                <Path Data="M 0 0 L 3.5 4 L 7 0 Z" Fill="Black"></Path>
            </ToggleButton>
            <Popup Name="popupContainer" Placement="Bottom" StaysOpen="False">
                <Button Name="btn_AdvanceMode" Click="btn_AdvanceMode_Click">
                    <StackPanel>
                        <CheckBox Name="chk_advEnable" Content="{x:Static prop:Resources.labelAdvancedMode}"  IsHitTestVisible="False"/>
                    </StackPanel>
                </Button>
            </Popup>
        </StackPanel>

        <TreeView Grid.Row="1" Background="White" Name="trv_RowMembersItems" AllowDrop="True"
                  ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                  SelectedItemChanged="SelectedItemChanged" PreviewMouseRightButtonDown="Tree_PreviewMouseRightButtonDown" 
                  PreviewDrop="TreeItems_PreviewDrop" DragOver="TreeItems_PreviewDragOver" DragEnter="TreeItems_PreviewDragEnter">
        </TreeView>
        <TreeView Grid.Row="1" Grid.Column="2"  Background="White" Name="trv_ColumnMembersItems" AllowDrop="True" 
                  ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto" 
                  SelectedItemChanged="SelectedItemChanged" PreviewMouseRightButtonDown="Tree_PreviewMouseRightButtonDown" 
                  PreviewDrop="TreeItems_PreviewDrop" DragOver="TreeItems_PreviewDragOver" DragEnter="TreeItems_PreviewDragEnter">
        </TreeView>
    </Grid>
</UserControl>
