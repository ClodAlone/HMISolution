<!--Defines the following:
    1) ChartSeries Style
    2) Default ChartSeries Segment Template
    -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Chart">
   
    <local:ColorModelToPaletteConverter x:Key="colorConverter"/>
    
    <Style TargetType="{x:Type local:ChartSeries}">
        <Setter Property="LegendIcon" Value="Rectangle" />
         <Setter Property="Interior">
            <Setter.Value>
                <MultiBinding Converter="{StaticResource colorConverter}">
                    <Binding Path="Area.ColorModel.CurrentPalette" RelativeSource="{RelativeSource self}"/>
                    <Binding RelativeSource="{RelativeSource self}"/>
                </MultiBinding>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Series Template -->
    <DataTemplate x:Key="ChartSeriesTemplate">
        <local:ChartSeriesPresenter Series="{Binding Mode=OneTime}" XAxis="{Binding XAxis}" YAxis="{Binding YAxis}"/>
    </DataTemplate>

       
</ResourceDictionary>
