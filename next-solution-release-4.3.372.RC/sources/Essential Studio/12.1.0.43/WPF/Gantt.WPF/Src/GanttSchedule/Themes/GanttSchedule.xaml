<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"                    
                    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
                    xmlns:schedule="clr-namespace:Syncfusion.Windows.Controls.Gantt.Schedule">

    <!--Gantt Schedule Styles-->

    <Style TargetType="{x:Type schedule:GanttSchedule}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type schedule:GanttSchedule}">
                    <Border SnapsToDevicePixels="True" 
                            ClipToBounds="True" 
                            Background="{TemplateBinding Background}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            BorderBrush="{TemplateBinding BorderBrush}">
                        <ItemsPresenter Margin="0" Width="{TemplateBinding Width}" x:Name="PART_ItemsPresenter" HorizontalAlignment="Left" VerticalAlignment="Top" Height="Auto"/>
                    </Border>                   
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ClipToBounds" Value="True"/>
    </Style>

    <Style TargetType="{x:Type schedule:GanttScheduleRow}">
        <Setter Property="MinHeight" Value="20"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type schedule:GanttScheduleRow}">
                    <schedule:GanttScheduleRowPanel x:Name="PART_RowItemsPresenter" MinHeight="{TemplateBinding MinHeight}" Width="Auto" HorizontalAlignment="Left"
                                SnapsToDevicePixels="True" Background="{TemplateBinding Background}">
                    </schedule:GanttScheduleRowPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type schedule:GanttScheduleCell}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type schedule:GanttScheduleCell}">
                    <Border BorderThickness="1,1,0,0"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type schedule:GanttSchedule}},Path=BorderBrush}" 
                            SnapsToDevicePixels="True" >
                       <ToolTipService.ToolTip>
                            <ToolTip Content="{TemplateBinding CellToolTip}"/>
                        </ToolTipService.ToolTip>
                           <ContentPresenter  Margin="1,0,1,0"
                                              SnapsToDevicePixels="True"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                             />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
   
</ResourceDictionary>