<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Grid">

    <local:BoolToVisiblityConverter x:Key="boolToVisibilityConverter"/>
    
    <SolidColorBrush x:Key="OuterBorderBrush"
                     Opacity="0.75"
                     Color="#E9FDFD" />
    <SolidColorBrush x:Key="InnerBorderBresh" Color="#868686" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="#FFFFFFFF" />
    

    <!--  ResizeDecorator Template  -->
    <Style TargetType="{x:Type Shape}" x:Key="ThumbCorner">
        <Setter Property="SnapsToDevicePixels" Value="true" />
        <Setter Property="Stroke" Value="#FFC8C8C8" />
        <Setter Property="StrokeThickness" Value="1" />
        <Setter Property="Width" Value="9" />
        <Setter Property="Height" Value="9" />
        <Setter Property="Margin" Value="-2" />
        <Setter Property="Fill">
            <Setter.Value>
                <RadialGradientBrush Center="0.2, 0.2" GradientOrigin="0.2, 0.2" RadiusX="0.8" RadiusY="0.8">
                    <GradientStop Color="White" Offset="0.8" />
                </RadialGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="DecoratorTemplate">
    <Grid>
        <Grid Opacity="0" Margin="-3">
            <local:MoveThumb Cursor="SizeAll" Margin="1"
                Height="3"
                VerticalAlignment="Top" 
                HorizontalAlignment="Stretch"/>
            <local:MoveThumb Cursor="SizeAll" Margin="1"
                Height="3"
                VerticalAlignment="Bottom" 
                HorizontalAlignment="Stretch"/>
            <local:MoveThumb Cursor="SizeAll" Margin="1"
                Width="3"
                VerticalAlignment="Stretch" 
                HorizontalAlignment="Right"/>
            <local:MoveThumb Cursor="SizeAll" Margin="1"
                Width="3"
                VerticalAlignment="Stretch" 
                HorizontalAlignment="Left"/>
                            
            <local:ResizeThumb Height="5"
                Cursor="SizeNS"
                VerticalAlignment="Top"
                HorizontalAlignment="Center"/>
            <local:ResizeThumb Width="5"
                Cursor="SizeWE"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"/>
            <local:ResizeThumb Width="5"
                Cursor="SizeWE"
                VerticalAlignment="Center"
                HorizontalAlignment="Right"/>
            <local:ResizeThumb Height="5"
                Cursor="SizeNS"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Center"/>
            <local:ResizeThumb Width="7"
                Height="7"
                Margin="-2"
                Cursor="SizeNWSE"
                VerticalAlignment="Top"
                HorizontalAlignment="Left"/>
            <local:ResizeThumb Width="7"
                Height="7"
                Margin="-2"
                Cursor="SizeNESW"
                VerticalAlignment="Top"
                HorizontalAlignment="Right"/>
            <local:ResizeThumb Width="7"
                Height="7"
                Margin="-2"
                Cursor="SizeNESW"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Left"/>
            <local:ResizeThumb Width="7"
                Height="7"
                Margin="-2"
                Cursor="SizeNWSE"
                VerticalAlignment="Bottom"
                HorizontalAlignment="Right"/>
                            

        </Grid>
        <Grid IsHitTestVisible="False" Opacity="1" Margin="-1.5">
            <Rectangle SnapsToDevicePixels="True"
            StrokeThickness="1.2"
            Margin="1"
            Stroke="Gray"/>
            <Ellipse Style="{StaticResource ThumbCorner}" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <Ellipse Style="{StaticResource ThumbCorner}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
            <Ellipse Style="{StaticResource ThumbCorner}" HorizontalAlignment="Left" VerticalAlignment="Bottom"/>
            <Ellipse Style="{StaticResource ThumbCorner}" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
            <Rectangle Style="{StaticResource ThumbCorner}" HorizontalAlignment="Center" VerticalAlignment="Top"/>
            <Rectangle Style="{StaticResource ThumbCorner}" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
            <Rectangle Style="{StaticResource ThumbCorner}" HorizontalAlignment="Left" VerticalAlignment="Center"/>
            <Rectangle Style="{StaticResource ThumbCorner}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</ControlTemplate>

    <Style x:Key="GraphicCellStyle" TargetType="local:GraphicCellControl">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:GraphicCellControl">
                    <Grid DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                 >
                            <ContentPresenter Content="{TemplateBinding Content}"/>
                        </Border>
                        <Control x:Name="Decorator" Template="{StaticResource DecoratorTemplate}" 
                                 Visibility="{Binding Path=IsSelected, Converter={StaticResource boolToVisibilityConverter}}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource GraphicCellStyle}" TargetType="local:GraphicCellControl" />
</ResourceDictionary>