<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Grid"
                    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Shared.WPF;component/SkinManager/DefaultStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="Windows7ComboBoxBackground" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="Windows7ComboBoxBorderBrush" Color="#FFCFCFCF" />
    <SolidColorBrush x:Key="PathFill" Color="#FF323232" />
    
    <LinearGradientBrush x:Key="Windows7ComboBoxMouseOverToggleButtonBackground" StartPoint="0.781000018119812,0.0140000004321337" EndPoint="0.778999984264374,0.986999988555908">
        <GradientStop Offset="0.051" Color="#FFEAF6FD" />
        <GradientStop Offset="0.503" Color="#FFD9F0FC" />
        <GradientStop Offset="0.508" Color="#FFBEE6FD" />
        <GradientStop Offset="0.898" Color="#FFACDCF7" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Windows7ComboBoxToggleButtonMouseOverBackground" StartPoint="0.781000018119812,0.0140000004321337" EndPoint="0.778999984264374,0.986999988555908">
        <GradientStop Offset="0.051" Color="#FFEAF6FD" />
        <GradientStop Offset="0.503" Color="#FFD9F0FC" />
        <GradientStop Offset="0.508" Color="#FFBEE6FD" />
        <GradientStop Offset="0.898" Color="#FFACDCF7" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Windows7ComboBoxMouseOverToggleButtonBorderBrush" Color="#FF3D7BAC" />

    <SolidColorBrush x:Key="Windows7ComboBoxToggleButtonMouseOverBorderBrush" Color="#FF3D7BAC" />
    <SolidColorBrush x:Key="Windows7ComboBoxMouseOverBorderBrush" Color="#FF3D7BAD" />
    <SolidColorBrush x:Key="Windows7ComboBoxMouseOverBackground" Color="#FFFFFFFF" />


    <SolidColorBrush x:Key="Windows7ComboBoxMouseOverPathFill" Color="#FF323232" />


    <LinearGradientBrush x:Key="Windows7ComboBoxToggleButtonCheckedBackground" StartPoint="0.814000010490417,0.0410000011324883" EndPoint="0.820999979972839,0.903999984264374">
        <GradientStop Offset="0.033" Color="#FFA9C6D6" />
        <GradientStop Offset="0.038" Color="#FFB9D9EC" />
        <GradientStop Offset="0.414" Color="#FFC1E3F6" />
        <GradientStop Offset="0.42" Color="#FF97D1F0" />
    </LinearGradientBrush>


    <SolidColorBrush x:Key="Windows7ComboBoxToggleButtonCheckedBorderBrush" Color="#FF3D7BAC" />

    <SolidColorBrush x:Key="Windows7ComboBoxForeground" Color="Black" />

    <SolidColorBrush x:Key="Windows7ComboBoxCheckedPathFill" Color="#FF323232" />

    <SolidColorBrush x:Key="Windows7ComboBoxItemBorderBrush" Color="#FF3D7BAD" />

    <SolidColorBrush x:Key="Windows7ComboBoxItemBackground" Color="#FFFFFFFF" />

    <SolidColorBrush x:Key="Windows7ComboBoxItemForeground" Color="#FF000000" />

    <LinearGradientBrush x:Key="Windows7ComboBoxItemMouseOverBackground" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Offset="0" Color="#FFFAFBFD" />
        <GradientStop Offset="0.798" Color="#FFEDF4FD" />
        <GradientStop Offset="1" Color="#FFF0F7FE" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Windows7ComboBoxItemMouseOverBorderBrush" Color="#FFD0E3FA" />

    <SolidColorBrush x:Key="Windows7ListBoxBackground" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="Windows7ListBoxForeground" Color="#FF000000" />
    <SolidColorBrush x:Key="Windows7ListBoxBorderBrush" Color="#FFE2E2E2" />

    <Style x:Key="Windows7HoverListBoxStyle" TargetType="{x:Type local:HoverListBox}">
        <Setter Property="Background" Value="{StaticResource Windows7ListBoxBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Windows7ListBoxBorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Windows7ListBoxForeground}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HoverListBox}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="1"
                            SnapsToDevicePixels="true">
                        <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter x:Name="PART_ItemsPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Windows7HoverListBoxStyle}" TargetType="{x:Type local:HoverListBox}" />

    <Style x:Key="Windows7HoverListBoxItemStyle" TargetType="{x:Type local:HoverListBoxItem}">
        <Setter Property="Padding" Value="3,0,3,0" />
        <Setter Property="Background" Value="{StaticResource Windows7ListBoxBackground}" />
        <Setter Property="Foreground" Value="{StaticResource Windows7ListBoxForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HoverListBoxItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="true">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource Windows7ComboBoxToggleButtonMouseOverBackground}" />
                            <!--  <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>  -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource Windows7HoverListBoxItemStyle}" TargetType="{x:Type local:HoverListBoxItem}" />
    
    <Style x:Key="Windows7ComboBoxReadonlyToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource Windows7ComboBoxBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Windows7ComboBoxBorderBrush}" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">

                    <Border Name="Chrome1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="ComboBoxButton" />
                            </Grid.ColumnDefinitions>

                            <Border Name="Chrome"
                                    Grid.Column="1"
                                    Width="16"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0">
                                <Path Name="Arrow"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="{StaticResource DownArrowGeometry}"
                                      Fill="{StaticResource PathFill}" />
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxToggleButtonCheckedBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderThickness" Value="1" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxToggleButtonCheckedBorderBrush}" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Windows7ComboBoxToggleButtonCheckedBackground}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource Windows7ComboBoxCheckedPathFill}" />
                        </Trigger>

                        <Trigger SourceName="Chrome1" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome1" Property="BorderThickness" Value="1" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Windows7ComboBoxMouseOverToggleButtonBackground}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxMouseOverToggleButtonBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxMouseOverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="Background" Value="{StaticResource Windows7ComboBoxMouseOverBackground}" />
                        </Trigger>

                        <Trigger SourceName="Chrome" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxToggleButtonMouseOverBorderBrush}" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Windows7ComboBoxToggleButtonMouseOverBackground}" />
                            <Setter TargetName="Arrow" Property="Fill" Value="{StaticResource Windows7ComboBoxMouseOverPathFill}" />
                        </Trigger>

                        <Trigger SourceName="Chrome1" Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Chrome1" Property="BorderThickness" Value="1" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource Windows7ComboBoxToggleButtonMouseOverBackground}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxToggleButtonMouseOverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource Windows7ComboBoxMouseOverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="Background" Value="{StaticResource Windows7ComboBoxMouseOverBackground}" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="Windows7GridCellDropDownControlBaseStyle" TargetType="{x:Type local:GridCellDropDownControlBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GridCellDropDownControlBase}">
                    <Border BorderThickness="0" Padding="0">
                        <Grid Name="MainGrid" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="0" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                            </Grid.ColumnDefinitions>
                            <TextBox Name="PART_Textbox"
                                     Grid.Column="0"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Stretch"
                                     VerticalContentAlignment="{TemplateBinding VerticalAlignment}"
                                     HorizontalContentAlignment="{TemplateBinding HorizontalAlignment}"
                                     Background="{TemplateBinding Background}"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     IsReadOnly="{Binding Path=IsReadOnly,
                                                          Mode=TwoWay,
                                                          RelativeSource={RelativeSource TemplatedParent}}"
                                     Text="{Binding Path=Text,
                                                    Mode=TwoWay,
                                                    RelativeSource={RelativeSource TemplatedParent}}" />
                            <ToggleButton Name="PART_Togg"
                                          Grid.Column="1"
                                          HorizontalAlignment="Right"
                                          IsChecked="{Binding Path=IsDropDownOpen,
                                                              Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{StaticResource Windows7ComboBoxReadonlyToggleButton}" />
                            <!--
                                BorderBrush="{TemplateBinding Border.BorderBrush}"
                                Background="{x:Static SystemColors.ControlBrush}"
                            -->
                            <Popup Name="PART_Popup"
                                   Grid.ColumnSpan="2"
                                   Margin="1"
                                   AllowsTransparency="true"
                                   IsOpen="{Binding Path=IsDropDownOpen,
                                                    Mode=TwoWay,
                                                    RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                                   StaysOpen="False">
                                <theme:SystemDropShadowChrome Name="Shdw"
                                                              MinWidth="{Binding ElementName=MainGrid,
                                                                                 Path=ActualWidth}"
                                                              MaxHeight="{x:Static local:GridCellDropDownControlBase.MaxDropDownHeight}"
                                                              Color="Transparent">
                                    <Border x:Name="DropDownBorder"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1">
                                        <ContentControl Name="PART_Content" />
                                    </Border>
                                </theme:SystemDropShadowChrome>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                        <Trigger SourceName="PART_Popup" Property="Popup.HasDropShadow" Value="true">
                            <Setter TargetName="Shdw" Property="Margin" Value="0,0,5,5" />
                            <Setter TargetName="Shdw" Property="SnapsToDevicePixels" Value="true" />
                            <Setter TargetName="Shdw" Property="Color" Value="#71000000" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="true">
                            <Setter Property="Padding" Value="2" />
                            <Setter Property="IsTabStop" Value="false" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Windows7GridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellDropDownControlBase}" />

    <Style BasedOn="{StaticResource Windows7GridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellComboBoxDropDown}" />

    <Style BasedOn="{StaticResource Windows7GridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellGridListControlDropDown}" />

</ResourceDictionary>