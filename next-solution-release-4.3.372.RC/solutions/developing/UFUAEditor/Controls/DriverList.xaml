<UserControl x:Class="UFUAEditor.Controls.DriverList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:UFUAEditor.Properties"
             xmlns:controls="clr-namespace:UFUAEditor.Controls"
             xmlns:converters="clr-namespace:WPFUtilities.Converters;assembly=WPFUtilities"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             mc:Ignorable="d">
    <UserControl.Resources>
      <ResourceDictionary>
        <converters:NullVisibilityConverter x:Key="NullVisibilityConverter" />
      </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <controls:DriverListControl x:Name="gridDataControl" ShowSystemCompatibility="True"
                                    SelectedItemChanged="gridDataControl_SelectedItemChanged"
                                    dx:DXSerializer.SerializationID="gridDataControl"/>
  
        <TextBlock Text="{x:Static properties:Resources.EmptyDriverList}" 
                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                   FontSize="18"  FontWeight="Bold" Opacity="0.3"
                   Visibility="{Binding Path=ItemsSource, ElementName=gridDataControl, Converter={StaticResource NullVisibilityConverter}, Mode=OneWay}">
           <!--
            <TextBlock.LayoutTransform>
                <RotateTransform Angle="45" />
            </TextBlock.LayoutTransform>
           -->
        </TextBlock>
    </Grid>
</UserControl>
