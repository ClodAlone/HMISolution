<!-- DXPage can be used on its own or navigated to. -->
<Layout:DXPage
    x:Class="NextAR.View.QRReaderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Layout="using:DevExpress.UI.Xaml.Layout"
    xmlns:local="using:NextAR.View"
    mc:Ignorable="d">
    <Page.DataContext>
        <local:QRReaderViewModel/>
    </Page.DataContext>

    <!-- The PageAdornerControl displays the back button and a page title.  -->
    <Layout:PageAdornerControl
            Background="{StaticResource ApplicationPageBackgroundThemeBrush}"
            Header="{Binding DataSource.Name}">
        <Grid>
            <TextBlock Text="Move the camera to a QR code to scan" VerticalAlignment="Top" FontSize="32" Width="640" TextAlignment="Left"  />
            <Canvas Width="640" Height="360">
                <CaptureElement x:Name="VideoCapture" Width="640" Height="360" />
                <Image x:Name="CaptureImage" Width="640" Height="360" Visibility="Collapsed" />
            </Canvas>
            <TextBlock x:Name="Error" VerticalAlignment="Bottom" FontSize="32" Width="640" TextAlignment="Center" Margin="363,0,363,37" />
            
            <Button x:Name="ScanResult" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="32" Click="ScanResult_Click" Visibility="Collapsed"/>
        </Grid>
    </Layout:PageAdornerControl>
</Layout:DXPage>
