<UserControl x:Class="StreamMediaElement.StreamMediaElementUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="control"
             d:DesignHeight="350" d:DesignWidth="400"
             xmlns:ffme="clr-namespace:Unosquare.FFME;assembly=ffme"
             xmlns:local="clr-namespace:StreamMediaElement"
             xmlns:properties="clr-namespace:StreamMediaElement.Properties">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary>
                    <local:TimeSpanToSecondsConverter x:Key="TimeSpanToSecondsConverter" />
                    <local:TimeSpanFormatter x:Key="TimeSpanFormatter" />
                    <local:PercentageFormatter x:Key="PercentageFormatter" />
                </ResourceDictionary>
                <ResourceDictionary Source="Icons.xaml" />
                <ResourceDictionary Source="Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border x:Name="mainBorder" ClipToBounds="False"
                            BorderBrush="{Binding BorderBrush}"
                            BorderThickness="{Binding BorderThickness}">
        <Grid Name="Player">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <ffme:MediaElement Name="Media" Background="Black" Grid.Row="0" Grid.Column="0"></ffme:MediaElement>
            <DockPanel LastChildFill="True" Grid.Row="1">
                <!--<Button Margin="4,0,0,0" Visibility="{Binding ElementName=control, Path=PlayButtonVisibility}" DockPanel.Dock="Left" HorizontalAlignment="Left" Style="{DynamicResource ModernButtonStyle}" Command="{Binding ElementName=control, Path=OpenCommand}">
                    <Path Stretch="Uniform" Data="{Binding Source={StaticResource PlayIcon}, Path=Data}" Fill="{Binding Path=Foreground, ElementName=control}" />
                </Button>-->
                <!--<Button DockPanel.Dock="Left" HorizontalAlignment="Left" Style="{DynamicResource ModernButtonStyle}" Command="{Binding ElementName=control, Path=PauseCommand}">
                    <Path Stretch="Uniform" Data="{Binding Source={StaticResource PauseIcon}, Path=Data}" Fill="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" />
                </Button>-->
                <!--<Button Margin="4,0,0,0" Visibility="{Binding ElementName=control, Path=StopButtonVisibility}" DockPanel.Dock="Left" HorizontalAlignment="Left" Style="{DynamicResource ModernButtonStyle}" Command="{Binding ElementName=control, Path=StopCommand}">
                    <Path Stretch="Uniform" Data="{Binding Source={StaticResource StopIcon}, Path=Data}" Fill="{Binding Path=Foreground, ElementName=control}" />
                </Button>-->
                <TextBlock Visibility="{Binding ElementName=control, Path=BufferingProgressVisibility}" VerticalAlignment="Center" Margin="4,0,0,0" DockPanel.Dock="Left" Name="mediaBufferingStatus" Text="{Binding ElementName=control, Path=BufferingProgress, Mode=OneWay}"/>
                <TextBlock Foreground="{Binding ElementName=control, Path=Foreground}" VerticalAlignment="Center" Margin="4,0,0,0" DockPanel.Dock="Left" Name="mediaStatusPrefixTextblock" Text="{x:Static properties:Resources.StreamStatusPrefix}"/>
                <TextBlock Foreground="{Binding ElementName=control, Path=Foreground}" VerticalAlignment="Center" Margin="4,0,0,0" DockPanel.Dock="Left" Name="mediaStatusTextblock" Text="{Binding ElementName=control, Path=StreamStatus, Mode=OneWay}"/>
            </DockPanel>
        </Grid>
    </Border>

</UserControl>