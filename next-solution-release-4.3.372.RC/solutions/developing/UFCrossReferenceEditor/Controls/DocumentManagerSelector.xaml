<UserControl x:Class="UFCrossReferenceEditor.Controls.DocumentManagerSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:UFCrossReferenceEditor.Properties"
             xmlns:local="clr-namespace:UFCrossReferenceEditor.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label Content="{x:Static properties:Resources.DocumentManagerSelection}" Height="50"/>
        <ListView x:Name="listView1" SelectionMode="Single" Grid.Row="1">
          <ListView.ItemsPanel>
            <ItemsPanelTemplate>
              <WrapPanel Orientation="Horizontal" />
            </ItemsPanelTemplate>
          </ListView.ItemsPanel>
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid>
                  <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" 
                            IsEnabled="{Binding IsAllowed, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                  <TextBlock HorizontalAlignment="Left"
                             Text="{Binding Name, Mode=OneWay}"
                             TextWrapping="Wrap" Background="Transparent" />
                </CheckBox>
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>

      <ScrollViewer Grid.Row="2">
        <ListView x:Name="listView" SelectionMode="Single">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                            <TextBlock HorizontalAlignment="Left"
                                       Text="{Binding Name, Mode=OneWay}" 
                                       TextWrapping="Wrap" Background="Transparent" />
                        </CheckBox>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        </ScrollViewer>
        <Grid Grid.Row="3" HorizontalAlignment="Stretch">
          <Grid.ColumnDefinitions>
            <ColumnDefinition  Width="1*"/>
            <ColumnDefinition  Width="8*"/>
            <ColumnDefinition  Width="8*"/>
            <ColumnDefinition  Width="1*"/>
          </Grid.ColumnDefinitions>
          <Button Grid.Column="1" HorizontalAlignment="Stretch" Content="{x:Static properties:Resources.SelectAll}" Click="SelectAll_Click" Margin="10"/>
          <Button Grid.Column="2" HorizontalAlignment="Stretch" Content="{x:Static properties:Resources.SelectNone}" Click="SelectNone_Click" Margin="10"/>
        </Grid>
    </Grid>
</UserControl>
