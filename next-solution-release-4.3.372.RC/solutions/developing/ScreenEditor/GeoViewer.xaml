<UserControl x:Class="ScreenManager.GeoViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" 
             xmlns:local="clr-namespace:ScreenManager"
             xmlns:properties="clr-namespace:ScreenManager.Properties" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:dxm="http://schemas.devexpress.com/winfx/2008/xaml/map"
             PreviewKeyDown="GeoViewerControl_PreviewKeyDown"
             PreviewKeyUp="GeoViewerControl_PreviewKeyUp">
    <UserControl.Resources>
        <ResourceDictionary>
          <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/SharedResources;component/Resources/Screen.xaml" />
          </ResourceDictionary.MergedDictionaries>
            <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConv" />

            <DataTemplate x:Key="nodeTemplate">
                <Grid Width="12" Height="12" >
                    <Ellipse Fill="#FF333333"/>
                    <Ellipse Margin="3" Fill="Green"/>
                    <Grid.RenderTransform>
                        <TranslateTransform X="-6" Y="-6"/>
                    </Grid.RenderTransform>
                </Grid>
            </DataTemplate>
            <DataTemplate x:Key="barTemplate">
                <Grid>
                    <Expander IsExpanded="False" Visibility="{Binding IsHistorizing, Converter={StaticResource BoolToVisibilityConv}}"
                              HorizontalAlignment="Left" VerticalAlignment="Top" ToolTip="{x:Static properties:Resources.GeoHistPanel}">
                        <Expander.Header>
                            <Image Source="{StaticResource SMDataFindGeo}" Height="48" Width="48" />
                        </Expander.Header>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Top">
                            <dx:WaitIndicator DeferedVisibility="{Binding IsBusy, Mode=OneWay}"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Static properties:Resources.GeoStartDate}" Margin="2" Width="70"/>
                                <dxe:DateEdit Margin="2" AllowNullInput="False" DateTime="{Binding StartDate}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Static properties:Resources.GeoEndDate}" Margin="2" Width="70"/>
                                <dxe:DateEdit Margin="2" AllowNullInput="False" DateTime="{Binding EndDate}"/>
                            </StackPanel>
                            <Button Command="{Binding ShowPathCommand}" ToolTip="{x:Static properties:Resources.GeoShowPath}">
                                <Button.Content>
                                    <Image Source="{StaticResource SMShowGeoPath}" Height="48" Width="48" />
                                </Button.Content>
                            </Button>
                            <Button Command="{Binding ClearPathCommand}" ToolTip="{x:Static properties:Resources.GeoClearPath}">
                                <Button.Content>
                                        <Image Source="{StaticResource SMClearGeoPath}" Height="48" Width="48" />
                                </Button.Content>
                            </Button>
                        </StackPanel>
                    </Expander>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid KeyboardNavigation.IsTabStop="False" x:Name="gridMenu">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
    <Grid Grid.Row="1">
        <dxm:MapControl x:Name="mapControl" IsManipulationEnabled="True" VirtualizingPanel.IsVirtualizing="True" ZoomLevel="8">

            <dxm:MapControl.MiniMap>
                <dxm:MiniMap Width="256" Height="256" Alignment="TopLeft" x:Name="miniMap">
                    <dxm:MiniMap.Behavior>
                        <dxm:FixedMiniMapBehavior ZoomLevel="0.5"/>
                    </dxm:MiniMap.Behavior>
                    <dxm:MiniMapImageTilesLayer x:Name="imageMiniMapTilesLayer" 
                         DataProvider="{Binding ElementName=imageTilesLayer, Path=DataProvider}"/>
                    <dxm:MiniMapVectorLayer ShapeFill="Red" ShapeStroke="White">
                        <dxm:ListSourceDataAdapter x:Name="listSourceDataAdapter">
                            <dxm:ListSourceDataAdapter.Mappings>
                                <dxm:MapItemMappingInfo Latitude="Latitude" Longitude="Longitude"/>
                            </dxm:ListSourceDataAdapter.Mappings>
                            <dxm:ListSourceDataAdapter.ItemSettings>
                                <dxm:MapDotSettings Size="8" />
                            </dxm:ListSourceDataAdapter.ItemSettings>
                        </dxm:ListSourceDataAdapter>
                    </dxm:MiniMapVectorLayer>
                </dxm:MiniMap>
            </dxm:MapControl.MiniMap>


            <dxm:MapControl.ScrollButtonsOptions>
                <dxm:ScrollButtonsOptions Visible="False"/>
            </dxm:MapControl.ScrollButtonsOptions>
            <dxm:MapControl.ZoomTrackbarOptions>
                <dxm:ZoomTrackbarOptions Visible="False"/>
            </dxm:MapControl.ZoomTrackbarOptions>

            <dxm:ImageTilesLayer x:Name="imageTilesLayer">
                <dxm:ImageTilesLayer.DataProvider>
                    <dxm:BingMapDataProvider x:Name="bingMapDataProvider" BingKey="AlyRXdH0-7gITM8V4SpqrJg7QxwD9zzbzLIICmxrhn1LS8rtJn7bpcCym6NK0qGb" Kind="Hybrid"/>
                </dxm:ImageTilesLayer.DataProvider>
            </dxm:ImageTilesLayer>

            <dxm:VectorLayer>
                <dxm:MapItemStorage x:Name="screenLayer"/>
            </dxm:VectorLayer>
            <dxm:VectorLayer>
                <dxm:MapItemStorage x:Name="pathLayer"/>
            </dxm:VectorLayer>

        </dxm:MapControl>
        
        <Button x:Name="btnNext" ToolTip="{x:Static properties:Resources.GeoFindNextPlace}" Margin="2,2" Width="150" Height="50" HorizontalAlignment="Right" VerticalAlignment="Top" Click="btnNext_Click">
            <Button.Content>
                <Image Source="{StaticResource SMReport_Next}" Height="48" Width="48" />
            </Button.Content>
        </Button>
        <!--<Grid.CacheMode>
            <BitmapCache EnableClearType="True" RenderAtScale="1.0" />
        </Grid.CacheMode>-->
        <Button x:Name="btnClose" IsCancel="True" Content="X" Margin="2,2" Width="150" Height="50" HorizontalAlignment="Center" VerticalAlignment="Bottom" Visibility="Collapsed">
        </Button>
        <ProgressBar x:Name="progressBar" IsIndeterminate="True" Visibility="Collapsed"  HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="15" />
    </Grid>
    </Grid>
</UserControl>
