<UserControl x:Class="ScreenManager.PropertyDataTemplate.GeoScadaExplorerPropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ScreenManager"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel LastChildFill="True">
      <!--<Grid x:Name="details" DockPanel.Dock="Top" IsEnabled="False">
        <Grid.ColumnDefinitions>
          <ColumnDefinition/>
          <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
          <RowDefinition/>
          <RowDefinition/>
          <RowDefinition/>
          <RowDefinition/>
          <RowDefinition/>
          <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content="Latitue" />
        <Label Grid.Column="1" Content="{Binding Latitude, Mode=OneWay}" />

        <Label Grid.Row="1" Content="Longitude" />
        <Label Grid.Row="1" Grid.Column="1" Content="{Binding Longitude, Mode=OneWay}" />

        <Label Grid.Row="2" Content="LatitudeTag" />
        <Label Grid.Row="2" Grid.Column="1" Content="{Binding LatitudeTag, Mode=OneWay}" />

        <Label Grid.Row="3" Content="LongitudeTag" />
        <Label Grid.Row="3" Grid.Column="1" Content="{Binding LongitudeTag, Mode=OneWay}" />

        <Label Grid.Row="4" Content="MapMinZoomLevelVisibility" />
        <Label Grid.Row="4" Grid.Column="1" Content="{Binding MapMinZoomLevelVisibility, Mode=OneWay}" />

        <Label Grid.Row="5" Content="MapMaxZoomLevelVisibility" />
        <Label Grid.Row="5"  Grid.Column="1" Content="{Binding MapMaxZoomLevelVisibility, Mode=OneWay}" />

      </Grid>-->
        <Button x:Name="brush" Tag="{Binding Value, Mode=TwoWay}" Click="DlgButton_Click">
            <Button.Content>
                <Image Source="{StaticResource EditGeneralSmall}" Height="16" Width="16" />
            </Button.Content>
        </Button>
    </DockPanel>
</UserControl>
