<Page
    x:Class="NExTIoTShell.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NExTIoTShell"
    xmlns:converters="using:Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <ResourceDictionary>
            <converters:BiStateValueConverter x:Key="BiStateConverter" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Image Source="Images\SPLASHNEXTIOT.png"/>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
            <Button x:Name="btnOpen" Click="Button_OpenClick" Content="Open Project..." Visibility="Collapsed" Margin="2"/>
            <Button x:Name="btnPublish" Click="Button_PublishClick" Content="Publish Device..." Visibility="Collapsed" Margin="2"/>
            <Grid x:Name="gridDeviceName" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Device Name" HorizontalAlignment="Right" Margin="2" VerticalAlignment="Center"/>
                <TextBox x:Name="txtDeviceName" Grid.Column="1" HorizontalAlignment="Stretch" Margin="2" VerticalAlignment="Stretch"/>
            </Grid>
        </StackPanel>
        
        <Grid x:Name="busyPanel" Visibility="Collapsed" Margin="5">
            <Rectangle Fill="Black" Opacity="0.3"/>
            <Grid VerticalAlignment="Center" Background="{Binding Path=Background, ElementName=progressBar}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Border CornerRadius="5" Background="{Binding Path=Background, ElementName=progressBar}" VerticalAlignment="Center" HorizontalAlignment="Center" >
                    <TextBlock x:Name="StatusText" Text="Processing..." FontSize="26.667" Margin="10"/>
                </Border>
                <ProgressBar x:Name="progressBar" IsIndeterminate="True" VerticalAlignment="Bottom"
                             Height="{Binding Path=ActualHeight, ElementName=StatusText}" 
                             Width="{Binding Path=ActualWidth, ElementName=StatusText}"
                             Grid.Row="1" />
            </Grid>
        </Grid>
    </Grid>
</Page>
