<UserControl x:Class="CommonControls.ConnectionWizard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:self="clr-namespace:CommonControls"
             mc:Ignorable="d" Width="500" Height="400"
             d:DesignHeight="300" d:DesignWidth="300" 
             xmlns:properties="clr-namespace:CommonControls.Properties"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             PreviewKeyDown="UserControl_PreviewKeyDown">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <dx:DXTabControl x:Name="tabControlExt1" >
            <dx:DXTabItem Header="{x:Static properties:Resources.SQLServer}" x:Name="tabSQL">
                <self:SQLConnectControl x:Name="sqlConnectControl"/>
            </dx:DXTabItem>
            <dx:DXTabItem Header="{x:Static properties:Resources.SQLAzure}" x:Name="tabSQLAzure">
                <self:SQLAzureConnectControl x:Name="sqlAzureConnectControl"/>
            </dx:DXTabItem>
            <dx:DXTabItem Visibility="Collapsed" Header="{x:Static properties:Resources.MSAccess}" x:Name="tabAccess">
                <Grid VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label>MS Access File:</Label>
                    <TextBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Validation.ErrorTemplate="{x:Null}"
                                x:Name="buttonEditAccess" />
                    <Button Grid.Column="2" Click="buttonEditAccess_DefaultButtonClick" Width="30">
                        <Button.Content>
                            <Image Source="{StaticResource SelectPathSmall}" Height="16" Width="16" />
                        </Button.Content>
                    </Button>
                </Grid>
            </dx:DXTabItem>
            <dx:DXTabItem Visibility="Collapsed" Header="{x:Static properties:Resources.XMLFile}" x:Name="tabXML">
                <Grid VerticalAlignment="Top">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Label>Xml Document:</Label>
                    <TextBox Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Center" Validation.ErrorTemplate="{x:Null}"
                                x:Name="buttonEditXML" />
                    <Button Grid.Column="2" x:Name="buttonOpenXML" Click="buttonEditXML_DefaultButtonClick" Width="30">
                        <Button.Content>
                            <Image Source="{StaticResource SelectPathSmall}" Height="16" Width="16" />
                        </Button.Content>
                    </Button>
                </Grid>
            </dx:DXTabItem>
            <dx:DXTabItem Header="{x:Static properties:Resources.MySQL}" x:Name="tabMySQL">
                <self:MySQLConnectControl x:Name="mysqlConnectControl"/>
            </dx:DXTabItem>
            <dx:DXTabItem Header="{x:Static properties:Resources.SQLite}" x:Name="tabSQLite">
                <self:SQLiteConnectControl UIService="{Binding UIService, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}" x:Name="sqliteConnectControl"/>
            </dx:DXTabItem>
            <dx:DXTabItem Header="{x:Static properties:Resources.PostgreSQL}" x:Name="tabPostgreSQL">
                <self:PostgreSQLConnectControl x:Name="postgresqlConnectControl" />
            </dx:DXTabItem>
        </dx:DXTabControl>
        <StackPanel Orientation="Vertical" Grid.Row="1" Margin="2,2,2,2">
            <TextBlock Margin="2,2,2,2" VerticalAlignment="Center" Text="{x:Static properties:Resources.ConnectionString}"/>
            <TextBox Margin="2,2,2,2" x:Name="editConnectionString" HorizontalAlignment="Stretch" TextWrapping="Wrap" TextChanged="editConnectionString_TextChanged"/>
        </StackPanel>
    </Grid>
</UserControl>
