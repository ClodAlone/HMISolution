<UserControl x:Class="CommonControls.PropertyDataTemplate.HistorianSettingsPropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ProgressBar x:Name="progressBar" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="10"
                     IsIndeterminate="True" Visibility="Collapsed" />
        <ComboBox x:Name="comboPrototype" IsEditable="True" IsReadOnly="True" Grid.Row="1"
                  Text="{Binding Value, Mode=OneTime}" 
                  SelectedValue="{Binding Value, Mode=TwoWay, ValidatesOnDataErrors=True}" 
                  Validation.ErrorTemplate="{x:Null}"
                  DropDownOpened="comboPrototype_DropDownOpened" />
        <ContentPresenter Grid.Column="1" Grid.Row="1"
                Content="{Binding ElementName=comboPrototype, Path=(Validation.Errors).CurrentItem}" />
    </Grid>
</UserControl>
