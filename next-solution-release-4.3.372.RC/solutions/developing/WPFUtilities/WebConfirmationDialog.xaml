<UserControl x:Class="WPFUtilities.WebConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:WPFUtilities.Properties"
             xmlns:self="clr-namespace:WPFUtilities"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style x:Key="WebDialogTxtStyle">
                <Setter Property="TextBlock.Padding" Value="5" />
                <Setter Property="TextBlock.TextAlignment" Value="Center" />
                <Setter Property="TextBlock.Foreground" Value="White" />
                <Setter Property="TextBlock.FontSize" Value="16" />
            </Style>
            <Style x:Key="WebDialogBtnStyle">
                <Setter Property="Button.Padding" Value="5" />
                <Setter Property="Button.Margin" Value="5" />
            </Style>
            <Style TargetType="{x:Type self:WebConfirmationDialog}">
                <Setter Property="Background" Value="DimGray" />
                <!--<Setter Property="Opacity" Value="0.75" />-->
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="webDialogTxt" Grid.Row="1" VerticalAlignment="Bottom" Style="{StaticResource WebDialogTxtStyle}"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Center">
            <Button x:Name="webYesBtn" Style="{StaticResource WebDialogBtnStyle}" Content="{x:Static properties:Resources.Yes}" Click="WebDialogYes_Click" />
            <Button x:Name="webNoBtn" Style="{StaticResource WebDialogBtnStyle}" Content="{x:Static properties:Resources.No}" Click="WebDialogNo_Click" />
        </StackPanel>
    </Grid>
</UserControl>
