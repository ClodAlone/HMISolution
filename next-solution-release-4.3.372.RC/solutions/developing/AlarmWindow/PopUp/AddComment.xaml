<UserControl x:Class="AlarmWindow.PopUp.AddComment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:wpfutilities="clr-namespace:WPFUtilities.Properties;assembly=WPFUtilities"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="300">
    <Grid>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type ValidationError}">
                <TextBlock FontStyle="Italic"
          Foreground="Red" FontSize="16"
          HorizontalAlignment="Right"
          Margin="0,1"
          Text="{Binding Path=ErrorContent}" 
          />
            </DataTemplate>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <TextBox Validation.ErrorTemplate="{x:Null}" 
                 x:Name="textBoxComment"
                 Text="{Binding UserTempComment, ValidatesOnDataErrors=True, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 Background="{x:Null}"/>
        <ContentPresenter Grid.Row="1" Grid.Column="2" 
            Content="{Binding ElementName=textBoxComment, Path=(Validation.Errors).CurrentItem}"/>        
        <Button x:Name="selectButton"
                MinHeight="{Binding Source={x:Static wpfutilities:Settings.Default}, Path=ButtonsHeight}"
                MinWidth="{Binding Source={x:Static wpfutilities:Settings.Default}, Path=ButtonsWidth}"
                FontSize="{Binding ElementName=textBoxComment, Path=FontSize}"
                VerticalContentAlignment="Center"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Content="..."
                Grid.Column="1" Click="selectButton_Click"/>

    </Grid>
</UserControl>
