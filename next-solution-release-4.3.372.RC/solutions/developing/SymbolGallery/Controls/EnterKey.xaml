<UserControl x:Class="SymbolGallery.Controls.EnterKey"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:SymbolGallery.Properties"
             Height="239" Width="351">

  <UserControl.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/SharedResources;component/Resources/SymbolGallery.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid  Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="10*" />
            </Grid.ColumnDefinitions>
            <TextBlock FontFamily="Segoe UI"  Text="{x:Static properties:Resources.EnterKeyKey}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,20,5,20" Height="20"/>
            <PasswordBox x:Name="keyBox" HorizontalAlignment="Stretch" Grid.Column="1" VerticalAlignment="Center" />
        </Grid>
        <Grid  Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="2*" />
            </Grid.ColumnDefinitions>
            <TextBlock Margin="20,10,0,0" Width="250" x:Name="textBox" FontFamily="Segoe UI" FontWeight="Normal" FontSize="18" TextWrapping="WrapWithOverflow"
                   Text="" VerticalAlignment="top"/>
            <Image x:Name="image" Margin="10,10,10,10" Source="{StaticResource SGProtected}" Grid.Column="1" VerticalAlignment="Top" Width="32" Height="32"/>
        </Grid>
        <StackPanel Grid.Row="2">
            <Button Content="{x:Static properties:Resources.EnterKey_ManagePasswordBtnTitle}" Visibility="Collapsed" x:Name="ManagePassword" />
            <Button Content="{x:Static properties:Resources.EnterKey_RemovePasswordBtnTitle}" Visibility="Collapsed" x:Name="RemovePassword" />
        </StackPanel>

    </Grid>
</UserControl>
