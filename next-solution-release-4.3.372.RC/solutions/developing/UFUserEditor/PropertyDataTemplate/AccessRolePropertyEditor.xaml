<UserControl x:Class="UFUserEditor.PropertyDataTemplate.AccessRolePropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
                <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <ProgressBar x:Name="progressBar" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="10"
                     IsIndeterminate="True" Visibility="Collapsed" />
        <ComboBox Grid.Column ="0" x:Name="comboRole" Grid.Row="1" SelectedItem="{Binding Value, ValidatesOnDataErrors=True}"
                  DropDownOpened="comboRole_DropDownOpened" PreviewKeyDown="comboRole_KeyDown"
                  VerticalAlignment="Center" HorizontalAlignment="Stretch" IsEditable="False" />
        <ContentPresenter Grid.Column="1"  Grid.Row="1"
                        Content="{Binding ElementName=comboRole, Path=(Validation.Errors).CurrentItem}"/>
    </Grid>
</UserControl>
