<dx:DXWindow x:Class="MSZUtils.Controls.LoginControl"
        SizeToContent="WidthAndHeight" BorderEffect="None"
        FocusManager.FocusedElement="{Binding ElementName=txtUser}"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ToolWindow" Width="500"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:properties="clr-namespace:MSZUtils.Properties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d">
    <Border Margin="10,10,10,10" CornerRadius="10">
        <StackPanel Margin="10,10,10,10">

            <StackPanel Orientation="Horizontal">
                <Image Source="..\Images\User-Login.png"/>
            </StackPanel>
            <StackPanel Margin="10,10,10,10">
                <StackPanel Orientation="Vertical">
                    <Label x:Name="userName" Content="{x:Static properties:Resources.UserNameLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding UserName}" HorizontalAlignment="Stretch" FontSize="18" x:Name="txtUser"/>
                    </Grid>
                </StackPanel>
                <StackPanel Orientation="Vertical">
                    <Label x:Name="password" Content="{x:Static properties:Resources.PasswordLabel}"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <PasswordBox x:Name="passwordBox" HorizontalAlignment="Stretch"  FontSize="18" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10,10,10,10">
                <Button Content="{x:Static properties:Resources.OkLabel}" x:Name="loginButton" Click="LoginButton_Click" IsDefault="True" Margin="2,2,12,2" Width="120" Height="50"/>
                <Button Content="{x:Static properties:Resources.CancelLabel}" x:Name="loginCancel" IsCancel="True" Margin="12,2,2,2" Width="120"  Height="50"/>
            </StackPanel>
        </StackPanel>

    </Border>

</dx:DXWindow>
