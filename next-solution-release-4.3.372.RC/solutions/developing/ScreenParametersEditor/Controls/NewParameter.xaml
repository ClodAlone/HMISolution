<UserControl x:Class="ScreenParametersEditor.Controls.NewParameter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:ScreenParametersEditor.Properties"
             xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
             xmlns:local="clr-namespace:ScreenParametersEditor.Converters"
             xmlns:propeditor="clr-namespace:ScreenParametersEditor.PropertyDataTemplate"
             FocusManager.FocusedElement="{Binding ElementName=lblEditAlias}"
             mc:Ignorable="d" 
             Width="360" Height="60"
             d:DesignHeight="60" d:DesignWidth="360">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
              <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <local:ParameterItemConverter x:Key="ParameterItemConverter" />
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="lblEditAlias" Text="{x:Static properties:Resources.NewParameterAlias}" VerticalAlignment="Center"   />
            <propeditor:ParameterItemPropertyEditorCombo x:Name="ID_editor" ButtonTagBindingProp="text" Grid.Column="1" />

            <TextBlock Grid.Row="1" x:Name="lblEditVar" Text="{x:Static properties:Resources.NewParameterVar}" VerticalAlignment="Center" />
            <propeditor:ParameterItemPropertyEditorCombo x:Name="text_editor" ButtonTagBindingProp="ID" Grid.Row="1" Grid.Column="1" />

        </Grid>
    </ScrollViewer>
</UserControl>
