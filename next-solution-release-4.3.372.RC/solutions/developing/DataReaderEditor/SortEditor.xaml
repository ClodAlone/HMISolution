<UserControl x:Class="DataReaderEditor.SortEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:DataReaderEditor.Properties"
             mc:Ignorable="d" 
             Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.ColumnsTitle}" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <ListBox Grid.Row="1" x:Name="listColums" SelectionMode="Multiple"/>
        <ComboBox Grid.Row="2" x:Name="comboDirection">
            <ComboBoxItem Content="{x:Static properties:Resources.AscendingTitle}" Tag="ASC" IsSelected="True"/>
            <ComboBoxItem Content="{x:Static properties:Resources.DescendingTitle}" Tag="DESC"/>
        </ComboBox>
    </Grid>
</UserControl>
