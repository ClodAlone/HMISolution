<UserControl x:Class="DataReaderEditor.SelectEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:properties="clr-namespace:DataReaderEditor.Properties"
             mc:Ignorable="d" 
             Height="300" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width=".5*"/>
            <ColumnDefinition Width="10*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="10*" />
            <RowDefinition/>
        </Grid.RowDefinitions>
        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Grid.RowSpan="2"/>
        <TextBlock Grid.Column="0" Grid.Row="0" Text="{x:Static properties:Resources.TablesTitle}" VerticalAlignment="Center" HorizontalAlignment="Left" />
        <ListBox Grid.Column="0" Grid.Row="1" x:Name="listTables" SelectionChanged="listTables_SelectionChanged"/>
        <StackPanel Orientation="Horizontal" Grid.Column="2" Grid.Row="0">
            <TextBlock Text="{x:Static properties:Resources.ColumnsTitle}" VerticalAlignment="Center" HorizontalAlignment="Left" />
            <Button Content="{x:Static properties:Resources.SelectAllTitle}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnSelectAllColumns_Click"/>
            <Button Content="{x:Static properties:Resources.UnselectAllTitle}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="btnUnselectAllColumns_Click"/>
        </StackPanel>
        <ListBox Grid.Column="2" Grid.Row="1" x:Name="listColums" SelectionMode="Multiple" SelectionChanged="listColums_SelectionChanged"/>

        <Grid Grid.Row="2" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static properties:Resources.MaxTakeTitle}" VerticalAlignment="Center" HorizontalAlignment="Left"/>
            <TextBox x:Name="txtMaxTake" Grid.Column="1" TextChanged="TextBox_TextChanged_1" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        </Grid>
    </Grid>
</UserControl>
