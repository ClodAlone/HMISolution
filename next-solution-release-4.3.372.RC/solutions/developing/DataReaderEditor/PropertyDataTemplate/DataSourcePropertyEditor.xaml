<UserControl x:Class="DataReaderEditor.PropertyDataTemplate.DataSourcePropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:converters="clr-namespace:DataReaderEditor.Converters"
             xmlns:utilities="clr-namespace:WPFUtilities.Converters;assembly=WPFUtilities"
             d:DesignHeight="25" d:DesignWidth="200">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <converters:DataSourceConverter x:Key="DataSourceConverter" />
            <converters:DataReaderModelConverter x:Key="DataReaderModelConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    
    <DockPanel>
        <Button x:Name="reset" DockPanel.Dock="Right" Click="ResetButton_Click">
            <Button.Content>
                <Image Source="{StaticResource CancelSmall}" Height="16" Width="16" />
            </Button.Content>
        </Button>
        <Button x:Name="connection" Tag="{Binding Value, Mode=TwoWay}"  DockPanel.Dock="Right" Click="DlgButton_Click">
            <Button.Content>
                <Image Source="{StaticResource EditGeneralSmall}" Height="16" Width="16" />
            </Button.Content>
        </Button>
        <Label x:Name="connectionlabel" Content="{Binding Path=Tag, ElementName=connection, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource DataSourceConverter}, ConverterParameter={StaticResource DataReaderModelConverter}}"/>
    </DockPanel>
</UserControl>
