<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Grid"
                    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Shared.WPF;component/SkinManager/Office2007BlueStyle.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <SolidColorBrush x:Key="Office2007BlueListBoxBorderBrush" Color="#FF7F9DB9" />
    <SolidColorBrush x:Key="Office2007BlueListBoxBackground" Color="#FFDEEBFE" />
    <SolidColorBrush x:Key="Office2007BlueListBoxForeground" Color="#FF15428B" />
    <SolidColorBrush x:Key="ComboBoxBackground.Blue" Color="#FFEAF2FB" />
    <SolidColorBrush x:Key="ComboBoxBorderBrush.Blue" Color="#FFABC1DE" />
    <SolidColorBrush x:Key="ComboBoxHoverBackground.Blue" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="ComboBoxItemBackground.Blue" Color="Transparent" />
    <Geometry x:Key="DownArrowGeometry">M 0 0 L 3.5 4 L 7 0 Z</Geometry>
    <SolidColorBrush x:Key="PathFill.Blue" Color="#FF4A6B96" />
    <LinearGradientBrush x:Key="ButtonPressedBrush.Blue" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Offset="0" Color="#FFF3B277" />
        <GradientStop Offset="0.4" Color="#FFF4B378" />
        <GradientStop Offset="0.4" Color="#FFF4A770" />
        <GradientStop Offset="1" Color="#FFF37E24" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ButtonMouseOverBrush.Blue" StartPoint="0,0" EndPoint="0,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Offset="1" Color="#FFFFE69E" />
            <GradientStop Offset="0.4" Color="#FFFFE8A7" />
            <GradientStop Offset="0.4" Color="#FFFFD767" />
            <GradientStop Color="#FFFFFCDE" />
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ButtonMouseOverBorderBrush.Blue" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Offset="0" Color="#FFDBCE99" />
        <GradientStop Offset="0.996" Color="#FFCEC0A4" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ButtonPressedBorderBrush.Blue" StartPoint="0.5,0" EndPoint="0.5,1">
        <GradientStop Offset="0" Color="#FFA78E66" />
        <GradientStop Offset="1" Color="#FFB9AE93" />
    </LinearGradientBrush>

    <Style x:Key="BlueComboBoxReadonlyToggleButton" TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle" Value="true" />
        <Setter Property="IsTabStop" Value="false" />
        <Setter Property="Focusable" Value="false" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="{StaticResource ComboBoxBackground.Blue}" />
        <Setter Property="BorderBrush" Value="{StaticResource ComboBoxBorderBrush.Blue}" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">

                    <Border Name="Chrome1"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="ComboBoxButton" />
                            </Grid.ColumnDefinitions>

                            <Border Name="Chrome"
                                    Grid.Column="1"
                                    Width="16"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="0">

                                <Path Name="Arrow"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="{StaticResource DownArrowGeometry}"
                                      Fill="{StaticResource PathFill.Blue}" />
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition SourceName="Chrome" Property="IsMouseOver" Value="true" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ButtonMouseOverBorderBrush.Blue}" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />

                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ButtonMouseOverBrush.Blue}" />
                        </MultiTrigger>

                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ButtonPressedBorderBrush.Blue}" />
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ButtonPressedBrush.Blue}" />
                        </Trigger>
                        <Trigger SourceName="Chrome1" Property="IsMouseOver" Value="True">
                            <Setter TargetName="Chrome1" Property="BorderThickness" Value="1" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />

                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ButtonMouseOverBrush.Blue}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxBorderBrush.Blue}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxBorderBrush.Blue}" />
                            <Setter TargetName="Chrome1" Property="Background" Value="White" />



                        </Trigger>
                        <Trigger SourceName="Chrome1" Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="Chrome1" Property="BorderThickness" Value="1" />
                            <Setter TargetName="Chrome" Property="BorderThickness" Value="1,0,0,0" />

                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ButtonMouseOverBrush.Blue}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxBorderBrush.Blue}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxBorderBrush.Blue}" />
                            <Setter TargetName="Chrome1" Property="Background" Value="White" />



                        </Trigger>


                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="Office2007BlueHoverListBoxStyle" TargetType="{x:Type local:HoverListBox}">
        <Setter Property="Background" Value="{StaticResource Office2007BlueListBoxBackground}" />
        <Setter Property="BorderBrush" Value="{StaticResource Office2007BlueListBoxBorderBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Office2007BlueListBoxForeground}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="true" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HoverListBox}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="1"
                            SnapsToDevicePixels="true">
                        <ScrollViewer Focusable="false" Padding="{TemplateBinding Padding}">
                            <ItemsPresenter x:Name="PART_ItemsPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        </ScrollViewer>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007BlueHoverListBoxStyle}" TargetType="{x:Type local:HoverListBox}" />

    <Style x:Key="Office2007BlueHoverListBoxItemStyle" TargetType="{x:Type local:HoverListBoxItem}">
        <Setter Property="Padding" Value="3,0,3,0" />
        <Setter Property="Background" Value="{StaticResource Office2007BlueListBoxBackground}" />
        <Setter Property="Foreground" Value="{StaticResource Office2007BlueListBoxForeground}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:HoverListBoxItem}">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="true">
                        <ContentPresenter HorizontalAlignment="Left"
                                          VerticalAlignment="{TemplateBinding VerticalAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource ButtonMouseOverBrush.Blue}" />
                            <!--  <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}"/>  -->
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource Office2007BlueHoverListBoxItemStyle}" TargetType="{x:Type local:HoverListBoxItem}" />

    <Style x:Key="Office2007BlueGridCellDropDownControlBaseStyle" TargetType="{x:Type local:GridCellDropDownControlBase}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GridCellDropDownControlBase}">
                    <Border BorderThickness="0" Padding="0">
                        <Grid Name="MainGrid" SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="0" MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" />
                            </Grid.ColumnDefinitions>
                            <TextBox Name="PART_Textbox"
                                     Grid.Column="0"
                                     HorizontalAlignment="Stretch"
                                     VerticalAlignment="Stretch"
                                     VerticalContentAlignment="{TemplateBinding VerticalAlignment}"
                                     HorizontalContentAlignment="{TemplateBinding HorizontalAlignment}"
                                     Background="{TemplateBinding Background}"
                                     BorderThickness="0"
                                     Foreground="{TemplateBinding Foreground}"
                                     IsReadOnly="{Binding Path=IsReadOnly,
                                                          Mode=TwoWay,
                                                          RelativeSource={RelativeSource TemplatedParent}}"
                                     Text="{Binding Path=Text,
                                                    Mode=TwoWay,
                                                    RelativeSource={RelativeSource TemplatedParent}}" />
                            <ToggleButton Name="PART_Togg"
                                          Grid.Column="1"
                                          HorizontalAlignment="Right"
                                          IsChecked="{Binding Path=IsDropDownOpen,
                                                              Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"
                                          Style="{StaticResource BlueComboBoxReadonlyToggleButton}" />
                            <!--
                                BorderBrush="{TemplateBinding Border.BorderBrush}"
                                Background="{x:Static SystemColors.ControlBrush}"
                            -->
                            <Popup Name="PART_Popup"
                                   Grid.ColumnSpan="2"
                                   Margin="1"
                                   AllowsTransparency="true"
                                   IsOpen="{Binding Path=IsDropDownOpen,
                                                    Mode=TwoWay,
                                                    RelativeSource={RelativeSource TemplatedParent}}"
                                   Placement="Bottom"
                                   PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}"
                                   StaysOpen="False">
                                <theme:SystemDropShadowChrome Name="Shdw"
                                                              MinWidth="{Binding ElementName=MainGrid,
                                                                                 Path=ActualWidth}"
                                                              MaxHeight="{x:Static local:GridCellDropDownControlBase.MaxDropDownHeight}"
                                                              Color="Transparent">
                                    <Border x:Name="DropDownBorder"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="1">
                                        <ContentControl Name="PART_Content" />
                                    </Border>
                                </theme:SystemDropShadowChrome>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                        <Trigger SourceName="PART_Popup" Property="Popup.HasDropShadow" Value="true">
                            <Setter TargetName="Shdw" Property="Margin" Value="0,0,5,5" />
                            <Setter TargetName="Shdw" Property="SnapsToDevicePixels" Value="true" />
                            <Setter TargetName="Shdw" Property="Color" Value="#71000000" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="true">
                            <Setter Property="Padding" Value="2" />
                            <Setter Property="IsTabStop" Value="false" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007BlueGridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellDropDownControlBase}" />

    <Style BasedOn="{StaticResource Office2007BlueGridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellComboBoxDropDown}" />

    <Style BasedOn="{StaticResource Office2007BlueGridCellDropDownControlBaseStyle}" TargetType="{x:Type local:GridCellGridListControlDropDown}" />





</ResourceDictionary>