<Window 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    x:Class="Syncfusion.Windows.PdfViewer.PasswordToolBar"
    xmlns:local="clr-namespace:Syncfusion.Windows.PdfViewer"
    Title="Password" Width="419" Height="140"
    ResizeMode="NoResize"
    ShowInTaskbar="False"
    WindowStartupLocation="CenterOwner" 
    FocusManager.FocusedElement="{Binding ElementName=passwordTextBox}" Icon="/Syncfusion.PdfViewer.WPF;component/Syncfusion.Windows.PdfViewer/PdfViewer.Icon.png" WindowStyle="ThreeDBorderWindow" Background="#FFF0F0F0">

    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Grid}">
                <Setter Property="Margin" Value="10" />
            </Style>
           
            
            <Style TargetType="{x:Type StackPanel}">
                <Setter Property="Orientation" Value="Horizontal" />
                <Setter Property="HorizontalAlignment" Value="Right" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Width" Value="70" />
                <Setter Property="Height" Value="25" />
                <Setter Property="Margin" Value="5,0,0,0" />
            </Style>
        </Grid.Resources>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="38" />
            <RowDefinition Height="0*" />
        </Grid.RowDefinitions>

        <Label Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2">This document is protected. Please enter a Document Open Password.</Label>
        <Label Grid.Column="0" Grid.Row="1">Enter Password:</Label>
        <PasswordBox Height="22" Name="passwordTextBox"  Grid.Column="1" Grid.Row="1">
        </PasswordBox>

        <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="2">
            <Button Name="okButton" Click="okButton_Click" IsDefault="True">OK</Button>
            <Button Name="cancelButton" IsCancel="True">Cancel</Button>
        </StackPanel>

    </Grid >

</Window>
