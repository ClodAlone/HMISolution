<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna"
    >
  <ControlTemplate x:Key="MiniToolbarTemplate" TargetType="{x:Type ItemsControl}">
    <AdornerDecorator>
      <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="PART_Shadow" Margin="0" SnapsToDevicePixels="true">
        <Border x:Name="PART_Border" Height="51" Background="{TemplateBinding Background}" CornerRadius="3"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
          <ItemsPresenter></ItemsPresenter>
        </Border>
      </Microsoft_Windows_Themes:SystemDropShadowChrome>
    </AdornerDecorator>
    <ControlTemplate.Triggers>
      <Trigger Property="Popup.HasDropShadow" Value="True">
        <Setter TargetName="PART_Shadow" Property="Margin" Value="0,0,3,3"/>
      </Trigger>
    </ControlTemplate.Triggers>
  </ControlTemplate>
  
  
</ResourceDictionary>