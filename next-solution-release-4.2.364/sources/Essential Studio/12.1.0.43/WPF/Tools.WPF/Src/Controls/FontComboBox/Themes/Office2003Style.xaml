<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
     xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
    xmlns:conv="clr-namespace:Syncfusion.Windows.Tools"
		xmlns:utilsOuter="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Luna"
>
    <!--converters-->

    <conv:PopupHeightToThumbOffsetConverter x:Key="PopupHeightToThumbOffsetConverter" />

    <conv:FontToStringConverter x:Key="FontToStringConverter" />

    <!--Brushes-->
    <SolidColorBrush x:Key="Office2003.ForegroundBrush" Color="#FF313031" />

    <SolidColorBrush x:Key="TextBoxBorderBrushKey.Default" Color="Transparent" />

    <SolidColorBrush x:Key="Office2003.BackgroundBrush" Color="#FFEAF2FB" />

    <SolidColorBrush x:Key="FontListComboBoxBorderBrush" Color="#7B9EBD" />

    <SolidColorBrush x:Key="Office2003.ArrowFill" Color="#FF303030" />

    <SolidColorBrush x:Key="Black" Color="Black" />

    <SolidColorBrush x:Key="Office2003.ListBoxBorderBrush" Color="#FF295697" />

    <SolidColorBrush x:Key="Office2003.BottomThumbBackground" Color="#EBF4FF" />

    <LinearGradientBrush x:Key="Office2003.DropDownButton.Background" StartPoint="0.5,1" EndPoint="0.5,0">
        <GradientStop Color="#D7E0F3" Offset="1" />
        <GradientStop Color="#9DB7E4" Offset="0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Office2003.DropDownButton.BorderBrush" StartPoint="0.5,1" EndPoint="0.5,0">
        <GradientStop Color="#EDF1F9" Offset="1" />
        <GradientStop Color="#CDDAF2" Offset="0" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="Office2003.DropDownButton.BorderOutside" Color="#4886D2" />

    <LinearGradientBrush x:Key="Office2003.DropDownButton.HighlightBackground" StartPoint="0.5,1" EndPoint="0.5,0">
        <GradientStop Color="#F1FCFF" Offset="1" />
        <GradientStop Color="#DCF2FF" Offset="0.53" />
        <GradientStop Color="#CBE9FF" Offset="0.5" />
        <GradientStop Color="#C2E3FF" Offset="0" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="Office2003.DropDownButton.HighlightBorder" StartPoint="0.5,1" EndPoint="0.5,0">
        <GradientStop Color="#F8FEFF" Offset="1" />
        <GradientStop Color="#DFF0FF" Offset="0" />
    </LinearGradientBrush>

    <!--Styles-->

    <Style x:Key="Office2003ToggleDropDownButtonStyle" TargetType="ToggleButton">
        <Setter Property="Margin" Value="1" />
        <Setter Property="FrameworkElement.MinWidth" Value="0" />
        <Setter Property="FrameworkElement.MinHeight" Value="0" />
        <Setter Property="FrameworkElement.Width" Value="15" />
        <Setter Property="FrameworkElement.Height" Value="Auto" />
        <Setter Property="ClickMode" Value="Press" />
        <Setter Property="Background" Value="{StaticResource Office2003.DropDownButton.Background}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <utilsOuter:TrippleBorder Name="PART_Border"
                                Background="{TemplateBinding Panel.Background}"
                                InsideBorderThickness="1"
                                BorderThickness="1"
                                OutsideBorderThickness="1"
                                InsideBorderBrush="{StaticResource Office2003.DropDownButton.Background}"
                                BorderBrush="{StaticResource Office2003.DropDownButton.BorderBrush}"
                                OutsideBorderBrush="{StaticResource Office2003.DropDownButton.BorderOutside}">
                        <ContentPresenter RecognizesAccessKey="True"
                              Content="{TemplateBinding ContentControl.Content}"
                              ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}"
                              Margin="{TemplateBinding Control.Padding}"
                              HorizontalAlignment="Stretch"
                              VerticalAlignment="Stretch"
                              SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                              ClipToBounds="True" />
                    </utilsOuter:TrippleBorder>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="PART_Border" Property="InsideBorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBackground}" />
                            <Setter TargetName="PART_Border" Property="BorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBorder}" />
                            <Setter TargetName="PART_Border" Property="OutsideBorderBrush" Value="{StaticResource Office2003.DropDownButton.BorderOutside}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Office2003.DropDownButton.HighlightBackground}" />
                <Setter Property="BorderBrush" Value="{StaticResource Office2003.DropDownButton.HighlightBorder}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Office2003FontListComboBoxStyle" TargetType="{x:Type local:FontListComboBox}">
        <Setter Property="Background" Value="{StaticResource Office2003.BackgroundBrush}" />
        <Setter Property="Foreground" Value="{StaticResource Office2003.ForegroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource TextBoxBorderBrushKey.Default}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:FontListComboBox}">
                    <Border SnapsToDevicePixels="True"
				BorderThickness="1" Name="Bd1"
				BorderBrush="{StaticResource FontListComboBoxBorderBrush}">
                        <Grid SnapsToDevicePixels="True">
                            <Border Name="Bd"
                        BorderThickness="{TemplateBinding Border.BorderThickness}"
						BorderBrush="{Binding Path=BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                        Background="{Binding Path=Background, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
						TextBlock.Foreground="{Binding Path=Foreground, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}">
                                <Grid Grid.IsSharedSizeScope="True">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="1" />
                                        <ColumnDefinition Width="8*" />
                                        <ColumnDefinition SharedSizeGroup="ComboBoxButton" Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border Margin="{TemplateBinding Control.Padding}"
                                Name="SelectedItemBorder"
                                Grid.ColumnSpan="2" />
                                    <ContentPresenter Name="textBox"
                                          Margin="{TemplateBinding Control.Padding}"
                                          HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}"
                                          Content="{Binding Path=SelectedFontFamily, Converter={StaticResource FontToStringConverter},
                                                    Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                          Grid.Column="1" />
                                    <ToggleButton Name="toggleButton"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay,
                                                  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                                      Grid.Column="3"
                                      Style="{StaticResource Office2003ToggleDropDownButtonStyle}">
                                        <Path Name="PART_Arrow"
                                Data="M0,0L3.5,3 7,0"
                                Stroke="{StaticResource Black}"
                                Fill="Transparent"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,0" />
                                    </ToggleButton>
                                </Grid>
                            </Border>
                            <Popup Placement="Bottom"
                       Name="PART_Popup"
			           TextBlock.Foreground="{Binding Path=ItemsForeground, Mode=TwoWay,
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Height="{Binding Path=PopupDropDownHeight, Mode=TwoWay,
                        RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       Focusable="False"
                       AllowsTransparency="True"
			           StaysOpen="True"
                       IsOpen="{Binding Path=IsDropDownOpen, Mode=TwoWay,
                                RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}"
                       PopupAnimation="Fade">
                                <Border BorderThickness="1.5" Name="listBorder" BorderBrush="{StaticResource Office2003.ListBoxBorderBrush}" CornerRadius="2" Background="{StaticResource Office2003.BackgroundBrush}" ClipToBounds="True">
                                    <DockPanel Name="FontListBoxPanel"  LastChildFill="True" Background="White"
                                   Height="{Binding ElementName=bt, Path=Offset}">
                                        <local:BottomThumb  x:Name="bt"  Height="10" DockPanel.Dock="Bottom"
                                            Offset="{Binding Path=PopupDropDownHeight, Mode=TwoWay, Converter={ StaticResource PopupHeightToThumbOffsetConverter},
                                            ConverterParameter=6,  RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
                                        <!--Style="{utilsOuter:SkinObject ScrollViewerStyle}"-->
                                        <ScrollViewer>
                                            <local:FontListBox x:Name="FontListBoxElement" DockPanel.Dock="Top"
                                    Width="{Binding Path=Width, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    SelectedFontFamily="{Binding Path=SelectedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FocusedFontFamily="{Binding Path=FocusedFontFamily, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    FontsSource="{Binding Path=FontsSource, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ThemeFonts="{Binding Path=ThemeFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    RecentlyUsedFonts="{Binding Path=RecentlyUsedFonts, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    HasFocus="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    DisplayFontNamesInSystemFont="{Binding Path=DisplayFontNamesInSystemFont, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    GroupHeaderStyle="{Binding Path=GroupHeaderStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemTemplate="{Binding Path=ItemTemplate, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
                                    ItemContainerStyle="{Binding Path=ItemContainerStyle, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}, UpdateSourceTrigger=PropertyChanged}"
 />
                                        </ScrollViewer>
                                    </DockPanel>
                                </Border>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>

                        <Trigger Property="UIElement.IsEnabled" Value="False">
                            <Setter Property="TextElement.Foreground">
                                <Setter.Value>
                                    <DynamicResource ResourceKey="{x:Static SystemColors.GrayTextBrushKey}" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Panel.Background" TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="textBox" Property="Content" Value="{Binding Path=FocusedFontFamily, Converter={StaticResource FontToStringConverter}, Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type local:FontListComboBox}}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2003FontListComboBoxStyle}" TargetType="{x:Type local:FontListComboBox}" />

    <Style x:Key="{x:Type local:BottomThumb}" TargetType="{x:Type local:BottomThumb}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BottomThumb}">
                    <Border Name="Border" SnapsToDevicePixels="True" Height="{TemplateBinding Height}" Width="{TemplateBinding Width}"  Background="{StaticResource Office2003.BottomThumbBackground}"
            BorderThickness="0,1,0,0" BorderBrush="{StaticResource FontListComboBoxBorderBrush}">
                        <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <Border Name="B1" Background="{StaticResource Office2003.ArrowFill}" Width="3" Height="3" CornerRadius="5" Margin="1" />
                            <Border Name="B2" Background="{StaticResource Office2003.ArrowFill}" Width="3" Height="3" CornerRadius="5" Margin="1" />
                            <Border Name="B3" Background="{StaticResource Office2003.ArrowFill}" Width="3" Height="3" CornerRadius="5" Margin="1" />
                            <Border Name="B4" Background="{StaticResource Office2003.ArrowFill}" Width="3" Height="3" CornerRadius="5" Margin="1" />
                        </WrapPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>