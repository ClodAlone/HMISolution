<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Chart="clr-namespace:Syncfusion.Windows.Controls.Gantt.Chart"
                    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF">

    <Style TargetType="Chart:GanttChart">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Chart:GanttChart}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="LightGray"
                            BorderThickness="0"
                            ClipToBounds="True"
                            SnapsToDevicePixels="True">
                        <ScrollViewer x:Name="PART_GanttChartScrollViewer"
                                      CanContentScroll="True"
                                      HorizontalScrollBarVisibility="Disabled"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalScrollBarVisibility="Hidden">
                            <ScrollViewer.Template>
                                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Chart:GanttChartBackgroundPanel x:Name="PART_GanttChartBackgroundPanel"
                                                                         Grid.Row="0"
                                                                         Grid.Column="0"
                                                                         HorizontalAlignment="Stretch"
                                                                         VerticalAlignment="Stretch" />
                                        <Chart:GanttChartStripLinePanel x:Name="PART_GanttChartStripLinePanel"
                                                                        Grid.Row="0"
                                                                        Grid.Column="0"
                                                                        HorizontalAlignment="Stretch"
                                                                        VerticalAlignment="Stretch"
                                                                        IsHitTestVisible="True" />
                                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                                Grid.Row="0"
                                                                Grid.Column="0"
                                                                Margin="{TemplateBinding Padding}"
                                                                CanContentScroll="{TemplateBinding CanContentScroll}"
                                                                CanHorizontallyScroll="False"
                                                                CanVerticallyScroll="False"
                                                                Content="{TemplateBinding Content}"
                                                                ContentTemplate="{TemplateBinding ContentTemplate}" />
                                        <Chart:GanttNodeConnector x:Name="PART_GanttNodeConnectorPanel"
                                                                  Grid.Row="0"
                                                                  Grid.Column="0"
                                                                  HorizontalAlignment="Stretch"
                                                                  VerticalAlignment="Stretch"
                                                                  Background="Transparent"
                                                                  IsHitTestVisible="False" />
                                        <ScrollBar x:Name="PART_VerticalScrollBar"
                                                   Grid.Row="0"
                                                   Grid.Column="1"
                                                   AutomationProperties.AutomationId="VerticalScrollBar"
                                                   Cursor="Arrow"
                                                   Maximum="{TemplateBinding ScrollableHeight}"
                                                   Minimum="0"
                                                   ViewportSize="{TemplateBinding ViewportHeight}"
                                                   Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                                   Value="{Binding VerticalOffset,
                                                                   Mode=OneWay,
                                                                   RelativeSource={RelativeSource TemplatedParent}}" />
                                        <ScrollBar x:Name="PART_HorizontalScrollBar"
                                                   Grid.Row="1"
                                                   Grid.Column="0"
                                                   AutomationProperties.AutomationId="HorizontalScrollBar"
                                                   Cursor="Arrow"
                                                   Maximum="{TemplateBinding ScrollableWidth}"
                                                   Minimum="0"
                                                   Orientation="Horizontal"
                                                   ViewportSize="{TemplateBinding ViewportWidth}"
                                                   Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                                   Value="{Binding HorizontalOffset,
                                                                   Mode=OneWay,
                                                                   RelativeSource={RelativeSource TemplatedParent}}" />
                                    </Grid>
                                </ControlTemplate>
                            </ScrollViewer.Template>
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <Chart:GanttChartItemsPanel IsItemsHost="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Chart:GanttNodeConnector">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:GanttNodeConnector">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Path Name="PART_Connetions"
                                  Data="{TemplateBinding ConnectorGeometry}"
                                  Stretch="None"
                                  Stroke="{TemplateBinding ConnectorStroke}"
                                  StrokeThickness="1" />
                            <Path Name="PART_Arrows"
                                  Data="{TemplateBinding ArrowGeometry}"
                                  Fill="{TemplateBinding ConnectorStroke}"
                                  Stretch="None"
                                  Stroke="{TemplateBinding ConnectorStroke}"
                                  StrokeThickness="1" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Chart:StripLine">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:StripLine">
                    <Border Background="{TemplateBinding Background}">
                        <ContentPresenter x:Name="PART_InnerContent"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}"
                                          ContentTemplate="{TemplateBinding ContentTemplate}">
                            <ContentPresenter.LayoutTransform>
                                <RotateTransform Angle="-90" />
                            </ContentPresenter.LayoutTransform>
                        </ContentPresenter>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Content" Value="{Binding Content}" />
        <Setter Property="Background" Value="{Binding Background}" />
        <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, Mode=TwoWay}" />
        <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, Mode=TwoWay}" />
        <Setter Property="ContentTemplate" Value="{Binding ContentTemplate}" />
        <Style.Triggers>
            <Trigger Property="Type" Value="Absolute">
                <Setter Property="Height" Value="{Binding Height}" />
                <Setter Property="Width" Value="{Binding Width}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Chart:GanttChartRow">
        <Setter Property="BorderBrush" Value="LightGray" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:GanttChartRow">
                    <Border Background="Transparent"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <Canvas x:Name="PART_BackgoundCanvas">
                            <Rectangle x:Name="PART_ResizingRect"
                                       Height="{TemplateBinding ActualHeight}"
                                       Stroke="Gray"
                                       StrokeThickness="1.5"
                                       Visibility="Hidden" />
                            <Chart:GanttChartRowItemsPresenter x:Name="ItemsPresenterElement" Height="{TemplateBinding ActualHeight}" />
                            <Popup x:Name="PART_DateTimeResizingTooltip"
                                   Width="Auto"
                                   Height="Auto"
                                   IsOpen="False">
                                <Border Width="210"
                                        Height="70"
                                        Background="White"
                                        BorderBrush="Gray"
                                        BorderThickness="0.5">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Margin="5,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="Task Name: " />
                                                <TextBlock Grid.Column="1"
                                                           Width="125"
                                                           Margin="10,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="{Binding TaskName}"
                                                           TextTrimming="CharacterEllipsis" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Margin="5"
                                                       FontWeight="Bold"
                                                       Text="StartTime :" />
                                            <TextBlock Grid.Column="1"
                                                       Margin="5"
                                                       Text="{Binding StartTime}" />
                                            <TextBlock Grid.Row="1"
                                                       Margin="5"
                                                       FontWeight="Bold"
                                                       Text="EndTime :" />
                                            <TextBlock Grid.Row="1"
                                                       Grid.Column="1"
                                                       Margin="5"
                                                       Text="{Binding EndTime}" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                            <Popup x:Name="PART_NumericResizingTooltip"
                                   Width="Auto"
                                   Height="Auto"
                                   IsOpen="False">
                                <Border Width="150"
                                        Height="70"
                                        Background="White"
                                        BorderBrush="Gray"
                                        BorderThickness="0.5">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Margin="5,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="Task Name: " />
                                                <TextBlock Grid.Column="1"
                                                           Width="80"
                                                           Margin="5,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="{Binding TaskName}"
                                                           TextTrimming="CharacterEllipsis" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Margin="5"
                                                       FontWeight="Bold"
                                                       Text="Start :" />
                                            <TextBlock Grid.Column="1"
                                                       Margin="5"
                                                       Text="{Binding Start}" />
                                            <TextBlock Grid.Row="1"
                                                       Margin="5"
                                                       FontWeight="Bold"
                                                       Text="End :" />
                                            <TextBlock Grid.Row="1"
                                                       Grid.Column="1"
                                                       Margin="5"
                                                       Text="{Binding End}" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                            <Popup x:Name="PART_ProgressResizingTooltip"
                                   Width="Auto"
                                   Height="Auto"
                                   IsOpen="False">
                                <Border Width="200"
                                        Height="50"
                                        Background="White"
                                        BorderBrush="Gray"
                                        BorderThickness="0.5">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Margin="5,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="Task Name: " />
                                                <TextBlock Grid.Column="1"
                                                           Width="120"
                                                           Margin="5,2,2,2"
                                                           FontWeight="Bold"
                                                           Text="{Binding TaskName}"
                                                           TextTrimming="CharacterEllipsis" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Margin="5"
                                                       FontWeight="Bold"
                                                       Text="Progress :" />
                                            <TextBlock Grid.Column="1"
                                                       Margin="5"
                                                       Text="{Binding Progress}" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                        </Canvas>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>