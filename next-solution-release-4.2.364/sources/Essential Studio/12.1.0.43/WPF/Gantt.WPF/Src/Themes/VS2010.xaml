<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.WPF"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Gantt"
                    xmlns:schedule="clr-namespace:Syncfusion.Windows.Controls.Gantt.Schedule"
                    xmlns:Chart="clr-namespace:Syncfusion.Windows.Controls.Gantt.Chart"
                    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero"
                    xmlns:Grid="clr-namespace:Syncfusion.Windows.Controls.Gantt.Grid">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.Gantt.Wpf;component/GanttChart/Themes/GanttChart.xaml"/>
        <ResourceDictionary Source="/Syncfusion.Gantt.Wpf;component/GanttChart/Themes/GanttChartItems.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <!--ScrollBar Brushes -->
<LinearGradientBrush x:Key="VerticalScrollBarBackground"
                         StartPoint="0,0"
                         EndPoint="1,0">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#E1E1E1"
                          Offset="0"/>
            <GradientStop Color="#EDEDED"
                          Offset="0.20"/>
            <GradientStop Color="#EDEDED"
                          Offset="0.80"/>
            <GradientStop Color="#E3E3E3"
                          Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="ForegroundBrush" Color="Black"/>

    <SolidColorBrush x:Key="ScrollBarDisabledBackground"
                     Color="#F4F4F4"/>
    <LinearGradientBrush x:Key="HorizontalScrollBarBackground"
                         StartPoint="0,0"
                         EndPoint="0,1">
        <LinearGradientBrush.GradientStops>
            <GradientStop Color="#E1E1E1"
                          Offset="0"/>
            <GradientStop Color="#EDEDED"
                          Offset="0.20"/>
            <GradientStop Color="#EDEDED"
                          Offset="0.80"/>
            <GradientStop Color="#E3E3E3"
                          Offset="1"/>
        </LinearGradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="DefaultCornerBackground" Color="#EDEDED"/>
    
    <!-- Gantt Control Brushes-->
    
    <SolidColorBrush x:Key="ganttBackground" Color="#FFFEFEFE"/>
    <SolidColorBrush x:Key="ganttBorderBrush" Color="LightGray"/>

    <!-- Gantt Chart Brushes-->
    <SolidColorBrush x:Key="chartBackground" Color="#FFFEFEFE"/>
    <SolidColorBrush x:Key="gridSplitterBackground" Color="#FFFEFEFE"/>
    <SolidColorBrush x:Key="gridSplitterBorderBrush" Color="#FF8591A2"/>

    <!-- Schedule brushes -->
    <LinearGradientBrush x:Key="scheduleheaderbg" MappingMode="RelativeToBoundingBox"
                         EndPoint="0.5, 1"
                         StartPoint="0.5,0">
        <GradientStop Color="#FF4A5D80"
                      Offset="0.225" />
        <GradientStop Color="#FF3E5378"
                      Offset="0.885" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="scheduleheaderfg">#FFFFFFFF</SolidColorBrush>
    <SolidColorBrush x:Key="scheduleborderbrush">#FF8591A2</SolidColorBrush>

    <!-- Scroll Bar Button Styles-->   
  
    <Style x:Key="ScrollBarButton"
           TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle"
                Value="true"/>
        <Setter Property="Focusable"
                Value="false"/>
        <Setter Property="IsTabStop"
                Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <theme:ScrollChrome Name="Chrome"
                                        ScrollGlyph="{TemplateBinding theme:ScrollChrome.ScrollGlyph}"
                                        RenderMouseOver="{TemplateBinding IsMouseOver}"
                                        RenderPressed="{TemplateBinding IsPressed}"
                                        SnapsToDevicePixels="true"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- scroll Bar Thumb Styles-->
    <Style x:Key="ScrollBarThumb"
           TargetType="{x:Type Thumb}">
        <Setter Property="OverridesDefaultStyle"
                Value="true"/>
        <Setter Property="IsTabStop"
                Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <theme:ScrollChrome x:Name="Chrome"
                                        ScrollGlyph="{TemplateBinding theme:ScrollChrome.ScrollGlyph}"
                                        RenderMouseOver="{TemplateBinding IsMouseOver}"
                                        RenderPressed="{TemplateBinding IsDragging}"
                                        SnapsToDevicePixels="true"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Scroll Bar Page Button Styles-->
    
    <Style x:Key="HorizontalScrollBarPageButton"
           TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle"
                Value="true"/>
        <Setter Property="Background"
                Value="Transparent"/>
        <Setter Property="Focusable"
                Value="false"/>
        <Setter Property="IsTabStop"
                Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Rectangle Fill="{TemplateBinding Background}"
                               Width="{TemplateBinding Width}"
                               Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style x:Key="VerticalScrollBarPageButton"
           TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle"
                Value="true"/>
        <Setter Property="Background"
                Value="Transparent"/>
        <Setter Property="Focusable"
                Value="false"/>
        <Setter Property="IsTabStop"
                Value="false"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RepeatButton}">
                    <Rectangle Fill="{TemplateBinding Background}"
                               Width="{TemplateBinding Width}"
                               Height="{TemplateBinding Height}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Scroll Bar Style-->
    
    <Style x:Key="DefaultScrollBarStyle"
           TargetType="{x:Type ScrollBar}">
        <Setter Property="Background"
                Value="{StaticResource VerticalScrollBarBackground}"/>
        <Setter Property="Stylus.IsPressAndHoldEnabled"
                Value="false"/>
        <Setter Property="Stylus.IsFlicksEnabled"
                Value="false"/>
        <Setter Property="Foreground"
                Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Width"
                Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
        <Setter Property="MinWidth"
                Value="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollBar}" >
                    <Grid Name="Bg"
                          Background="{TemplateBinding Background}"
                          SnapsToDevicePixels="true">
                        <Grid.RowDefinitions>
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                            <RowDefinition Height="0.00001*"/>
                            <RowDefinition MaxHeight="{DynamicResource {x:Static SystemParameters.VerticalScrollBarButtonHeightKey}}"/>
                        </Grid.RowDefinitions>
                        <RepeatButton Style="{StaticResource ScrollBarButton}"
                                      IsEnabled="{TemplateBinding IsMouseOver}"
                                      Command="{x:Static ScrollBar.LineUpCommand}"
                                      theme:ScrollChrome.ScrollGlyph="UpArrow"/>
                        <Track Grid.Row="1"
                               Name="PART_Track"
                               IsEnabled="{TemplateBinding IsMouseOver}"
                               IsDirectionReversed="true">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Style="{StaticResource VerticalScrollBarPageButton}"
                                              Command="{x:Static ScrollBar.PageUpCommand}"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Style="{StaticResource VerticalScrollBarPageButton}"
                                              Command="{x:Static ScrollBar.PageDownCommand}"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Style="{StaticResource ScrollBarThumb}"
                                       theme:ScrollChrome.ScrollGlyph="VerticalGripper"/>
                            </Track.Thumb>
                        </Track>
                        <RepeatButton Style="{StaticResource ScrollBarButton}"
                                      Grid.Row="2"
                                      IsEnabled="{TemplateBinding IsMouseOver}"
                                      Command="{x:Static ScrollBar.LineDownCommand}"
                                      theme:ScrollChrome.ScrollGlyph="DownArrow"/>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled"
                                 Value="false">
                            <Setter TargetName="Bg"
                                    Property="Background"
                                    Value="{StaticResource ScrollBarDisabledBackground}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation"
                     Value="Horizontal">
                <Setter Property="Width"
                        Value="Auto"/>
                <Setter Property="MinWidth"
                        Value="0"/>
                <Setter Property="Height"
                        Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                <Setter Property="MinHeight"
                        Value="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarHeightKey}}"/>
                <Setter Property="Background"
                        Value="{StaticResource HorizontalScrollBarBackground}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type ScrollBar}">
                            <Grid Name="Bg"
                                  Background="{TemplateBinding Background}"
                                  SnapsToDevicePixels="true">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}"/>
                                    <ColumnDefinition Width="0.00001*"/>
                                    <ColumnDefinition MaxWidth="{DynamicResource {x:Static SystemParameters.HorizontalScrollBarButtonWidthKey}}"/>
                                </Grid.ColumnDefinitions>
                                <RepeatButton Style="{StaticResource ScrollBarButton}"
                                              IsEnabled="{TemplateBinding IsMouseOver}"
                                              Command="{x:Static ScrollBar.LineLeftCommand}"
                                              theme:ScrollChrome.ScrollGlyph="LeftArrow"/>
                                <Track Grid.Column="1"
                                       Name="PART_Track"
                                       IsEnabled="{TemplateBinding IsMouseOver}">
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource HorizontalScrollBarPageButton}"
                                                      Command="{x:Static ScrollBar.PageLeftCommand}"/>
                                    </Track.DecreaseRepeatButton>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Style="{StaticResource HorizontalScrollBarPageButton}"
                                                      Command="{x:Static ScrollBar.PageRightCommand}"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.Thumb>
                                        <Thumb Style="{StaticResource ScrollBarThumb}"
                                               theme:ScrollChrome.ScrollGlyph="HorizontalGripper"/>
                                    </Track.Thumb>
                                </Track>
                                <RepeatButton Style="{StaticResource ScrollBarButton}"
                                              Grid.Column="2"
                                              IsEnabled="{TemplateBinding IsMouseOver}"
                                              Command="{x:Static ScrollBar.LineRightCommand}"
                                              theme:ScrollChrome.ScrollGlyph="RightArrow"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsEnabled"
                                         Value="false">
                                    <Setter TargetName="Bg"
                                            Property="Background"
                                            Value="{StaticResource ScrollBarDisabledBackground}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource DefaultScrollBarStyle}" TargetType="{x:Type ScrollBar}"/>

    <!-- Scroll Viewer Style-->
    
    <Style x:Key="DefaultScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
        <Setter Property="OverridesDefaultStyle" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ScrollViewer}">
                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Rectangle x:Name="Corner" Fill="{StaticResource DefaultCornerBackground}" Grid.Column="1" Grid.Row="1"/>
                        <ScrollContentPresenter Margin="{TemplateBinding Padding}" x:Name="PART_ScrollContentPresenter" Grid.Column="0" Grid.Row="0" Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" CanContentScroll="{TemplateBinding CanContentScroll}" CanHorizontallyScroll="False" CanVerticallyScroll="False"/>
                        <ScrollBar Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_VerticalScrollBar" Grid.Column="1" Grid.Row="0" ViewportSize="{TemplateBinding ViewportHeight}" Maximum="{TemplateBinding ScrollableHeight}" Minimum="0" Value="{Binding Path=VerticalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="VerticalScrollBar"/>
                        <ScrollBar Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" Cursor="Arrow" x:Name="PART_HorizontalScrollBar" Grid.Column="0" Grid.Row="1" Orientation="Horizontal" ViewportSize="{TemplateBinding ViewportWidth}" Maximum="{TemplateBinding ScrollableWidth}" Minimum="0" Value="{Binding Path=HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" AutomationProperties.AutomationId="HorizontalScrollBar"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style BasedOn="{StaticResource DefaultScrollViewerStyle}" TargetType="{x:Type ScrollViewer}"/>   

    <!-- Gantt Control styles-->
    
    <Style TargetType="local:GanttControl"
           x:Key="GanttControlStyle">
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="BorderBrush"
                Value="{StaticResource ganttBorderBrush}" />
        <Setter Property="Background"
                Value="{StaticResource ganttBackground}" />
        <Setter Property="ScheduleBackground"
                Value="{StaticResource scheduleheaderbg}" />
        <Setter Property="ScheduleBorderBrush"
                Value="{StaticResource scheduleborderbrush}" />
        <Setter Property="ScheduleBorderThickness"
                Value="0,0,0,1" />
        <Setter Property="ScheduleForeground"
                Value="{StaticResource scheduleheaderfg}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="NonWorkingHoursBackground" Value="#FFBCC7D8"/>
        <Setter Property="ResourceContainerTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" FontWeight="Bold" />
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:GanttControl}">
                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                             ClipToBounds="True" SnapsToDevicePixels="True"
                            Background="{TemplateBinding Background}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="{TemplateBinding GridWidth}"/>
                                <ColumnDefinition Width="5"/>
                                <ColumnDefinition Width="{TemplateBinding ChartWidth}"/>
                            </Grid.ColumnDefinitions>
                            <Grid:GanttGrid x:Name="PART_GanttGrid" 
												VisualStyle="VS2010"
                                                ScrollViewerStyle="{StaticResource DefaultScrollViewerStyle}" 
                                                HeaderBackground ="{TemplateBinding GridHeaderBackground}"
                                                HeaderForeground="{TemplateBinding GridHeaderForeground}"
                                                ShowAddNewColumn="{TemplateBinding ShowAddNewColumn}"
                                                ShowDateWithTime="{TemplateBinding ShowDateWithTime}"
                                                BorderBrush="Transparent"
                                                BorderThickness="0"
                                                Grid.IsSharedSizeScope="True"
                                                Grid.Column="0" />
                            <GridSplitter Grid.Column="1" 
                                              Width="5" 
                                              Background="{StaticResource gridSplitterBackground}"
                                              BorderBrush="{StaticResource gridSplitterBorderBrush}"
                                              BorderThickness="1,0,1,0"
                                              ShowsPreview="True"
                                              VerticalAlignment="Stretch"
                                              HorizontalAlignment="Stretch"
                                              x:Name="PART_GanttGridSplitter">
                            </GridSplitter>
                            <ScrollViewer x:Name="PART_ScheduleViewScrollViewer"
                                              Grid.Column="2"                                          
                                              Style="{StaticResource DefaultScrollViewerStyle}"
                                              VerticalScrollBarVisibility="Disabled"
                                              HorizontalScrollBarVisibility="Auto">
                                <Grid Margin="-1,-1,0,0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <schedule:GanttSchedule x:Name="PART_GantSchedule" Grid.Row="0" ClipToBounds="True"
                                                                 HorizontalAlignment="Left" VerticalAlignment="Top"
                                                                 StartTime="{TemplateBinding StartTime}"
                                                                 IsUIVirtualizaionEnabled="{TemplateBinding UseOnDemandSchedule}"
                                                                 EndTime="{TemplateBinding EndTime}" SnapsToDevicePixels="True"
                                                                 BorderBrush="{TemplateBinding ScheduleBorderBrush}"
                                                                 BorderThickness="{TemplateBinding ScheduleBorderThickness}"
                                                                 Foreground="{TemplateBinding ScheduleForeground}"
                                                                 Background="{TemplateBinding ScheduleBackground}" />

                                    <Chart:GanttChart x:Name="PART_GanttChart" Grid.Row="1" TaskNodeBackground="{TemplateBinding TaskNodeBackground}"
                                                                 HighlightItemBrush="{TemplateBinding HighlightItemBrush}"
                                                                 ConnectorStroke="{TemplateBinding ConnectorStroke}"
                                                                 ToolTipTemplate="{TemplateBinding ToolTipTemplate}"
                                                                 ResourceNameVisibility="{TemplateBinding ResourceNameVisibility}"
                                                                 HorizontalAlignment="Left" 
                                                                 StartTime="{TemplateBinding StartTime}"
                                                                 EndTime="{TemplateBinding EndTime}"
                                                                 NonWorkingHoursBackground="{TemplateBinding NonWorkingHoursBackground}" 
                                                                 CurrentDateLine="{TemplateBinding CurrentDateLine}"
                                                                 StickCurrentDateLineTo="{TemplateBinding StickCurrentDateLineTo}"
                                                                 ProgressIndicatorBackground="{TemplateBinding ProgressIndicatorBackground}"
                                                                 ShowResizingTooltip="{TemplateBinding ShowResizingTooltip}"
                                                                 ShowNonWorkingHoursBackground="{TemplateBinding ShowNonWorkingHoursBackground}"
                                                                 ShowChartLines="{TemplateBinding ShowChartLines}"
																 ShowGridLinesOnZooming="{TemplateBinding ShowGridLinesOnZooming}"
                                                                 RowHeight="{TemplateBinding RowHeight}"
                                                                 ResourceNamePlacement="{TemplateBinding ResourceNamePlacement}"
                                                                 Width="{Binding ElementName=PART_GantSchedule, Path=Width}"
                                                                 Background="{StaticResource chartBackground}" />
                                </Grid>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>

    <!--Gantt Control Styles-->
    <Style TargetType="local:GanttControl"
           BasedOn="{StaticResource GanttControlStyle}">
    </Style>

</ResourceDictionary>
