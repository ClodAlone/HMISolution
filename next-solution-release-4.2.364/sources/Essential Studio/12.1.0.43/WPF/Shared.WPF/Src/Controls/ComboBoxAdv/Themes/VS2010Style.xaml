<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:skin="clr-namespace:Syncfusion.Windows.Shared"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls"
                    xmlns:Microsoft_Windows_Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero">

    <SolidColorBrush x:Key="VS2010ComboBoxForeground" Color="Black" />
    <SolidColorBrush x:Key="PathFill.Blue" Color="Black" />

    <SolidColorBrush x:Key="VS2010ComboBoxItemBackground" Color="White" />
    <SolidColorBrush x:Key="VS2010ComboBoxItemBorderBrush" Color="Transparent" />
    <SolidColorBrush x:Key="VS2010ComboBoxItemInActiveBackground" Color="#FFF1EEE1" />

    <SolidColorBrush x:Key="VS2010ComboBoxBackground" Color="White" />
    <SolidColorBrush x:Key="VS2010ComboBoxBorderBrush" Color="#FFD4D6D9" />

    <LinearGradientBrush x:Key="VS2010ComboBoxItemMouseOverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFDECA9" Offset="0.035" />
        <GradientStop Color="#FFFDE38A" Offset="0.316" />
        <GradientStop Color="#FFFDE794" Offset="0.614" />
        <GradientStop Color="#FFFDFAE0" Offset="0.947" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="VS2010ComboBoxItemMouseOverBorderBrush" Color="#FFF1CA58" />

    <LinearGradientBrush x:Key="VS2010ComboBoxToggleButtonMouseOverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFF5E193" Offset="0.101" />
        <GradientStop Color="#FFF4DA7D" Offset="0.388" />
        <GradientStop Color="#FFF4DE8C" Offset="0.784" />
        <GradientStop Color="#FFF4E6B2" Offset="0.957" />
        <GradientStop Color="#FFF4EBC4" Offset="0.964" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VS2010ComboBoxToggleButtonMouseOverBorderBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFE8C24E" />
        <GradientStop Color="#FFECCC43" Offset="1" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VS2010ComboBoxToggleButtonCheckedBorderBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFC27A2D" Offset="0.113" />
        <GradientStop Color="#FFC29A44" Offset="0.887" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VS2010ComboBoxToggleButtonCheckedBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFF3C360" Offset="0.038" />
        <GradientStop Color="#FFFAD373" Offset="0.045" />
        <GradientStop Color="#FFFEDF84" Offset="0.249" />
        <GradientStop Color="#FFFEDF84" Offset="0.962" />
        <GradientStop Color="#FFF8D375" Offset="0.968" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="VS2010ComboBoxMouseOverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="White" Offset="0.131" />
        <GradientStop Color="#FFF1F3F4" Offset="0.983" />
    </LinearGradientBrush>

    <SolidColorBrush x:Key="VS2010ComboBoxMouseOverBorderBrush" Color="#FFBBBFC4" />
    <Geometry x:Key="DownArrowGeometry">M 0 0 L 3 3 L 6 0 Z</Geometry>

    <SolidColorBrush x:Key="ComboBoxHoverBorderBrush" Color="#FFE5C365" />
    <SolidColorBrush x:Key="ComboboxToggleCheckedBrush" Color="#FFFFE8A6" />
    <LinearGradientBrush x:Key="ComboboxToggleHoverBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFFF9E9" Offset="0.102" />
        <GradientStop Color="#FFFFF3CF" Offset="0.484" />
        <GradientStop Color="#FFFFECB5" Offset="0.493" />
        <GradientStop Color="#FFFFECB5" Offset="0.991" />
        <GradientStop Color="#FFE5C365" Offset="1" />
    </LinearGradientBrush>
    <LinearGradientBrush x:Key="ComboboxItemHoverBrush" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFFFFAEC" Offset="0.086" />
        <GradientStop Color="#FFFFF4D2" Offset="0.431" />
        <GradientStop Color="#FFFFECB5" Offset="0.517" />
        <GradientStop Color="#FFFFECB5" Offset="0.983" />
        <GradientStop Color="#FFE5C365" Offset="1" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ComboboxItemHoverBorder" Color="#FFE5C365" />
    <SolidColorBrush x:Key="ComboboxItemSelectedBrush" Color="#FFFFE8A6" />
    <SolidColorBrush x:Key="ComboBoxBackground.Blue" Color="#FFF1F3F8" />
    <SolidColorBrush x:Key="ComboBoxBorderBrush.Blue" Color="#FF8591A2" />
    <SolidColorBrush x:Key="PathFill" Color="#FF1B293E" />
    <Style x:Key="BlueComboBoxReadonlyToggleButton"
           TargetType="{x:Type ToggleButton}">
        <Setter Property="OverridesDefaultStyle"
                Value="true" />
        <Setter Property="IsTabStop"
                Value="false" />
        <Setter Property="Focusable"
                Value="false" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background"
                Value="{StaticResource ComboBoxBackground.Blue}" />
        <Setter Property="BorderBrush"
                Value="{StaticResource ComboBoxBorderBrush.Blue}" />
        <Setter Property="ClickMode"
                Value="Press" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ToggleButton}">

                    <Border Name="Chrome1"  BorderThickness="{TemplateBinding BorderThickness}"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" SharedSizeGroup="ComboBoxButton" />
                            </Grid.ColumnDefinitions>
                            <Border Name="Chrome"  Background="{TemplateBinding Background}" BorderThickness="1,0,0,0"
                                        BorderBrush="Transparent"  Grid.Column="1" Width="13">
                                <Path Name="Arrow"
                                  Fill="{StaticResource PathFill}"
                                  Data="{StaticResource DownArrowGeometry}"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center" />
                            </Border>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="skin:SkinStorage.EnableTouch" Value="true">
                            <Setter Property="Data" Value="M 0 0 L 7 7 L 14 0 Z" TargetName="Arrow" />
                            <Setter Property="Background" TargetName="Chrome" Value="Transparent" />
                            <Setter Property="Width" Value="35" TargetName="Chrome" />
                            <Setter Property="Effect" TargetName="Chrome1">
                                <Setter.Value>
                                    <DropShadowEffect Direction="315" ShadowDepth="2" Opacity="0.3" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsChecked"
                                 Value="true">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ComboboxToggleCheckedBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition  Property="IsMouseOver" Value="true" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="true" />
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ComboboxToggleHoverBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                        </MultiTrigger>
                        <Trigger Property="IsKeyboardFocusWithin" SourceName="Chrome1" Value="True">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ComboboxToggleCheckedBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="true">
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ComboboxToggleCheckedBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="false" />
                                <Condition Property="IsChecked" Value="true" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="Chrome" Property="Background" Value="{StaticResource ComboboxToggleCheckedBrush}" />
                            <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                            <Setter TargetName="Chrome1" Property="BorderBrush" Value="{StaticResource ComboBoxHoverBorderBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="true" />
                                <Condition Property="IsChecked" Value="true" />
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                        <Trigger Property="IsEnabled"
                                 Value="false">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <LinearGradientBrush x:Key="ListBoxBackgroundBrushKey" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFE7EAED" Offset="0.05" />
        <GradientStop Color="#FFD4DAE4" Offset="0.95" />
    </LinearGradientBrush>
    <SolidColorBrush x:Key="ListBoxBorderBrushKey" Color="#FF9BA7B7" />
    <Style x:Key="ComboBoxFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle Margin="4,4,21,4" StrokeThickness="1"  Stroke="Black" StrokeDashArray="1 2" SnapsToDevicePixels="true" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="ComboBoxItemFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Rectangle SnapsToDevicePixels="true" />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style   TargetType="{x:Type local:ComboBoxAdv}" x:Key="VS2010ComboBoxAdvStyle">
        <Setter Property="FocusVisualStyle"   Value="{StaticResource ComboBoxFocusVisual}" />
        <Setter Property="BorderBrush"    Value="{StaticResource ComboBoxBorderBrush.Blue}" />
        <Setter Property="Background"   Value="{StaticResource ComboBoxBackground.Blue}" />
        <Setter Property="Foreground"  Value="{StaticResource  VS2010ComboBoxForeground}" />
        <Setter Property="BorderThickness"
                Value="1" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility"
                Value="Auto" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility"
                Value="Auto" />
        <Setter Property="Padding"
                Value="4,3" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ComboBoxAdv}">
                    <Grid x:Name="MainGrid" SnapsToDevicePixels="true">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0" />
                        </Grid.ColumnDefinitions>

                        <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" StaysOpen="False" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" Margin="0" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">
                            <Microsoft_Windows_Themes:SystemDropShadowChrome x:Name="Shdw" Color="#71000000" Margin="0,0,5,5" MinHeight="20" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=MainGrid}">
                                <Border x:Name="DropDownBorder" BorderBrush="{StaticResource ListBoxBorderBrushKey}" BorderThickness="1" Background="{StaticResource ListBoxBackgroundBrushKey}">
                                    <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{TemplateBinding ActualWidth}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <ScrollViewer x:Name="DropDownScrollViewer">
                                            <Grid>
                                                <Canvas HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                                    <Rectangle x:Name="OpaqueRect" Fill="{Binding Background, ElementName=DropDownBorder}" Height="{Binding ActualHeight, ElementName=DropDownBorder}" Width="{Binding ActualWidth, ElementName=DropDownBorder}" />
                                                </Canvas>
                                                <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </Grid>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </Microsoft_Windows_Themes:SystemDropShadowChrome>
                        </Popup>
                        <ToggleButton  BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource BlueComboBoxReadonlyToggleButton}" />
                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}" x:Name="ContentPresenter" ContentTemplate="{TemplateBinding SelectionBoxTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                        <ItemsControl x:Name="PART_SelectedItems" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                        <TextBlock Text="{TemplateBinding DefaultText}" x:Name="PART_DefaultText" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        <TextBox  x:Name="PART_Editable" Visibility="Collapsed"  HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}"  IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}"  VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}">
                        </TextBox>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="skin:SkinStorage.EnableTouch" Value="true">
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="Padding" Value="16,10,23,10" />
                        </Trigger>
                        <Trigger Property="HasItems" Value="false">
                            <Setter Property="MinHeight" Value="95" TargetName="DropDownBorder" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" />
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                        </Trigger>
                        <Trigger Property="AllowsTransparency" SourceName="PART_Popup" Value="true">
                            <Setter Property="Margin" Value="0,0,0,0" TargetName="DropDownBorder" />
                            <Setter Property="CornerRadius" TargetName="DropDownBorder" Value="3,3,3,3" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="true">
                            <Setter Property="IsTabStop" Value="false" />
                            <Setter TargetName="PART_Editable" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_DefaultText" Property="Visibility" Value="Collapsed" />
                            <Setter Property="AllowMultiSelect" Value="False" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <SolidColorBrush x:Key="ListBoxItemSelectedBrushKey" Color="#FFFFE8A6" />

    <Style   TargetType="{x:Type local:ComboBoxAdv}" BasedOn ="{StaticResource VS2010ComboBoxAdvStyle}" />
    <BooleanToVisibilityConverter x:Key="VisiblityConverter" />
    <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="#FFFFE8A6" />
    <Style
           TargetType="{x:Type local:ComboBoxItemAdv}" x:Key="VS2010ComboBoxItemAdvStyle">
        <Setter Property="FocusVisualStyle" Value="{StaticResource ComboBoxItemFocusVisual}" />
        <Setter Property="HorizontalContentAlignment"
                Value="{Binding Path=HorizontalContentAlignment,RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="VerticalContentAlignment"
                Value="{Binding Path=VerticalContentAlignment,RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}" />
        <Setter Property="Padding"
                Value="3,0,3,0" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ComboBoxItemAdv}">
                    <Border x:Name="Bd" CornerRadius="2" Margin="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Visibility="{Binding Path=AllowMultiSelect, Converter={StaticResource VisiblityConverter}, RelativeSource={RelativeSource AncestorType={x:Type local:ComboBoxAdv}}}" Margin="1,2,1,1" x:Name="PART_CheckBox" IsChecked="{Binding Path=IsSelected, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" />
                            <ContentPresenter Margin="1" Grid.Column="1"  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="skin:SkinStorage.EnableTouch" Value="true">
                            <Setter Property="Padding" Value="12,9,10,9" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                            <Setter Property="Margin" TargetName="Bd" Value="0" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Setter Property="BorderBrush" Value="{StaticResource ListBoxBorderBrushKey}" />
                        </Trigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=AllowMultiSelect,RelativeSource={RelativeSource AncestorType={x:Type local:ComboBoxAdv}}}" Value="True" />
                                <Condition Binding="{Binding Path=(skin:SkinStorage.EnableTouch), RelativeSource={RelativeSource Self}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Padding" Value="12,7,10,7" />
                            <Setter Property="FontSize" Value="18" />
                            <Setter Property="CornerRadius" TargetName="Bd" Value="0" />
                            <Setter Property="Margin" TargetName="Bd" Value="0" />
                            <Setter Property="BorderThickness" Value="0,0,0,1" />
                            <Setter Property="BorderBrush" Value="{StaticResource ListBoxBorderBrushKey}" />
                        </MultiDataTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="true" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="true" />
                            </MultiTrigger.Conditions>
                        </MultiTrigger>
                        <Trigger Property="IsHighlighted" Value="true">
                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBorder}" />
                            <Setter Property="Background" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBrush}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsKeyboardFocusWithin" Value="true" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBorder}" />
                            <Setter Property="Background" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="true" />
                                <Condition Property="skin:SkinStorage.EnableTouch" Value="false" />
                            </MultiTrigger.Conditions>
                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBorder}" />
                            <Setter Property="Background" TargetName="Bd" Value="{StaticResource ComboboxItemHoverBrush}" />
                        </MultiTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsSelected,RelativeSource={RelativeSource Self}}" Value="true" />
                                <Condition Binding="{Binding Path=IsKeyboardFocusWithin,RelativeSource={RelativeSource Self}}" Value="false" />
                                <Condition Binding="{Binding Path=IsHighlighted,RelativeSource={RelativeSource Self}}" Value="false" />
                                <Condition Binding="{Binding Path=AllowMultiSelect,RelativeSource={RelativeSource AncestorType={x:Type local:ComboBoxAdv}}}" Value="True" />
                            </MultiDataTrigger.Conditions>
                            <Setter Property="BorderBrush" TargetName="Bd" Value="{StaticResource ListBoxItemSelectedBrushKey}" />
                            <Setter Property="Background" TargetName="Bd" Value="{StaticResource VS2010ComboBoxItemInActiveBackground}" />
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style
           TargetType="{x:Type local:ComboBoxItemAdv}" BasedOn ="{StaticResource VS2010ComboBoxItemAdvStyle}" />
</ResourceDictionary>