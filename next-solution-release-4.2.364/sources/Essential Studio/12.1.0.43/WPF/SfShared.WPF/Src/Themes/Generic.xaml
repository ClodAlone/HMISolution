<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:local="clr-namespace:Syncfusion.Windows.Primitives" xmlns:controls="clr-namespace:Syncfusion.Windows.Controls"
    xmlns:converter="clr-namespace:Syncfusion.Windows.Converters">

    <FontFamily x:Key="SyncfusionUpDownThemeFontFamily">Segoe UI</FontFamily>
    <FontWeight x:Key="SyncfusionUpDownThemeFontWeight">SemiBold</FontWeight>

    <sys:Double x:Key="SyncfusionUpDownThemeFontSize">14.667</sys:Double>
    <Thickness x:Key="SyncfusionUpDownBorderThemeThickness">1</Thickness>
    <Thickness x:Key="SyncfusionUpDownThemePadding">12 4 12 4</Thickness>

    <SolidColorBrush x:Key="SyncfusionUpDownBorderThemeBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="SyncfusionUpDownBackgroundThemeBrush" Color="#FF002EFF" />
    <SolidColorBrush x:Key="SyncfusionUpDownAccentThemeBrush">#FF4617B4</SolidColorBrush>

    <SolidColorBrush x:Key="FocusVisualBlackStrokeThemeBrush" Color="#FF000000" />

    <SolidColorBrush x:Key="FocusVisualWhiteStrokeThemeBrush" Color="#FFFFFFFF" />


    <SolidColorBrush x:Key="ForegroundBrush.Blue" Color="Black"/>

    <SolidColorBrush x:Key="GlyphBrushKey1" Color="#FF4B4F55"/>

    <LinearGradientBrush x:Key="RepeatButtonBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFF2F2F2" Offset="0.102"/>
        <GradientStop Color="#FFE8E8E8" Offset="0.932"/>
        <GradientStop Color="#FFD1D1D1" Offset="0.966"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonBorderBrush" Color="#FFABADB3"/>
    <LinearGradientBrush x:Key="RepeatButtonHoverBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFE9F6FD" Offset="0.134"/>
        <GradientStop Color="#FFD2EDFC" Offset="0.966"/>
        <GradientStop Color="#FFE9F5FC" Offset="1"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonHoverBorderBrush" Color="#FF3C7FB1"/>
    <LinearGradientBrush x:Key="RepeatButtonPressedBackground" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFA6C7D9" Offset="0.113"/>
        <GradientStop Color="#FFC2E4F6" Offset="0.135"/>
        <GradientStop Color="#FFC2E4F6" Offset="0.842"/>
        <GradientStop Color="#FF91CCEB" Offset="0.865"/>
    </LinearGradientBrush>
    <SolidColorBrush x:Key="RepeatButtonPressedBorderBrush" Color="#FF3C7FB1"/>

    <Style TargetType="controls:TransitionContentControl">
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:TransitionContentControl">
                    <Grid x:Name="PART_LayoutRoot" Background="{TemplateBinding Background}">
                        <ContentControl IsTabStop="False" x:Name="PART_TempContent" ContentTemplate="{TemplateBinding ContentTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                        HorizontalContentAlignment="{Binding HorizontalAlignment, ElementName=PART_Content}"
                                        VerticalContentAlignment="{Binding VerticalAlignment, ElementName=PART_Content}">

                        </ContentControl>
                        <ContentPresenter x:Name="PART_Content" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">

                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

   
    <Style TargetType="ContentControl" x:Key="DefaultHeaderStyle">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="Margin" Value="10 5"/>
    </Style>

    <Style TargetType="ContentControl" x:Key="DefaultTitleStyle">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="FontSize" Value="10"/>
    </Style>


    <controls:RotateTransition Duration="0:0:0.4" Easing="{x:Null}" x:Key="RotateTransition"/>
    
    <Style TargetType="controls:SfNavigator">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="VerticalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SfNavigator">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid >

                            <ContentControl x:Name="PART_SupportingContent" 
                                            IsTabStop="False"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch">
                                <ContentControl.RenderTransform>
                                    <TranslateTransform/>                                   
                                </ContentControl.RenderTransform>
                            </ContentControl>

                            <ContentControl x:Name="PART_Content" 
                                            IsTabStop="False"
                                            HorizontalAlignment="Stretch"
                                            VerticalAlignment="Stretch"
                                            HorizontalContentAlignment="Stretch"
                                            VerticalContentAlignment="Stretch">
                                <ContentControl.RenderTransform>
                                    <TranslateTransform />
                                </ContentControl.RenderTransform>
                            </ContentControl>

                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <ControlTemplate x:Key="UpRepeatButtonTemplate" TargetType="{x:Type RepeatButton}">
        <Border Name="Chrome" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                                        CornerRadius="0"                                        
                                        SnapsToDevicePixels="true">
            <Border Name="innerBorder" BorderBrush="{TemplateBinding Background}" BorderThickness="1" CornerRadius="0" SnapsToDevicePixels="True">
                <ContentPresenter x:Name="ContentPresenter" 
                                              ContentTemplate="{TemplateBinding ContentTemplate}"                                             
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                    <ContentPresenter.Content>
                        <Path x:Name="path" Data="F1M181.297,177.841L181.205,177.746 181.385,177.563 202.804,156.146 202.804,135.07 178.497,159.373 170.847,167.026 170.666,167.205 163.107,159.653 138.804,135.345 138.804,156.42 160.219,177.841 170.76,188.379 181.297,177.841z"
                                  Stretch="Uniform" Fill="{StaticResource GlyphBrushKey1}" Width="12" Height="12" Margin="0,0,0,0" RenderTransformOrigin=".5,.5">
                            <Path.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform Angle="180"/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </Path.RenderTransform>
                        </Path>
                    </ContentPresenter.Content>
                </ContentPresenter>
            </Border>
        </Border>
        <ControlTemplate.Triggers>           
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource RepeatButtonPressedBorderBrush}"/>
                <Setter TargetName="innerBorder" Property="BorderBrush" Value="Transparent"/>
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource RepeatButtonPressedBackground}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.5"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="UpRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="Background"                                
                Value="{StaticResource RepeatButtonBackground}"/>
        <Setter Property="BorderBrush"
                Value="{StaticResource RepeatButtonBorderBrush}"/>
        <Setter Property="Foreground"
                Value="{StaticResource ForegroundBrush.Blue}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="HorizontalContentAlignment"
                Value="Center"/>
        <Setter Property="VerticalContentAlignment"
                Value="Center"/>
        <Setter Property="Padding"
                Value="0"/>
        <Setter Property="Template" Value="{StaticResource UpRepeatButtonTemplate}"/>
    </Style>

    <ControlTemplate x:Key="DownRepeatButtonTemplate" TargetType="{x:Type RepeatButton}">
        <Border Name="Chrome" 
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                                        CornerRadius="0"                                        
                                        SnapsToDevicePixels="true">
            <Border Name="innerBorder" BorderBrush="{TemplateBinding Background}" BorderThickness="1" CornerRadius="0" SnapsToDevicePixels="True">
                <ContentPresenter x:Name="ContentPresenter" 
                                              ContentTemplate="{TemplateBinding ContentTemplate}"                                            
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              Margin="{TemplateBinding Padding}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                    <ContentPresenter.Content>
                        <Path x:Name="path" Data="F1M181.297,177.841L181.205,177.746 181.385,177.563 202.804,156.146 202.804,135.07 178.497,159.373 170.847,167.026 170.666,167.205 163.107,159.653 138.804,135.345 138.804,156.42 160.219,177.841 170.76,188.379 181.297,177.841z"
                                  Stretch="Uniform" Fill="{StaticResource GlyphBrushKey1}" Width="12" Height="12" Margin="0,0,0,0"/>
                    </ContentPresenter.Content>
                </ContentPresenter>
            </Border>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter TargetName="Chrome" Property="BorderBrush" Value="{StaticResource RepeatButtonPressedBorderBrush}"/>
                <Setter TargetName="innerBorder" Property="BorderBrush" Value="Transparent"/>
                <Setter TargetName="Chrome" Property="Background" Value="{StaticResource RepeatButtonPressedBackground}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Chrome" Property="Opacity" Value="0.5"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="DownRepeatButtonStyle" TargetType="{x:Type RepeatButton}">
        <Setter Property="OverridesDefaultStyle" Value="true"/>
        <Setter Property="Background"                                
                Value="{StaticResource RepeatButtonBackground}"/>
        <Setter Property="BorderBrush"
                Value="{StaticResource RepeatButtonBorderBrush}"/>
        <Setter Property="Foreground"
                Value="{StaticResource ForegroundBrush.Blue}"/>
        <Setter Property="BorderThickness"
                Value="1"/>
        <Setter Property="HorizontalContentAlignment"
                Value="Center"/>
        <Setter Property="VerticalContentAlignment"
                Value="Center"/>
        <Setter Property="Padding"
                Value="0"/>
        <Setter Property="Template" Value="{StaticResource DownRepeatButtonTemplate}"/>
    </Style>

    <Style x:Key="UpDownStyle" TargetType="controls:SfUpDown">
        <Setter Property="AccentBrush" Value="{StaticResource SyncfusionUpDownAccentThemeBrush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:SfUpDown">
                    <Grid x:Name="PART_OuterGrid">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ContentControl x:Name="PART_Content" Content="{TemplateBinding UpDownContent}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch"/>
                        <RepeatButton x:Name="PART_DownButton" HorizontalAlignment="Stretch" Background="{TemplateBinding AccentBrush}" 
                                      VerticalAlignment="Stretch"  Grid.Column="1" IsTabStop="False"  
                                      Style="{StaticResource DownRepeatButtonStyle}" Command="{TemplateBinding DownCommand}">

                        </RepeatButton>
                        <RepeatButton x:Name="PART_UpButton" HorizontalAlignment="Stretch" Background="{TemplateBinding AccentBrush}"
                                      VerticalAlignment="Stretch" Grid.Column="2" IsTabStop="False" 
                                      Style="{StaticResource UpRepeatButtonStyle}" 
                                      Command="{TemplateBinding UpCommand}">
                        </RepeatButton>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="controls:SfUpDown" BasedOn="{StaticResource UpDownStyle}"/>

</ResourceDictionary>

    