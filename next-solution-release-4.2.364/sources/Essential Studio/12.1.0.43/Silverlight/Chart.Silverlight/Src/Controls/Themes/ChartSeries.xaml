<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:toolkit="clr-namespace:Syncfusion.Windows.Controls;assembly=Syncfusion.Shared.Silverlight"
    xmlns:shared="clr-namespace:Syncfusion.Windows.Shared;assembly=Syncfusion.Shared.Silverlight"
    xmlns:syncfusion="clr-namespace:Syncfusion.Windows.Chart">

    <!--Series template-->
    <Style TargetType="syncfusion:ChartSeries">
        <Setter Property="Stroke" Value="#8A53564F"/>
        <Setter Property="StrokeThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>

                <ControlTemplate TargetType="syncfusion:ChartSeries">
                    <Grid x:Name="series" >
                        <syncfusion:SeriesPresenter x:Name="SeriesPresenter_Part" SegmentTemplate="{TemplateBinding ActualSegmentTemplate}" Segments="{TemplateBinding Segments}" 
                                  Margin="0" />

                        <syncfusion:AnnotationPresenter IsTabStop="False" ItemsSource="{Binding Annotations, RelativeSource={RelativeSource TemplatedParent}}">
                            <syncfusion:AnnotationPresenter.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <syncfusion:AnnotationPanel />
                                </ItemsPanelTemplate>
                            </syncfusion:AnnotationPresenter.ItemsPanel>
                        </syncfusion:AnnotationPresenter>
                    </Grid>

                </ControlTemplate>

            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
