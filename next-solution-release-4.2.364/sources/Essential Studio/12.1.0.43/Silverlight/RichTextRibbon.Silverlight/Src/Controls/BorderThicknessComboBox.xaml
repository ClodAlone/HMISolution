<UserControl x:Class="Syncfusion.Windows.Tools.Controls.BorderThicknessComboBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ribbon="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Ribbon.Silverlight"
    xmlns:this="clr-namespace:Syncfusion.Windows.Tools.Controls"
    >
    
    <Grid>
        <ribbon:RibbonComboBox x:Name="Combo"  ItemsSource="{Binding ComboBoxItems}" >
            <ribbon:RibbonComboBox.ItemTemplate>
                <DataTemplate >
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Path=Text}"/>
                        <Rectangle Margin="2" Fill="{Binding Path=FillBrush}" Width="70" Grid.Column="1" Height="{Binding Path=Height}"/>
                    </Grid>
                </DataTemplate>
            </ribbon:RibbonComboBox.ItemTemplate>
        </ribbon:RibbonComboBox>
    </Grid>
</UserControl>