<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
	xmlns:local="clr-namespace:Syncfusion.Silverlight.Tools.Olap"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/Syncfusion.OlapClient.Silverlight;component/Tools/SplitButton/SplitButton.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="local:AxisElementBuilder">
        <Setter Property="Background" Value="White"/>
        <Setter Property="BorderBrush" Value="Gray"/>
        <Setter Property="Padding" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:AxisElementBuilder">
                    <Border x:Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
							CornerRadius="5" Background="{TemplateBinding Background}">
                        <ScrollViewer Margin="2,2,2.5,2" x:Name="ScrollViewer" BorderBrush="Transparent"  BorderThickness="0"  
								 Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}"  
								 HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" TabNavigation="Once">
                            <ItemsPresenter Margin="0.5"/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="3"/>
                    <Setter Property="HorizontalContentAlignment" Value="Left"/>
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="TabNavigation" Value="Local"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Grid Background="{TemplateBinding Background}">
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroup x:Name="CommonStates">
                                            <VisualState x:Name="Normal"/>
                                            <VisualState x:Name="MouseOver"/>
                                            <VisualState x:Name="Disabled"/>
                                        </VisualStateGroup>
                                        <VisualStateGroup x:Name="SelectionStates">
                                            <VisualState x:Name="Unselected"/>
                                            <VisualState x:Name="Selected"/>
                                        </VisualStateGroup>
                                        <VisualStateGroup x:Name="FocusStates">
                                            <VisualState x:Name="Focused"/>
                                            <VisualState x:Name="Unfocused"/>
                                        </VisualStateGroup>
                                    </VisualStateManager.VisualStateGroups>
                                    <!--<Rectangle x:Name="fillColor" Fill="#FFBADDE9" IsHitTestVisible="False" Opacity="0" RadiusY="1" RadiusX="1"/>
                                    <Rectangle x:Name="fillColor2" Fill="#FFBADDE9" IsHitTestVisible="False" Opacity="0" RadiusY="1" RadiusX="1"/>-->
                                    <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" Margin="{TemplateBinding Padding}"/>
                                    <!--<Rectangle x:Name="FocusVisualElement" RadiusY="1" RadiusX="1" Stroke="#FF6DBDD1" StrokeThickness="1" Visibility="Collapsed"/>-->
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
        <!--<Setter Property="ItemTemplate">
			<Setter.Value>
				<DataTemplate>
					<local:SplitButton MetaTreeNode="{Binding}"/>
				</DataTemplate>
			</Setter.Value>
		</Setter>-->
    </Style>

    <DataTemplate x:Key="DefaultStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource DefaultSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="BlendStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource BlendSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2007BlueStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2007BlueSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2007BlackStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2007BlackSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2007SilverStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2007SilverSplitButtonStyle}"/>
    </DataTemplate>
    
    <DataTemplate x:Key="MetroStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource MetroSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2010BlueStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2010BlueSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2010BlackStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2010BlackSplitButtonStyle}"/>
    </DataTemplate>

    <DataTemplate x:Key="Office2010SilverStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource Office2010SilverSplitButtonStyle}"/>
    </DataTemplate>
    
    <DataTemplate x:Key="TransparentStyle">
        <local:SplitButton MetaTreeNode="{Binding}" Style="{StaticResource TransparentSplitButtonStyle}"/>
    </DataTemplate>
</ResourceDictionary>
