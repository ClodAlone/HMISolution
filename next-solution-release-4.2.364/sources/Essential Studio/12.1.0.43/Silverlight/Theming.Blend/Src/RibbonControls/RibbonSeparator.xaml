<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Ribbon.Silverlight" >

    <Style TargetType="local:RibbonSeparator" x:Key="BlendRibbonSeperatorStyle">
		<Setter Property="Height" Value="20"/>
        <Setter Property="Margin" Value="1.5,0,0,0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RibbonSeparator">
                    <Grid>
                        <Line X1="1" Stroke="Gray" Name="PART_Horizontal" Stretch="Fill" />
                        <Grid Name="PART_Vertical" Visibility="Collapsed">
                            <Border Margin="{TemplateBinding Margin}" Width="1" Height="{TemplateBinding Height}" BorderBrush="#FF5C5C5C" Background="#FF5C5C5C"/>
                            <Border Margin="{TemplateBinding Margin}" Width="1" Height="{TemplateBinding Height}" BorderBrush="#FF5C5C5C" Background="#FF5C5C5C"/>

                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style TargetType="local:RibbonSeparator" BasedOn="{StaticResource BlendRibbonSeperatorStyle}"/>
</ResourceDictionary>
