<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controlsToolkit="clr-namespace:Syncfusion.Windows.Controls;assembly=Syncfusion.Shared.Silverlight"
  xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Tools.Silverlight">
    

    <!-- Brushes -->
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="CommonBrushes.xaml"/>
    </ResourceDictionary.MergedDictionaries>


    <!--<SolidColorBrush x:Key="TaskBarItemHeaderBackgroundBrushKey" Color="#FFE8EAEC"/>-->
    <LinearGradientBrush x:Key="TaskBarItemHeaderBackgroundBrushKey" EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FFDEE3EB" Offset="0"/>
        <GradientStop Color="#FFE0E4EC" Offset="0.489"/>
        <GradientStop Color="#FFE2E6ED" Offset="1"/>
    </LinearGradientBrush>

    
        <LinearGradientBrush x:Key="TaskBarItemBackgroundBrushKey" EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="#FFDEE3EB" Offset="0"/>
            <GradientStop Color="#FFE0E4EC" Offset="0.489"/>
            <GradientStop Color="#FFE2E6ED" Offset="1"/>
        </LinearGradientBrush>

    <SolidColorBrush x:Key="TextBrushKey" Color="#FF15428A"/>

    <SolidColorBrush x:Key="TaskBarItemBorderBrushKey" Color="#FF979CA2"/>
    <SolidColorBrush x:Key="ToggleButtonFillBrushKey" Color="#656870"/>


    <LinearGradientBrush x:Key="MouseOverBrushKey" EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="White" Offset="0"/>
            <GradientStop Color="#FFE7EAEE" Offset="0.002"/>
            <GradientStop Color="#FFFEFEFE" Offset="0.52"/>
            <GradientStop Color="#FFE9ECEF" Offset="0.955"/>
        </LinearGradientBrush>


    <Style x:Key="Office2007SilverTaskBarItemStyle" TargetType="local:TaskBarItem">
        <Setter Property="Background" Value="{StaticResource TaskBarItemBackgroundBrushKey}"/>
        <Setter Property="BorderBrush" Value="{StaticResource TaskBarItemBorderBrushKey}"/>
        <Setter Property="Foreground" Value="{StaticResource TextBrushKey}"/>
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="IsHeaderShown" Value="True"/>
        <Setter Property="HeaderHeight" Value="25"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TaskBarItem">

                    <Grid x:Name="MainGrid"  >
                        <Grid VerticalAlignment="Top" HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="{TemplateBinding HeaderHeight}"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid x:Name="HeaderGrid"   Background="{StaticResource TaskBarItemHeaderBackgroundBrushKey}" >
                                <Border x:Name="HeaderBorder" BorderBrush="{StaticResource TaskBarItemBorderBrushKey}" 
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}" Padding="1">
                                    <controlsToolkit:DockPanel x:Name="Head" LastChildFill="False">
                                        <ToggleButton Background="{StaticResource ToggleButtonFillBrushKey}" Style="{StaticResource ToggleButton}" controlsToolkit:DockPanel.Dock="Right"  x:Name="ExpanderButton" HorizontalAlignment="Right" VerticalAlignment="Stretch" IsTabStop="False"  Margin="3"/>
                                        <ContentPresenter controlsToolkit:DockPanel.Dock="Left" x:Name="HeaderContent" Content="{TemplateBinding Header}" ContentTemplate="{Binding RelativeSource={RelativeSource TemplatedParent},  Path=HeaderTemplate}" HorizontalAlignment="Left" Margin="3" VerticalAlignment="Center"/>
                                    </controlsToolkit:DockPanel>
                                </Border>
                            </Grid>

                            <Grid x:Name="ContentGrid"   Background="{TemplateBinding Background}" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Top" >
                                <Border x:Name="ContentBorder" BorderBrush="{TemplateBinding BorderBrush}"
                                    Padding="1"        
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Background="{TemplateBinding Background}">
                                    <Grid>
                                        <ContentPresenter x:Name="TaskBarContent"  Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ItemContentTemplate}">
                                        </ContentPresenter>
                                    </Grid>
                                </Border>
                            </Grid>
                        </Grid>

                    </Grid>

                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007SilverTaskBarItemStyle}" TargetType="local:TaskBarItem"/>

    <Style x:Key="Office2007SilverTaskBarStyle" TargetType="local:TaskBar">
        <Setter Property="Foreground" Value="{StaticResource TextBrushKey}"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate  TargetType="local:TaskBar">
                    <Border x:Name="MainBorder"  BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                     Background="{TemplateBinding Background}">
                        <StackPanel  x:Name="MainGrid" Orientation="Horizontal">
                            <ItemsPresenter />
                        </StackPanel>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource Office2007SilverTaskBarStyle}" TargetType="local:TaskBar">
        <Setter Property="ItemContainerStyle" Value="{StaticResource Office2007SilverTaskBarItemStyle}"/>
    </Style>

</ResourceDictionary>
