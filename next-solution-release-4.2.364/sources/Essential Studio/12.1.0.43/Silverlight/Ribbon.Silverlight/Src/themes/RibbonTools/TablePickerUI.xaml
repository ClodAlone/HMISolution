<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:Syncfusion.Windows.Tools.Controls;assembly=Syncfusion.Ribbon.Silverlight">
    
    <local:TableInfoToStringConverter x:Key="StringConverter"/>
    
    <Style TargetType="local:TablePickerUI">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TablePickerUI">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Border Background="LightGray">
                            <TextBlock Margin="2" FontWeight="Black" Text="{Binding Path=SelectedCell, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource StringConverter}}"/>
                        </Border>
                        <Border Grid.Row="1" x:Name="PART_Host">
                            <Grid x:Name="PART_Items" >

                            </Grid>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local:TablePickerItem">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:TablePickerItem">
                    <Grid Background="Transparent">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal"/>
                              
                                <VisualState x:Name="Validated">
                                    <Storyboard>
                                    <ObjectAnimationUsingKeyFrames Storyboard.TargetName="border" Storyboard.TargetProperty="BorderBrush">
                                        <DiscreteObjectKeyFrame KeyTime="0" Value="Orange"/>
                                    </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="border" Storyboard.TargetProperty="BorderThickness">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="2"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="border" Width="15" Height="15" Margin="2" BorderThickness="1" BorderBrush="Gray" Background='Transparent'/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
