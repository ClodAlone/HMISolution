<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:Chart="clr-namespace:Syncfusion.Windows.Controls.Gantt.Chart"
                    xmlns:local="clr-namespace:Syncfusion.Windows.Controls.Gantt">

    <Style TargetType="Chart:GanttChart">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:GanttChart">
                    <Border Background="{TemplateBinding Background}" Margin="0.25,0,0,0">
                        <ScrollViewer x:Name="PART_GanttChartScrollViewer" Padding="{TemplateBinding Padding}" 
                                      VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Disabled">
                            <ScrollViewer.Template>
                                <ControlTemplate TargetType="ScrollViewer">
                                    <Grid x:Name="Grid" Background="{TemplateBinding Background}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Chart:GanttChartBackgroundPanel Grid.Row="0" Grid.Column="0"  x:Name="PART_GanttChartBackgroundPanel" 
                                                                         HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                                        <Chart:GanttChartStripLinePanel IsHitTestVisible="True" Grid.Row="0" Grid.Column="0" x:Name="PART_GanttChartStripLinePanel" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                        <ScrollContentPresenter x:Name="PART_ScrollContentPresenter" CanHorizontallyScroll="False"
                                                                CanVerticallyScroll="False" ContentTemplate="{TemplateBinding ContentTemplate}" Content="{TemplateBinding Content}" Grid.Column="0"
                                                                Margin="{TemplateBinding Padding}" Grid.Row="0"/>
                                        <Chart:GanttNodeConnector Grid.Column="0" Grid.Row="0" x:Name="PART_GanttNodeConnectorPanel" HorizontalAlignment="Stretch" IsHitTestVisible="False"
                                                         VerticalAlignment="Stretch"  Background="Transparent"/>
                                        <ScrollBar x:Name="PART_VerticalScrollBar" AutomationProperties.AutomationId="VerticalScrollBar" Cursor="Arrow" Maximum="{TemplateBinding ScrollableHeight}"
                                                   Minimum="0" Grid.Row="0" Grid.Column="1" Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}" Value="{Binding VerticalOffset, 
                                            Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportHeight}"/>
                                        <ScrollBar x:Name="PART_HorizontalScrollBar" AutomationProperties.AutomationId="HorizontalScrollBar" Cursor="Arrow" Maximum="{TemplateBinding ScrollableWidth}"
                                                   Minimum="0" Orientation="Horizontal" Grid.Row="1" Grid.Column="0" Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}" 
                                                   Value="{Binding HorizontalOffset, Mode=OneWay, RelativeSource={RelativeSource TemplatedParent}}" ViewportSize="{TemplateBinding ViewportWidth}"/>
                                    </Grid>
                                </ControlTemplate>
                            </ScrollViewer.Template>
                            <ItemsPresenter/>
                        </ScrollViewer>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <Chart:GanttChartItemsPanel/>
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Chart:GanttNodeConnector">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:GanttNodeConnector">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Path Data="{TemplateBinding ConnectorGeometry}"
                                  Name="PART_Connetions"
                                  Stroke="{TemplateBinding ConnectorStroke}"
                                  Stretch="None"
                                  StrokeThickness="1"/>
                            <Path Data="{TemplateBinding ArrowGeometry}"
                                  Name="PART_Arrows"
                                  Stroke="{TemplateBinding ConnectorStroke}"
                                  Stretch="None"
                                  Fill="{TemplateBinding ConnectorStroke}"
                                  StrokeThickness="1" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Chart:StripLine">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:StripLine">
                    <Border Width="{TemplateBinding Width}"
                            Height="{TemplateBinding Height}"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Stretch"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter x:Name="PART_ParentContent"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}">
                            <!--Projection of content not rendered correctly in silverlight so we add the inner content presentor here-->
                            <ContentPresenter x:Name="PART_InnerContent"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}">
                                <ContentPresenter.Projection>
                                    <PlaneProjection RotationZ="90" />
                                </ContentPresenter.Projection>
                            </ContentPresenter>
                        </ContentPresenter>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Chart:GanttChartRow">
        <Setter Property="BorderBrush"
                Value="LightGray" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Chart:GanttChartRow">
                    <Border BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="Transparent">
                        <Canvas x:Name="PART_BackgoundCanvas" Grid.Row="0">
                            <Rectangle x:Name="PART_ResizingRect" Height="{TemplateBinding ActualHeight}"  Visibility="Collapsed" Stroke="Gray" StrokeThickness="1.5" />
                            <Chart:GanttChartRowItemsPresenter Height="{TemplateBinding ActualHeight}" x:Name="ItemsPresenterElement" />
                            <Popup x:Name="PART_DateTimeResizingTooltip" IsOpen="False" Width="Auto" Height="Auto">
                                <Border Background="White" Width="225" Height="70" BorderThickness="0.5" BorderBrush="Gray">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5" >
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Task Name: " Margin="5,2,2,2" FontWeight="Bold" />
                                                <TextBlock Width="125" Grid.Column="1" Text="{Binding TaskName}" FontWeight="Bold" TextTrimming="WordEllipsis" Margin="5,2,2,2" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock FontWeight="Bold" Text="StartTime :" Margin="5" />
                                            <TextBlock Grid.Column="1" Text="{Binding StartTime,Mode=TwoWay}" Margin="5" />
                                            <TextBlock FontWeight="Bold" Grid.Row="1" Text="EndTime :" Margin="5" />
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding EndTime,Mode=TwoWay}" Margin="5" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                            <Popup x:Name="PART_NumericResizingTooltip" IsOpen="False" Width="Auto" Height="Auto">
                                <Border Background="White" Width="150" Height="70" BorderThickness="0.5" BorderBrush="Gray">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5" >
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Task Name: " Margin="5,2,2,2" FontWeight="Bold" />
                                                <TextBlock Width="80" Grid.Column="1" Text="{Binding TaskName}" FontWeight="Bold" TextTrimming="WordEllipsis" Margin="5,2,2,2" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock FontWeight="Bold" Text="Start :" Margin="5" />
                                            <TextBlock Grid.Column="1" Text="{Binding Start}" Margin="5" />
                                            <TextBlock FontWeight="Bold" Grid.Row="1" Text="End :" Margin="5" />
                                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding End}" Margin="5" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                            <Popup x:Name="PART_ProgressResizingTooltip" IsOpen="False" Width="Auto" Height="Auto">
                                <Border Background="White" Width="200" Height="50" BorderThickness="0.5" BorderBrush="Gray">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border BorderBrush="Gray" BorderThickness="0,0,0,0.5" >
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="Task Name: " Margin="5,2,2,2" FontWeight="Bold" />
                                                <TextBlock Text="{Binding TaskName}" Grid.Column="1" FontWeight="Bold" Width="125" TextTrimming="WordEllipsis" Margin="5,2,2,2" />
                                            </Grid>
                                        </Border>
                                        <Grid Grid.Row="1">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock FontWeight="Bold" Text="Progress :" Margin="5" />
                                            <TextBlock Grid.Column="1" Text="{Binding Progress}" Margin="5" />
                                        </Grid>
                                    </Grid>
                                </Border>
                            </Popup>
                        </Canvas>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>