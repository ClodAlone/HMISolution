<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                    xmlns:dxi="http://schemas.devexpress.com/winfx/2008/xaml/core/internal"
                    xmlns:dxet="http://schemas.devexpress.com/winfx/2008/xaml/editors/themekeys"
                    xmlns:dxct="http://schemas.devexpress.com/winfx/2008/xaml/core/themekeys"
                    xmlns:System="clr-namespace:System;assembly=mscorlib"
                    xmlns:z="clr-namespace:FastControls">

    <Style TargetType="z:SparklineChart">
      <Setter Property="Background" Value="{dxi:ThemeResource {dxct:FloatingContainerThemeKey ResourceKey=FloatingContainerBackground}}"/>
      <Setter Property="Foreground" Value="{dxi:ThemeResource {dxet:TextEditThemeKey ResourceKey=CaretBrush}}"/>
      <Setter Property="Width" Value="450"/>
        <Setter Property="Height" Value="300"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="z:SparklineChart">
                    <ControlTemplate.Resources>
                        <Style x:Key="{x:Type TextBlock}" TargetType="{x:Type TextBlock}">
                            <Setter Property="Foreground" Value="{Binding LabelBrush,RelativeSource={RelativeSource AncestorType=z:SparklineChart,Mode=FindAncestor}}"/>
                        </Style>
                    </ControlTemplate.Resources>
                    <Border x:Name="mainBorder" ClipToBounds="False"
                            Background="{Binding Background, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}"
                            BorderBrush="{Binding BorderBrush, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}"
                            BorderThickness="{Binding BorderThickness, RelativeSource={RelativeSource AncestorType={x:Type UserControl}, Mode=FindAncestor}}">
                        <Grid>
                            <Grid x:Name="mainGrid" ClipToBounds="True">
                                <DockPanel LastChildFill="True">
                                    <TextBlock x:Name="argumentLabel" Margin="0,0,0,10" DockPanel.Dock="Bottom" Foreground="{Binding LabelBrush}" HorizontalAlignment="Center" Text="{Binding ArgumentTitle}"/>
                                    <TextBlock x:Name="valueLabel" Margin="0,0,0,10" DockPanel.Dock="Left" Foreground="{Binding LabelBrush}" VerticalAlignment="Center" Text="{Binding ValueTitle}">
                                        <TextBlock.LayoutTransform>
                                            <RotateTransform Angle="-90"/>
                                        </TextBlock.LayoutTransform>
                                    </TextBlock>
                                    <Grid x:Name="plotArea">
                                        <Grid Grid.ColumnSpan="20000" Grid.RowSpan="20000" x:Name="gridArea">
                                            <Grid.ColumnDefinitions>
                                            </Grid.ColumnDefinitions>
                                        </Grid>
                                        <dxe:SparklineEdit x:Name="sparklineEdit"
                                               PointValueMember="ValueColumn" PointArgumentMember="ArgumentColumn" >
                                        </dxe:SparklineEdit>
                                    </Grid>
                                </DockPanel>
                            </Grid>
                            <TextBlock Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="WrapWithOverflow" IsHitTestVisible="False" Opacity="0.25" FontSize="22" x:Name="statusText" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>