<UserControl x:Class="ScreenManager.Popups.ItemControlSourceProperties"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:properties="clr-namespace:ScreenManager.Properties"
    Height="200" Width="300" VerticalContentAlignment="Top">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <StackPanel>
            <TextBlock Text="{x:Static properties:Resources.ItemControlSourceHelper}"/>
            <Grid x:Name="gridText">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="30"/>
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding Path=ListItemSources, UpdateSourceTrigger=PropertyChanged}" x:Name="TextEditor" AcceptsReturn="True" AcceptsTab="True" />
                <Button Click="Button_Click" Grid.Column="1">
                    <Button.Content>
                        <Image Source="{StaticResource EditGeneralSmall}" Height="16" Width="16" />
                    </Button.Content>
                </Button>
            </Grid>
            <Button Content="{x:Static properties:Resources.ItemControlSourceDataSourceEditor}" Click="DataEditor_Click"/>
            <Button Content="{x:Static properties:Resources.ItemControlSourceClearDataSource}" Click="ClearDataSource_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
