<phone:PhoneApplicationPage x:Class="UFWebClient.Views.ScreenContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:UFWebClient.Controls"
    xmlns:TiltEffect="clr-namespace:ControlTiltEffect"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape" Orientation="Portrait"
    shell:SystemTray.IsVisible="False"
    TiltEffect:TiltEffect.IsTiltEnabled="True">
    <Grid x:Name="LayoutRoot">
        <local:BusyIndicator x:Name="busyIndicator" Style="{StaticResource busyIndicatorDefaultStyle}">
            <Grid>
                <local:ScreenViewer x:Name="screenViewer" />
                <Rectangle Height="20" HorizontalAlignment="Right" Opacity="1" Name="status" Stroke="Black" Fill="LightGreen" StrokeThickness="1" VerticalAlignment="Bottom" Width="20" RadiusY="10" RadiusX="10" />
            </Grid>
        </local:BusyIndicator>
        <ProgressBar x:Name="progressDownload" Visibility="Collapsed" Opacity=".5" VerticalAlignment="Bottom" Height="20" />
    </Grid>

    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True" Opacity="0.5">
            <shell:ApplicationBarIconButton IconUri="/Images/CreateGroupBox.png" Text="Fill" Click="ApplicationBarIconButton_Click" />
            <shell:ApplicationBarIconButton IconUri="/Images/StyleEditor.png" Text="Scroll" Click="ApplicationBarIconButton_Click_1" />
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>