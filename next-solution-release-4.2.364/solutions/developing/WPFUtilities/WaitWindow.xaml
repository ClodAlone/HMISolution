<UserControl x:Class="WPFUtilities.WaitWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:properties="clr-namespace:WPFUtilities.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <dx:WaitIndicator DeferedVisibility="True" Content="{Binding State}">
            <!-- DeferedVisibility="{Binding IsBusy}"-->
            <dx:WaitIndicator.ContentTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{x:Static properties:Resources.WaitIndicatorTitle}" FontSize="20"/>
                        <TextBlock Text="{Binding}"/>
                        <!--<ProgressBar 
                             Height="12" 
                             Margin="12" 
                             IsIndeterminate="{Binding IsIndeterminate}" 
                             Value="{Binding Progress}" 
                             Maximum="{Binding MaxProgress}"/>-->
                    </StackPanel>
                </DataTemplate>
            </dx:WaitIndicator.ContentTemplate>
        </dx:WaitIndicator>
    </Grid>
</UserControl>
