<UserControl x:Class="SymbolGallery.Controls.NewFolder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             FocusManager.FocusedElement="{Binding ElementName=txtboxFolderName}"
             Width="300" Height="25">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" VerticalAlignment="Center">Folder Name</TextBlock>
        <TextBox Grid.Column="1" x:Name="txtboxFolderName" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="3 0 0 0" />
    </Grid>
</UserControl>
