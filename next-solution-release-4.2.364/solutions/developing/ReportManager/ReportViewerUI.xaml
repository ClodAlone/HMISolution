<UserControl x:Class="ReportManager.ReportViewerUI"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:cc="http://progea.com/CommonControls" 
             xmlns:dxp="http://schemas.devexpress.com/winfx/2008/xaml/printing" 
             xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
             xmlns:dxpbars="http://schemas.devexpress.com/winfx/2008/xaml/printing/bars"
             mc:Ignorable="d" 
             xmlns:properties="clr-namespace:ReportManager.Properties"
             d:DesignHeight="300" d:DesignWidth="300" >
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid x:Name="gridView">
        <dxp:DocumentPreviewControl x:Name="preview" CommandBarStyle="Ribbon"
                                    dxb:BarNameScope.IsScopeOwner="True" >
            <dxp:DocumentPreviewControl.CommandProvider>
                <dxp:DocumentCommandProvider>
                    <!--<dxp:DocumentCommandProvider.RibbonActions>
                        <dxb:RemoveAction ElementName="{x:Static dxpbars:DefaultPreviewBarItemNames.FileGroup}"/>
                    </dxp:DocumentCommandProvider.RibbonActions>-->
                </dxp:DocumentCommandProvider>
            </dxp:DocumentPreviewControl.CommandProvider>

        </dxp:DocumentPreviewControl>
        
        <StackPanel x:Name="uriLoading" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed">
            <cc:CircularProgressBar Background="Black" Opacity="0.5" />
            <TextBlock Text="{x:Static properties:Resources.WaitText}" VerticalAlignment="Center" Opacity="0.5"
                       FontFamily="Segoe UI" FontWeight="Light" Margin="5,0,0,0" HorizontalAlignment="Left" Foreground="White" FontSize="32" />
        </StackPanel>

        <DockPanel x:Name="uriError" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="Collapsed" >
            <Image Source="{StaticResource Diamond}" Width="16" Height="16" Margin="2,2,2,2" />
            <TextBlock x:Name="txtUriError" Margin="5,0,0,0" VerticalAlignment="Center"
            TextWrapping="Wrap" FontFamily="Segoe UI" FontWeight="Light" HorizontalAlignment="Left" Foreground="White" FontSize="16" />
        </DockPanel>

    </Grid>
</UserControl>
