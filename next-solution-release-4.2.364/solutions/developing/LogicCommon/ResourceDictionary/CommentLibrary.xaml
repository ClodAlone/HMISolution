<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:go="http://schemas.nwoods.com/GoXam"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <go:BooleanBrushConverter x:Key="theValueConverter"
                              FalseBrush="Red" TrueBrush="Green" />
    <go:BooleanBrushConverter x:Key="theSelectionConverter"
                              FalseBrush="Black" TrueBrush="Cyan" />

    <go:DataTemplateDictionary x:Key="NodeTemplateDictionary">

        <DataTemplate x:Key="Comment">
            <go:NodePanel go:Node.Location="{Binding Path=Data.Location, Mode=TwoWay}" go:Part.SelectionAdorned="True"
                            Sizing="Fixed"
                            go:Part.SelectionElementName="Shape"
                            go:Part.Resizable="True">
                <go:NodeShape x:Name="Shape"
                    go:NodePanel.Figure="Document"
                     Width="{Binding Path=Data.Width, Mode=TwoWay}"
                     Height="{Binding Path=Data.Height, Mode=TwoWay}"
                    Stroke="{Binding Path=Node.IsSelected,
                                Converter={StaticResource theSelectionConverter}}" 
                    StrokeThickness="2" Fill="White" />
                <TextBlock Text="{Binding Path=Data.Text, Mode=TwoWay}" Margin="10" Foreground="Black"
                TextWrapping="Wrap" Cursor="Arrow" TextAlignment="Center" 
                VerticalAlignment="Center" HorizontalAlignment="Center"
                go:Node.LinkableFrom="False" go:Node.LinkableTo="False"
                go:Part.TextEditable="True" />
            </go:NodePanel>
        </DataTemplate>

    </go:DataTemplateDictionary>

</ResourceDictionary>
