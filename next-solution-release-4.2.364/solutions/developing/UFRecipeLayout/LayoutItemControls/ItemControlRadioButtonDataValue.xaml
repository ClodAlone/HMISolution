<UserControl x:Class="UFRecipeLayout.LayoutItemControls.ItemControlRadioButtonDataValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
             Width="Auto" Height="Auto" >

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="{x:Type ListBoxItem}">
                <Setter Property="SnapsToDevicePixels" Value="true"/>
                <Setter Property="OverridesDefaultStyle" Value="true"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <RadioButton IsThreeState="False"
                                        x:Name="radio" Click="Radio_Click">
                                <RadioButton.Content>
                                    <ContentPresenter 
                                        Content="{Binding}" 
                                        ContentTemplate="{TemplateBinding ContentControl.ContentTemplate}" 
                                        ContentStringFormat="{TemplateBinding ContentControl.ContentStringFormat}" 
                                        HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" 
                                        VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" 
                                        SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
                                </RadioButton.Content>
                            </RadioButton>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!--<ComboBox x:Name="textDataValue" Validation.ErrorTemplate="{x:Null}" />-->
        <ListBox x:Name="textDataValue" Validation.ErrorTemplate="{x:Null}" SelectionChanged="ListBox_SelectionChanged"  Background="Transparent"/>
        <ContentPresenter Grid.Column="1"
            Content="{Binding ElementName=textDataValue, Path=(Validation.Errors).CurrentItem}" />
        
    </Grid>
</UserControl>
