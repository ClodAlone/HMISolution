<UserControl x:Class="Gauges.PropertyDataTemplate.TimeZonePropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">
    <Grid>
        <Grid.ColumnDefinitions>
        <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Button x:Name="timezone" Tag="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Visibility="Collapsed" />
        <ComboBox x:Name="timezones" IsTextSearchEnabled="True" IsEditable="False" 
                  SelectedValue="{Binding Value.TimeZone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                  SelectionChanged="timezones_SelectionChanged" >
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <Label Width="{Binding Width, RelativeSource={RelativeSource AncestorType=ComboBox,Mode=FindAncestor}}" Content="{Binding DisplayName}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </Grid>
</UserControl>
