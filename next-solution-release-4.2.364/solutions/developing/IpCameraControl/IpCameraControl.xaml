<UserControl x:Class="IpCameraControl.IpCameraControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:cc="http://progea.com/CommonControls"
             mc:Ignorable="d"
             xmlns:properties="clr-namespace:IpCameraControl.Properties"
             xmlns:converters="clr-namespace:Converters;assembly=Converters"
             Height="300" Width="300">
    <UserControl.Resources>
        <ResourceDictionary>
            <converters:InverseTransformConverter x:Key="InverseTransformConverter" />
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid Background="Gray" x:Name="view">
        <Image x:Name="image">
            <Image.LayoutTransform>
                <MultiBinding Converter="{StaticResource InverseTransformConverter}">
                    <Binding Path="KeepAspectRatio" UpdateSourceTrigger="PropertyChanged" RelativeSource="{RelativeSource AncestorType=UserControl}"/>
                    <Binding Path="." RelativeSource="{RelativeSource AncestorType=Canvas}" />
                    <Binding Path="ActualWidth" UpdateSourceTrigger="PropertyChanged" RelativeSource="{RelativeSource AncestorType=Window}" />
                    <Binding Path="ActualHeight" UpdateSourceTrigger="PropertyChanged" RelativeSource="{RelativeSource AncestorType=Window}" />
                </MultiBinding>
            </Image.LayoutTransform>
        </Image>
        <cc:CircularProgressBar Background="Black" x:Name="busyControl" Opacity="0.5" Grid.RowSpan="3" Grid.ColumnSpan="2" Visibility="Collapsed"/>
        <TextBlock x:Name="busyContent" Background="Black" Opacity="0.7" Padding="10" Text="{x:Static properties:Resources.WaitText}" VerticalAlignment="Center" Visibility="Collapsed"
                   Grid.RowSpan="3" Grid.ColumnSpan="2" TextAlignment="Center" HorizontalAlignment="Center" Foreground="White"/>
    </Grid>
</UserControl>
