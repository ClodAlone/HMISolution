<Window x:Class="WPFTestClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFTestClient"
        mc:Ignorable="d" WindowState="Maximized"
        Title="MainWindow" Height="350" Width="525">
    <Window.Resources>
        <DataTemplate x:Key="ItemTemplate">
            <Border BorderThickness="1" CornerRadius="0">
                <Border.Style>
                    <Style TargetType="{x:Type Border}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Border.BorderBrush" Value="#FFB9D7FC" />
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter Property="Border.BorderBrush" Value="Transparent" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Image Source="{Binding Image}"/>
                    <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Center" Text="{Binding StatusText}" Foreground="White">
                        <TextBlock.Effect>
                            <DropShadowEffect ShadowDepth="0" BlurRadius="5" Color="Red"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <ScrollViewer x:Name="scrollView" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Grid x:Name="layoutGrid" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Image x:Name="backGround" />
                <Canvas x:Name="mainSurface" Background="{x:Null}" MouseDown="mainSurface_MouseDown" MouseUp="mainSurface_MouseUp"/>
            </Grid>
        </ScrollViewer>
        <Image x:Name="blinkImage" Visibility="Collapsed" VerticalAlignment="Bottom" HorizontalAlignment="Right" Source="Images\status.png" Width="24" Height="24"/>
        <Image x:Name="blinkImageOk" Visibility="Collapsed" VerticalAlignment="Bottom" HorizontalAlignment="Right" Source="Images\statusOK.png" Width="24" Height="24"/>
        <Image x:Name="blinkImageErr" Visibility="Collapsed" VerticalAlignment="Bottom" HorizontalAlignment="Right" Source="Images\statusErr.png" Width="24" Height="24"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button x:Name="btnConnect" Content="Connect" Click="BtnConnect_Click" Width="100" Height="100"/>
            <Button x:Name="btnSettings" Grid.Column="1" Content="Settings" Click="BtnSettings_Click" Width="100" Height="100"/>
        </Grid>
        <Grid x:Name="inputPanel" Visibility="Collapsed" Margin="5">
            <Rectangle Fill="Black" Opacity="0.5"/>
            <Grid VerticalAlignment="Center" Background="{Binding Path=Background, ElementName=progressBar}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid >
                    <CheckBox x:Name="checkBox" Visibility="Collapsed" Margin="5"/>
                    <ListBox x:Name="listBox" Visibility="Collapsed" Margin="5"/>
                    <TextBox x:Name="textBox" Visibility="Collapsed" Margin="5"/>
                </Grid>
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnOk" Width="{Binding Path=ActualWidth, ElementName=btnCancel}" Content="Ok" Click="btnOk_Click" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5"/>
                    <Button Grid.Column="1" x:Name="btnCancel" Content="Cancel" Click="btnCancel_Click" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="5"/>
                </Grid>
            </Grid>
        </Grid>
        <Grid x:Name="busyPanel" Visibility="Collapsed" Margin="5">
            <Rectangle Fill="Black" Opacity="0.5"/>
            <Grid VerticalAlignment="Center" Background="{Binding Path=Background, ElementName=progressBar}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Border CornerRadius="5" Background="{Binding Path=Background, ElementName=progressBar}" VerticalAlignment="Center" HorizontalAlignment="Center" >
                    <TextBlock x:Name="StatusText" Text="Processing..." FontSize="26.667" Margin="10"/>
                </Border>
                <ProgressBar x:Name="progressBar" IsIndeterminate="True" 
                             Height="{Binding Path=ActualHeight, ElementName=StatusText}" 
                             Width="{Binding Path=ActualWidth, ElementName=StatusText}"
                             Grid.Row="1" VerticalAlignment="Top"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
