<UserControl x:Class="UFUAEditor.PropertyDataTemplate.TimeDelayPropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ms="http://namespaces.mindscape.co.nz/wpf" 
             xmlns:converters="clr-namespace:UFUAEditor.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
              <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!--<converters:PropertyGridManyValueConverter x:Key="ManyValueConverter" />-->
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ms:TimeSpanPicker x:Name="timeSpanControl" SelectedTimeSpanChanged="timeSpanControl_SelectedTimeSpanChanged"
                           SelectedTimeSpan="{Binding Value, Mode=TwoWay, ValidatesOnDataErrors=True}" 
                           Validation.ErrorTemplate="{x:Null}"
                           HorizontalAlignment="Stretch" VerticalAlignment="Center" IsUpDownVisible="True" />
        <ContentPresenter Grid.Column="1" 
            Content="{Binding ElementName=timeSpanControl, Path=(Validation.Errors).CurrentItem}"/>

    </Grid>
</UserControl>
