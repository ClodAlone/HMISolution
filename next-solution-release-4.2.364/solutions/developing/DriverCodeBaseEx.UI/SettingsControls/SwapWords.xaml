<UserControl x:Class="DriverCodeBaseEx.UI.SettingsControls.SwapWords"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DriverCodeBaseEx.UI.SettingsControls"
             xmlns:properties="clr-namespace:DriverCodeBaseEx.UI.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/SharedResources;component/Resources/DataError.xaml" />
                <ResourceDictionary Source="/SharedResources;component/Resources/SharedImages.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition SharedSizeGroup="B" Width="Auto"/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition SharedSizeGroup="E" Width="Auto"/>
            <ColumnDefinition Width="10"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="{x:Static properties:Resources.SwapWords}" VerticalAlignment="Center" x:FieldModifier="public" Name="CheckSwapWordsText" Margin="0,1"/>
        <CheckBox Grid.Column="2" VerticalAlignment="Center" x:Name="CheckSwapWords" x:FieldModifier="public" Margin="0,5"
                  IsChecked="{Binding SwapWords, ValidatesOnDataErrors=True, UpdateSourceTrigger = PropertyChanged}"></CheckBox>
        <ContentPresenter Grid.Column="3" x:FieldModifier="public" Name="CheckSwapWordsErr"
            Content="{Binding ElementName=CheckSwapWords, Path=(Validation.Errors).CurrentItem}"/>
    </Grid>
</UserControl>
