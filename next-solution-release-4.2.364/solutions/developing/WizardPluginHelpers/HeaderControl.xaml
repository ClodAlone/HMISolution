<UserControl x:Class="WizardPluginHelpers.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             Background="{x:Null}"
             mc:Ignorable="d" xmlns:ed="http://schemas.microsoft.com/expression/2010/drawing"
             xmlns:properties="clr-namespace:WizardPluginHelpers.Properties" >
  <Grid HorizontalAlignment="Stretch">
    <Rectangle Grid.Row="0" IsHitTestVisible="False" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
      <Rectangle.Fill>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
          <GradientStop Color="#19000000" Offset="0"/>
          <GradientStop Color="#19000000" Offset="1"/>
        </LinearGradientBrush>
      </Rectangle.Fill>
    </Rectangle>
    <Grid Margin="5,0" HorizontalAlignment="Left" Background="{x:Null}">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="60"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="32"/>
        <RowDefinition Height="32"/>
      </Grid.RowDefinitions>
      <Image Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.RowSpan="2" Width="48" Height="48"
      Source="{Binding Image, RelativeSource={RelativeSource AncestorType=UserControl, Mode=FindAncestor}}"/>
      <TextBlock Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Left"  Background="{x:Null}" x:Name="titleLabel"
             Text="{Binding HeaderTitle, RelativeSource={RelativeSource AncestorType=UserControl, Mode=FindAncestor}}"
             FontWeight="Bold"/>
      <TextBlock x:Name="stepLabel" Grid.Row="1" Grid.Column="1"  Background="{x:Null}"
             VerticalAlignment="Top"
             FontWeight="Bold"/>
    </Grid>
  </Grid>    
</UserControl>